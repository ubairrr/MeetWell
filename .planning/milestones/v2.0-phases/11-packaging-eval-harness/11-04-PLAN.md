---
phase: 11-packaging-eval-harness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/corpus/test_11_standard_sync_05.json
  - eval/corpus/test_12_standard_sync_06.json
  - eval/corpus/test_13_standard_sync_07.json
  - eval/corpus/test_14_standard_sync_08.json
  - eval/corpus/test_15_standard_sync_09.json
  - eval/corpus/test_16_standard_sync_10.json
  - eval/corpus/test_17_action_item_dense_01.json
  - eval/corpus/test_18_action_item_dense_02.json
  - eval/corpus/test_19_action_item_dense_03.json
  - eval/corpus/test_20_action_item_dense_04.json
  - eval/corpus/test_21_action_item_dense_05.json
  - eval/corpus/test_22_action_item_dense_06.json
  - eval/corpus/test_23_action_item_dense_07.json
  - eval/corpus/test_24_action_item_dense_08.json
  - eval/corpus/test_25_action_item_dense_09.json
  - eval/corpus/test_26_action_item_dense_10.json
  - eval/corpus/test_27_date_heavy_01.json
  - eval/corpus/test_28_date_heavy_02.json
  - eval/corpus/test_29_date_heavy_03.json
  - eval/corpus/test_30_date_heavy_04.json
  - eval/corpus/test_31_date_heavy_05.json
  - eval/corpus/test_32_date_heavy_06.json
  - eval/corpus/test_33_date_heavy_07.json
  - eval/corpus/test_34_date_heavy_08.json
  - eval/corpus/test_35_date_heavy_09.json
  - eval/corpus/test_36_date_heavy_10.json
  - eval/corpus/test_37_high_speaker_count_01.json
  - eval/corpus/test_38_high_speaker_count_02.json
  - eval/corpus/test_39_high_speaker_count_03.json
  - eval/corpus/test_40_high_speaker_count_04.json
  - eval/corpus/test_41_high_speaker_count_05.json
  - eval/corpus/test_42_fabrication_bait_05.json
  - eval/corpus/test_43_fabrication_bait_06.json
  - eval/corpus/test_44_fabrication_bait_07.json
  - eval/corpus/test_45_fabrication_bait_08.json
  - eval/corpus/test_46_fabrication_bait_09.json
  - eval/corpus/test_47_fabrication_bait_10.json
  - eval/corpus/test_48_attribution_bait_01.json
  - eval/corpus/test_49_attribution_bait_02.json
  - eval/corpus/test_50_attribution_bait_03.json
  - eval/corpus/test_51_attribution_bait_04.json
  - eval/corpus/test_52_attribution_bait_05.json
  - eval/corpus/test_53_implicit_inference_01.json
  - eval/corpus/test_54_implicit_inference_02.json
  - eval/corpus/test_55_implicit_inference_03.json
  - eval/corpus/test_56_implicit_inference_04.json
  - eval/corpus/test_57_implicit_inference_05.json
  - eval/corpus/test_58_short_no_content_03.json
  - eval/corpus/test_59_short_no_content_04.json
  - eval/corpus/test_60_short_no_content_05.json
autonomous: true
requirements:
  - PACK-04
  - PACK-05
must_haves:
  truths:
    - eval/corpus/ contains exactly 60 test_*.json files after this plan completes
    - Each new file validates against AdversarialTestCase format (transcript_id, category, transcript, ground_truth with action_items + decisions + dates arrays)
    - fabrication_bait and attribution_bait files include a non-empty adversarial_injections array with expected_behavior field
    - implicit_inference_traps files include at least one action_item in ground_truth with a source_quote that uses hedging language ("might", "should probably", "it would be good if")
    - Transcript format is consistent with existing files: [Meeting date: YYYY-MM-DD] header + [MM:SS] Speaker: text lines
    - No test_id is duplicated across the corpus
  artifacts:
    - eval/corpus/ (50 new JSON files added)
  key_links:
    - eval/harness.ts (11-05) loads all test_*.json from eval/corpus/ and validates them
---

<objective>
Expand the adversarial eval corpus from 10 to 60 test cases. The AI-SPEC mandates 60 synthetic transcripts across 8 categories (§3.6). Currently 10 exist. This plan generates the remaining 50.

Each file follows the AdversarialTestCase format. Ground truth items must be accurate — they are what the harness measures the pipeline against. Adversarial categories must include realistic traps: fabrication bait has plausible-but-unspoken commitments adjacent to real ones; attribution bait has similar speaker names/roles; implicit inference has outcomes that were discussed but never explicitly agreed.

Output: 50 new JSON files in eval/corpus/. No code changes.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/11-packaging-eval-harness/11-CONTEXT.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/11-packaging-eval-harness/11-RESEARCH.md
@/Users/ubair/Gits/MeetingAssist/eval/corpus/test_01_standard_sync_01.json
@/Users/ubair/Gits/MeetingAssist/eval/corpus/test_05_fabrication_bait_01.json
@/Users/ubair/Gits/MeetingAssist/eval/corpus/test_09_short_no_content_01.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate 6 standard_sync cases (test_11 – test_16)</name>
  <files>eval/corpus/test_11_standard_sync_05.json through test_16_standard_sync_10.json</files>
  <behavior>
    - Each transcript: 4–6 speakers, 15–45 minutes simulated, realistic business topics (product roadmap, bug triage, team retrospective, budget review, onboarding, API design)
    - Ground truth contains 2–5 action items with explicit assignees, 1–3 decisions, 0–2 dates
    - No adversarial_injections field (standard category — no traps)
    - source_quote in ground_truth is a verbatim excerpt from the transcript field
  </behavior>
  <action>
Create each file as valid JSON matching AdversarialTestCase format. Use varied topics and speaker names. Examples: team retrospective (You + Speaker 1-3), API design review (You + Speaker 1-4), budget planning (You + Speaker 1-2), onboarding checklist (You + Speaker 1), code review meeting (You + Speaker 1-3), product roadmap (You + Speaker 1-4).

For each meeting, ensure:
1. The `[Meeting date: YYYY-MM-DD]` header is present on line 1 of transcript
2. Speaker turns use [MM:SS] format (not HH:MM:SS)
3. Every ground_truth item has a source_quote that is an exact substring of the transcript
4. action_items have assignee_label matching a speaker name used in the transcript
5. due_date is ISO-8601 (YYYY-MM-DD) or null

Write all 6 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.startsWith('test_1')).filter(f=>f.includes('standard_sync'));files.forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(!d.transcript_id||!d.ground_truth)throw new Error(f+' invalid')});console.log('ok',files.length,'files')"</automated>
  </verify>
  <done>6 test_1X_standard_sync_*.json files exist in eval/corpus/, each valid JSON with required fields.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate 10 action_item_dense cases (test_17 – test_26)</name>
  <files>eval/corpus/test_17_action_item_dense_01.json through test_26_action_item_dense_10.json</files>
  <behavior>
    - Each transcript: 4–6 speakers, 30–60 min simulated, dense with commitments — 6–12 action items across the meeting
    - Some action items are conditional ("if we get sign-off, Speaker 2 will do X") — these should be in ground_truth with the conditional context noted in description
    - Some action items are ambiguous assignee ("someone should look into this") — these are NOT in ground_truth (no clear assignee, not extractable as concrete action items)
    - Dates are included for some items (ISO-8601 or relative expressions like "by end of sprint", "before the demo")
    - No adversarial_injections field
  </behavior>
  <action>
Create 10 files with varied domains: engineering sprint planning, sales pipeline review, marketing campaign planning, legal compliance review, infrastructure migration planning, design system review, customer success sync, finance quarterly review, security audit planning, hiring pipeline review.

For each, include 6–12 clear action items in ground_truth.action_items. At least 2 items per file should have non-null due_date.

Write all 10 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('action_item_dense'));if(files.length!==10)throw new Error('expected 10 got '+files.length);files.forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(d.ground_truth.action_items.length&lt;6)throw new Error(f+' too few action items')});console.log('ok')"</automated>
  </verify>
  <done>10 test_1X_action_item_dense_*.json files exist; each has >= 6 ground_truth action items.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Generate 10 date_heavy cases (test_27 – test_36)</name>
  <files>eval/corpus/test_27_date_heavy_01.json through test_36_date_heavy_10.json</files>
  <behavior>
    - Each transcript is dense with dates: multiple explicit deadlines, relative dates ("next Tuesday", "by end of Q3", "in two weeks"), and unresolvable dates ("soon", "as soon as possible")
    - ground_truth.dates array is populated: items with resolvable dates have resolved_date as ISO-8601; items with unresolvable expressions have resolved_date: null
    - Relative dates are resolved relative to [Meeting date:] in the transcript header
    - The pipeline SHOULD extract resolvable dates with correct ISO-8601 resolution and set due_date: null with raw_deadline_text for unresolvable ones
    - No adversarial_injections field
  </behavior>
  <action>
Create 10 files covering: release planning (milestone dates), sales targets (quarterly deadlines), legal compliance (statutory deadlines), product launches (launch date + pre-launch milestones), hiring (offer deadlines, start dates), vendor contracts (renewal dates), conference preparation (submission deadlines), budget cycles (fiscal year end dates), onboarding (90-day plan checkpoints), incident postmortem (follow-up deadlines).

Each file should have 4–8 dates in ground_truth.dates. At least 2 per file should have unresolvable expressions (resolved_date: null).

Meeting date headers should span different months (some 2026-06, some 2026-07, some 2026-08) so relative date resolution is tested against different reference points.

Write all 10 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('date_heavy'));if(files.length!==10)throw new Error('expected 10 got '+files.length);files.forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(d.ground_truth.dates.length&lt;4)throw new Error(f+' too few dates')});console.log('ok')"</automated>
  </verify>
  <done>10 test_2X_date_heavy_*.json files exist; each has >= 4 ground_truth.dates items.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Generate 5 high_speaker_count cases (test_37 – test_41)</name>
  <files>eval/corpus/test_37_high_speaker_count_01.json through test_41_high_speaker_count_05.json</files>
  <behavior>
    - Each transcript: 7–8 speakers (You + Speaker 1 through Speaker 6 or 7), 30–60 min simulated
    - Action items are assigned to specific speakers — the test measures whether the harness correctly attributes action items to the right speaker (diarization stress test)
    - ground_truth.action_items all have specific assignee_label values that match speaker names in the transcript
    - No adversarial_injections field
  </behavior>
  <action>
Create 5 files covering large team meetings: all-hands engineering sync (8 participants), cross-functional product review (7 participants), executive strategy session (7 participants), multi-team incident postmortem (8 participants), company OKR planning (7 participants).

Speakers: You, Speaker 1, Speaker 2, Speaker 3, Speaker 4, Speaker 5, Speaker 6 (and Speaker 7 for 8-participant meetings).

Each speaker should have at least one action item in ground_truth to stress-test attribution accuracy.

Write all 5 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('high_speaker_count'));if(files.length!==5)throw new Error('expected 5 got '+files.length);console.log('ok')"</automated>
  </verify>
  <done>5 test_3X_high_speaker_count_*.json files exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Generate 6 more fabrication_bait cases (test_42 – test_47)</name>
  <files>eval/corpus/test_42_fabrication_bait_05.json through test_47_fabrication_bait_10.json</files>
  <behavior>
    - Each transcript contains plausible-but-unspoken commitments adjacent to real ones — designed to bait the model into extracting items that were never actually committed to
    - adversarial_injections array is REQUIRED; expected_behavior is 'not-extracted' for items that should NOT be in the pipeline's output
    - ground_truth.action_items contains ONLY the genuinely committed items (not the bait items)
    - Examples of fabrication bait: "We should probably do X someday" adjacent to "I'll definitely do Y by Friday" — "X" should not be extracted; "Y" should be
  </behavior>
  <action>
Create 6 files with varied fabrication bait patterns:
1. "Someone should look into" adjacent to a firm commitment
2. Past tense mention ("we talked about doing X") adjacent to new commitment
3. Rejected idea ("we decided NOT to do X") adjacent to accepted alternative
4. Hypothetical discussion ("if we were to launch in Europe, we'd need...") adjacent to actual commitment
5. Vague wish ("it would be great if someone could...") adjacent to explicit assignment
6. Attributed to wrong person (overheard "I heard you're going to...") adjacent to the real assignee's commitment

adversarial_injections description should explain what was injected and why it shouldn't be extracted.

Write all 6 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('fabrication_bait'));if(files.length!==10)throw new Error('expected 10 got '+files.length);files.slice(4).forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(!d.adversarial_injections||d.adversarial_injections.length===0)throw new Error(f+' missing adversarial_injections')});console.log('ok')"</automated>
  </verify>
  <done>10 total fabrication_bait files exist; new files have non-empty adversarial_injections.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Generate 5 attribution_bait cases (test_48 – test_52)</name>
  <files>eval/corpus/test_48_attribution_bait_01.json through test_52_attribution_bait_05.json</files>
  <behavior>
    - Each transcript has multiple speakers with similar names or roles, making attribution difficult
    - Patterns: two speakers both named "Alex" (Alex from engineering, Alex from design); two senior managers (Manager A and Manager B) with indistinguishable speaking styles; multiple Speakers with similar commitments across topics
    - adversarial_injections array documents the attribution trap
    - expected_behavior is 'not-extracted' when the injected item is assigned to the WRONG speaker — or 'flagged-inferred' when assignee is genuinely ambiguous
    - ground_truth.action_items has correct assignee_labels matching the actual speaker who committed
  </behavior>
  <action>
Create 5 files with varied attribution traps:
1. Two "Alex" speakers — one commits to task, other is mentioned in context
2. Two senior managers giving assignments — one delegates to the other, one self-assigns
3. A speaker quotes another speaker's past commitment ("Speaker 1 said they'd do X") — X should be attributed to Speaker 1
4. Speaker 2 volunteers for a task that Speaker 3 was asked to do — correct assignee is Speaker 2
5. Group commitment ("the frontend team will handle...") — no specific assignee — should be excluded or flagged inferred

Write all 5 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('attribution_bait'));if(files.length!==5)throw new Error('expected 5 got '+files.length);console.log('ok')"</automated>
  </verify>
  <done>5 test_4X_attribution_bait_*.json files exist with non-empty adversarial_injections.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Generate 5 implicit_inference_traps cases (test_53 – test_57)</name>
  <files>eval/corpus/test_53_implicit_inference_01.json through test_57_implicit_inference_05.json</files>
  <behavior>
    - Each transcript has meetings where outcomes were discussed but never explicitly agreed — the model must flag these as confidence: 'inferred' not 'direct'
    - Examples: "We'll probably go with option A" (not a decision); "I think Speaker 2 could handle that" (not a commitment); "Let's circle back on X" (deferred, not decided)
    - adversarial_injections documents which items should be 'flagged-inferred' if extracted
    - ground_truth.action_items that ARE in the corpus have confidence: 'inferred' noted in their description
    - The harness checks that if the pipeline extracts these items, they must carry confidence: 'inferred', not 'direct'
  </behavior>
  <action>
Create 5 files covering implicit inference scenarios:
1. "We'll probably..." / "I think we should..." — hedged future actions
2. "Let's take this offline" — acknowledged but not committed
3. Implicitly assigned by role ("the PM usually handles this") — role-based, no explicit commitment
4. A decision that was proposed but never voted on or agreed to explicitly
5. "Can someone look into X?" — request without confirmed volunteer

adversarial_injections.expected_behavior = 'flagged-inferred' for each implicit item.

Write all 5 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('implicit_inference'));if(files.length!==5)throw new Error('expected 5 got '+files.length);console.log('ok')"</automated>
  </verify>
  <done>5 test_5X_implicit_inference_*.json files exist with adversarial_injections having expected_behavior: 'flagged-inferred'.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Generate 3 more short_no_content cases (test_58 – test_60)</name>
  <files>eval/corpus/test_58_short_no_content_03.json through test_60_short_no_content_05.json</files>
  <behavior>
    - Each transcript is a short meeting (2–8 min) with no commitments, no decisions, and no extractable dates
    - The pipeline SHOULD extract nothing — ground_truth.action_items = [], decisions = [], dates = []
    - Tests that the pipeline does not fabricate artifacts from small talk or purely informational content
    - Examples: a 5-min check-in where everyone says "all good"; a quick status update with only information shared and no commitments; a team social "coffee chat"
    - No adversarial_injections field
  </behavior>
  <action>
Create 3 files covering:
1. A 3-minute check-in — everyone says they're unblocked, no commitments made
2. A 6-minute team social — casual conversation about weekend plans, no work commitments
3. An 8-minute informational briefing — speaker presents information, no questions or commitments from attendees

Write all 3 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.includes('short_no_content'));if(files.length!==5)throw new Error('expected 5 got '+files.length);files.forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(d.ground_truth.action_items.length>0)throw new Error(f+' should have empty action_items')});console.log('ok')"</automated>
  </verify>
  <done>5 total short_no_content files exist; new files have empty ground_truth arrays.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Verify corpus totals</name>
  <files>eval/corpus/</files>
  <behavior>
    - eval/corpus/ contains exactly 60 test_*.json files
    - Category distribution: standard_sync=10, action_item_dense=10, date_heavy=10, high_speaker_count=5, fabrication_bait=10, attribution_bait=5, implicit_inference=5 (implicit_inference_traps in some files), short_no_content=5
    - No duplicate transcript_id values
    - All files are valid JSON
  </behavior>
  <action>
Run a verification count:
```bash
ls eval/corpus/test_*.json | wc -l
# should output 60

node -e "
const fs = require('fs')
const files = fs.readdirSync('eval/corpus').filter(f => f.match(/^test_.*\.json$/))
console.log('Total files:', files.length)
const ids = new Set()
const cats = {}
files.forEach(f => {
  const d = JSON.parse(fs.readFileSync('eval/corpus/' + f, 'utf8'))
  if (ids.has(d.transcript_id)) throw new Error('Duplicate id: ' + d.transcript_id)
  ids.add(d.transcript_id)
  cats[d.category] = (cats[d.category] || 0) + 1
})
console.log('Categories:', cats)
console.log('All ids unique: true')
"
```

If count is not 60 or any category is wrong, fix by adding missing files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('eval/corpus').filter(f=>f.match(/^test_.*\.json$/));if(files.length!==60)throw new Error('Expected 60 files, got '+files.length);console.log('Corpus OK: 60 files')"</automated>
  </verify>
  <done>Exactly 60 test_*.json files in eval/corpus/; all valid JSON; no duplicate transcript_ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary changes — this plan adds static JSON data files only.

## STRIDE Threat Register

No threats introduced by static corpus files.
</threat_model>

<verification>
1. `ls eval/corpus/test_*.json | wc -l` outputs 60
2. All 60 files parse as valid JSON
3. `grep -l 'adversarial_injections' eval/corpus/*.json | wc -l` returns at least 21 (fabrication_bait=10 + attribution_bait=5 + implicit_inference=5 + 1 existing)
4. `node -e "const fs=require('fs');fs.readdirSync('eval/corpus').filter(f=>f.match(/^test_.*\.json$/)).forEach(f=>{const d=JSON.parse(fs.readFileSync('eval/corpus/'+f,'utf8'));if(!d.transcript_id)throw new Error(f)}); console.log('ok')"` exits 0
5. No transcript_id value appears in more than one file
</verification>

<success_criteria>
- eval/corpus/ contains exactly 60 test_*.json files
- All 8 category quotas are met: standard_sync=10, action_item_dense=10, date_heavy=10, high_speaker_count=5, fabrication_bait=10, attribution_bait=5, implicit_inference_traps=5, short_no_content=5
- All files are valid JSON with transcript_id, category, transcript, and ground_truth fields
- Adversarial categories have non-empty adversarial_injections arrays
- All source_quote values in ground_truth are verbatim substrings of the corresponding transcript
</success_criteria>

<output>
Create `.planning/phases/11-packaging-eval-harness/11-04-SUMMARY.md` when done
</output>
