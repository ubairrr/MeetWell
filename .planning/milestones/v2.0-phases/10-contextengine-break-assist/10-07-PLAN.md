---
phase: 10-contextengine-break-assist
plan: 07
type: execute
wave: 5
depends_on:
  - 10-05
files_modified:
  - tests/session.test.ts
  - tests/unit/break-digest.test.ts
autonomous: true
requirements:
  - CTX-04
  - CTX-05
must_haves:
  truths:
    - session.test.ts includes a test verifying the Capturing → OnBreak → Capturing FSM cycle (CTX-05 verification)
    - break-digest.test.ts verifies that SummaryCardStore.getCardsSince() returns only cards with created_at > breakStartMs (CTX-04 verification)
    - break-digest.test.ts verifies that cards generated BEFORE breakStartMs are excluded from the digest window
    - Both tests use the existing db.test.ts SQLCipher temp-file pattern
    - Neither test re-implements any code that is already working — these are VERIFY-ONLY plans for confirmed-complete features
  artifacts:
    - tests/session.test.ts (modified — OnBreak cycle test appended)
    - tests/unit/break-digest.test.ts (new file — SummaryCardStore getCardsSince filter verification)
  key_links:
    - CTX-04 is confirmed implemented in index.ts (start-break/end-break handlers + SummaryCardStore.getCardsSince)
    - CTX-05 is confirmed implemented in SessionManager.ts (OnBreak state + Capturing↔OnBreak transitions)
    - D-09 and D-11: break_start_timestamp is held in-memory as breakStartMs; getCardsSince uses it to filter cards
---

<objective>
Add targeted verification tests for the two already-implemented Break Assist requirements: CTX-05 (FSM OnBreak state transitions) and CTX-04 (break digest card window filtering). These are VERIFY-ONLY plans — the implementation is confirmed complete in Phase 9/index.ts. The tests provide regression protection.

Purpose: CTX-04 and CTX-05 are complete but untested. Without tests, a future refactor could silently break the Capturing → OnBreak → Capturing cycle or the card timestamp filter, removing break assist functionality. These tests lock in the correct behavior.

Output: Two test additions that pass against the existing implementation with zero code changes to production files.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ubair/Gits/MeetingAssist/.planning/PROJECT.md
@/Users/ubair/Gits/MeetingAssist/.planning/ROADMAP.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-CONTEXT.md
@/Users/ubair/Gits/MeetingAssist/tests/session.test.ts
@/Users/ubair/Gits/MeetingAssist/tests/db.test.ts
@/Users/ubair/Gits/MeetingAssist/src/main/session/SessionManager.ts
@/Users/ubair/Gits/MeetingAssist/src/main/store/SummaryCardStore.ts
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend session.test.ts — add Capturing → OnBreak → Capturing FSM cycle test (CTX-05, D-09, D-10)</name>
  <files>tests/session.test.ts</files>
  <action>
Read tests/session.test.ts in full before editing.

Append the following tests to the EXISTING describe block in session.test.ts (do not create a new describe block if one already covers SessionManager; add inside it):

Test A — Capturing to OnBreak transition (per D-09):
  it('transitions from Capturing to OnBreak on start-break')
  - Create a SessionManager, advance through Idle → PreCapture → Capturing using existing valid transitions
  - Call session.transition('start-break')
  - Expect session.getState() === 'OnBreak'

Test B — OnBreak back to Capturing on end-break (per D-09):
  it('transitions from OnBreak back to Capturing on end-break')
  - Advance to OnBreak state as above
  - Call session.transition('end-break')
  - Expect session.getState() === 'Capturing'

Test C — OnBreak cycle does NOT assign a new meeting ID (per D-11):
  This test is at the index.ts integration layer and cannot be unit-tested against SessionManager alone. Skip this test in this file — it is covered by the break-digest.test.ts (Task 2) and by the CTX-04 acceptance criteria.

Test D — Invalid transition from OnBreak (regression guard):
  it('throws when transitioning from OnBreak with an invalid event')
  - Advance to OnBreak
  - Expect session.transition('end-meeting') to throw (or however SessionManager signals an invalid transition — check the existing test pattern for this)

Match the exact coding style of the existing tests in session.test.ts (import statements, describe/it structure, assertion style).
  </action>
  <verify>
    <automated>npx vitest run tests/session.test.ts --reporter=verbose</automated>
  </verify>
  <done>All pre-existing session.test.ts tests pass; three new OnBreak-related tests pass; no production code modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create break-digest.test.ts — verify SummaryCardStore.getCardsSince filters by breakStartMs (CTX-04, D-09, D-11)</name>
  <files>tests/unit/break-digest.test.ts</files>
  <action>
Read tests/db.test.ts to extract the SQLCipher test DB setup pattern (temp file, vi.mock('electron'), DDL init, afterEach cleanup). Follow that pattern exactly.

Create tests/unit/break-digest.test.ts.

Verify that the tests/unit/ directory exists (it already has CaptureService.test.ts and others per the file listing). Create the file in that location.

The test verifies SummaryCardStore.getCardsSince(meetingId, sinceMs) — the method confirmed to be the CTX-04 break assist card filter.

Test setup:
  - vi.mock('electron', ...) at the top (same as db.test.ts)
  - SQLCipher temp-file DB with all DDLs inlined (do not import from db.ts)
  - Import SummaryCardStore from '../../src/main/store/SummaryCardStore'

Seed helper: insert 3 summary_cards:
  - Card A: created_at = breakStartMs - 10000 (10 seconds BEFORE the break started — must be EXCLUDED)
  - Card B: created_at = breakStartMs + 5000 (5 seconds into break — must be INCLUDED)
  - Card C: created_at = breakStartMs + 30000 (30 seconds into break — must be INCLUDED)
  Use the summary_cards DDL columns: id, meeting_id, card_index, interval_start, interval_end, wall_time_label, topic_headline, key_points_json, speaker_contributions_json, model_id, created_at

Tests:
  it('returns only cards created after breakStartMs', () => {
    const store = new SummaryCardStore(db)
    const cards = store.getCardsSince(meetingId, breakStartMs)
    expect(cards).toHaveLength(2)
    const ids = cards.map(c => c.id)
    expect(ids).toContain('card-b')
    expect(ids).toContain('card-c')
    expect(ids).not.toContain('card-a')
  })

  it('returns empty array when no cards exist after breakStartMs', () => {
    const store = new SummaryCardStore(db)
    const cards = store.getCardsSince(meetingId, breakStartMs + 60000)
    expect(cards).toHaveLength(0)
  })

  it('returns all cards when breakStartMs is 0 (no break yet)', () => {
    const store = new SummaryCardStore(db)
    const cards = store.getCardsSince(meetingId, 0)
    expect(cards).toHaveLength(3)
  })

Note: the summary_cards column storing speaker contributions is speaker_contributions_json (this is the correct name for the summary_cards table — distinct from speaker_attributions used in epoch_summaries). Use that column name when seeding.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/break-digest.test.ts --reporter=verbose</automated>
  </verify>
  <done>All three getCardsSince tests pass; the break window filter correctly excludes pre-break cards and includes break-window cards; no production code modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB → SummaryCardStore | Temp-file SQLCipher DB, test key only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-A | Tampering | FSM invalid transitions | low | mitigate | Test D in session.test.ts asserts OnBreak rejects invalid events — prevents silent FSM state corruption |
| T-10-07-B | Information Disclosure | Temp test DB | low | mitigate | afterEach cleanup deletes temp file; test key is not the production safeStorage key |
</threat_model>

<verification>
1. `npx vitest run tests/session.test.ts --reporter=verbose` — all tests PASS (existing 7 + new 3)
2. `npx vitest run tests/unit/break-digest.test.ts --reporter=verbose` — all 3 tests PASS
3. `npx vitest run --reporter=verbose` — full test suite passes (no regressions)
4. `grep -c 'OnBreak' tests/session.test.ts` returns at least 2 (test names + transition calls)
5. `grep -c 'getCardsSince' tests/unit/break-digest.test.ts` returns at least 2
</verification>

<success_criteria>
- CTX-05 verified: Capturing → OnBreak → Capturing FSM cycle tests pass in session.test.ts
- CTX-04 verified: SummaryCardStore.getCardsSince() correctly filters cards by breakStartMs in break-digest.test.ts
- Full Vitest suite passes with no regressions
- Zero production code changed in this plan
</success_criteria>

<output>
Create `.planning/phases/10-contextengine-break-assist/10-07-SUMMARY.md` when done
</output>
