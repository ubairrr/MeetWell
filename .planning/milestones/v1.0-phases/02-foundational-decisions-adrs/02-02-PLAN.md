---
phase: "02-foundational-decisions-adrs"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
autonomous: true
requirements:
  - DEC-02

must_haves:
  truths:
    - "A Data-handling & Privacy ADR exists at 02-DEC-02-data-handling-privacy.md in the phase folder"
    - "The ADR fixes local-first storage with SQLCipher (better-sqlite3-multiple-ciphers) + Electron safeStorage (Keychain-backed) encryption at rest per D-06"
    - "The ADR documents raw audio deleted by default after transcription with opt-in keep toggle per D-06"
    - "The ADR documents transcript/artifact retention as indefinite until user-initiated delete per D-07"
    - "The ADR documents data persistence after uninstall and recommends a 'Delete All Meeting Data' settings action per D-08"
    - "The ADR notes on-device mode as a planned future capability without committing to implementation scope per D-09"
    - "The ADR contains an Open Dependencies section explicitly naming RSCH-03 (vendor DPA / no-training confirmation) as an unresolved dependency gating full closure"
  artifacts:
    - ".planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md"
  key_links:
    - "DEC-02 cross-references DEC-01 (./02-DEC-01-consent-recording-posture.md) via relative markdown link"
    - "DEC-02 Open Dependencies section explicitly names RSCH-03 with the verbatim note from PATTERNS.md"
    - "DEC-02 anchors encryption choices to CLAUDE.md Local Persistence table (better-sqlite3-multiple-ciphers + safeStorage + sqlite-vec)"
---

<objective>
Write the Data-handling & Privacy ADR (DEC-02) — the second of the two existential product decisions. This document fixes the data-handling posture permanently: local-first encrypted storage (SQLCipher + Electron safeStorage), raw-audio deleted by default after transcription (opt-in keep), transcripts kept until user-initiated delete, data persists after uninstall with a recommended clean-delete path, and on-device mode flagged as a future capability. Critically, this ADR notes its open dependency on RSCH-03 (vendor DPA / no-training confirmation from Deepgram and the chosen LLM providers) because that dependency was surfaced in the Phase 2 planning discussion.

Purpose: Lock the data-handling posture before storage architecture, persistence spec, or vendor contracts are finalised so downstream phases (Phase 3 RSCH-03, Phase 5 PRD) cannot drift from this boundary.

Output: `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md` — a committed MADR-format ADR encoding decisions D-06, D-07, D-08, and D-09 from CONTEXT.md, with the RSCH-03 open dependency explicitly documented.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-foundational-decisions-adrs/02-CONTEXT.md
@.planning/phases/02-foundational-decisions-adrs/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 02-DEC-02-data-handling-privacy.md — Data-handling & Privacy ADR</name>
  <files>.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md</files>

  <read_first>
    - `.planning/phases/02-foundational-decisions-adrs/02-CONTEXT.md` — read ALL decisions D-01 through D-09; this task implements D-06, D-07, D-08, D-09
    - `.planning/phases/02-foundational-decisions-adrs/02-PATTERNS.md` — read "Pattern Assignments" §02-DEC-02 for exact options tables, the RSCH-03 open-dependency verbatim note, and the encryption stack reference; read "Shared Document Conventions" for MADR section order and header block format
    - `.planning/REQUIREMENTS.md` §"Foundational Decisions — ADRs (DEC)" — DEC-02 definition (5 content requirements)
    - `.planning/ROADMAP.md` §"Phase 2: Foundational Decisions (ADRs)" — 4 success criteria; criteria 3 and 4 both govern this ADR
    - `.claude/CLAUDE.md` §"Local Persistence & Encryption" — the locked persistence stack (better-sqlite3-multiple-ciphers, safeStorage, electron-store, sqlite-vec) that DEC-02 ratifies
    - `.planning/phases/02-foundational-decisions-adrs/02-PATTERNS.md` §"02-DEC-02 Stack context" — the exact 4-row persistence table to anchor decisions in
  </read_first>

  <action>
    Create `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md` using the MADR format established in PATTERNS.md §"Shared Document Conventions". The file encodes decisions D-06, D-07, D-08, and D-09 from CONTEXT.md, and explicitly notes the RSCH-03 open dependency.

    **Header block** (per PATTERNS.md header convention, matching `01-DEV-BASELINE.md` lines 1–5 style):
    ```
    # DEC-02: Data-handling & Privacy

    **Status:** Accepted (pending RSCH-03 vendor confirmation — see Open Dependencies)
    **Decided:** 2026-06-25
    **Deciders:** Product (ubair)
    **Supersedes:** —
    **Superseded by:** —
    ```

    **MADR section order** (7 sections, per D-01 and PATTERNS.md §"MADR section order"):
    1. Status
    2. Context / Problem Statement
    3. Decision Drivers
    4. Options Considered
    5. Decision Outcome
    6. Consequences
    7. Open Dependencies

    **Section 1 — Status:**
    `Accepted — 2026-06-25 (pending RSCH-03 vendor confirmation — see Open Dependencies)`
    Include a brief one-line note that this ADR satisfies requirement DEC-02 (link to `../../REQUIREMENTS.md`).

    **Section 2 — Context / Problem Statement:**
    1–3 sentences: MeetingAssist captures, transcribes, and stores audio and generated artifacts (minutes, summaries, action items) that contain sensitive meeting content. Before any persistence architecture is specified, the product must fix its data-handling posture — how data is stored, encrypted, retained, and deleted — because these choices gate the storage spec, vendor contract negotiations, and the product's privacy claims.

    **Section 3 — Decision Drivers:**
    List these drivers (from REQUIREMENTS.md DEC-02 definition and the ROADMAP phase goal):
    - User privacy: meeting content is sensitive; it must not be readable without the user's explicit access
    - Legal / regulatory defensibility: encryption at rest and clear retention policies are baseline privacy hygiene
    - Core product value: "trustworthy record" — data must persist as long as the user needs it, not be purged automatically
    - Storage efficiency: raw audio (large) need not be kept once transcription is complete; artifacts (small) should persist indefinitely
    - Data minimisation: retain only what is necessary; raw audio is an intermediate, not a product artifact
    - Future privacy-tier path: on-device / fully offline mode must be architecturally possible without redesigning persistence

    **Section 4 — Options Considered:**
    Present three separate options tables (one per sub-decision domain) following the PATTERNS.md pattern for DEC-02.

    **Raw audio retention** (| Option | Behaviour | Verdict |):
    - Delete after transcription (default): Raw audio purged once transcript is written; user can opt-in via settings toggle to retain. → **Chosen** (D-06)
    - Keep raw audio always: Raw audio retained; higher storage cost; larger privacy surface. → Rejected as default — available as opt-in only
    - Configurable retention window (N days): Auto-expiry after N days. → Not adopted — adds complexity without clear user need

    **Transcript & artifact retention** (| Option | Behaviour | Verdict |):
    - Keep until user deletes: Indefinite retention; user deletes per-meeting or all data via explicit in-app action. → **Chosen** (D-07)
    - Auto-expiry (N days): Automatic purge after configured period. → Rejected — directly contradicts the "trustworthy record" core value

    **Encryption at rest** (| Option | Behaviour | Verdict |):
    - SQLCipher (better-sqlite3-multiple-ciphers) + safeStorage: Full-database AES-256 encryption; key generated on first run and stored in macOS Keychain via Electron safeStorage. → **Chosen** (ratified by CLAUDE.md §"Local Persistence & Encryption")
    - Plain SQLite + field-level app-layer encryption: Application-layer encryption only; more complex key management. → Rejected — full-DB encryption is cleaner and provides the same protection without additional complexity
    - No encryption: Plaintext on disk. → Rejected — privacy non-starter; meeting content is sensitive

    **Section 5 — Decision Outcome:**
    Open with the one-sentence locked verdict (per PATTERNS.md §"Posture statement"):
    "MeetingAssist stores all meeting data locally, encrypted at rest, retained indefinitely until the user deletes it, with raw audio deleted by default after transcription."

    Then document each sub-decision explicitly, citing the decision ID:

    - Per D-06 (Raw audio retention): Raw audio is deleted by default once the transcript is written. A settings toggle lets users opt-in to keeping the raw audio file. "Delete after transcription" is the default stance; "keep audio" is an explicit opt-in, not the default. This is the transcribe-then-delete-raw-audio stance required by REQUIREMENTS.md DEC-02.

    - Per D-07 (Transcript and artifact retention): Transcripts, minutes of meeting (MOM), summaries, and action items are kept indefinitely until the user explicitly deletes them. There is no automatic expiry. The user deletes individual meetings or all data via an explicit in-app action. This directly supports the core product value: "a user walks out of any meeting with an accurate, trustworthy record."

    - Per D-08 (Data after uninstall): Meeting data persists on disk after the app is uninstalled unless the user explicitly deletes it first. The ADR recommends the app surface a prominent "Delete All Meeting Data" action in its settings, giving users a clean-delete path before uninstalling. No automatic cleanup mechanism is introduced at uninstall time.

    - Per D-09 (On-device / offline mode): On-device / offline mode (whisper.cpp + local LLM for fully private, no-cloud recording) is a planned future capability. Its scope (full offline vs. partial) and implementation details are deferred to a later phase. DEC-02 notes its existence as a future privacy-tier option without committing to implementation specifics now.

    **Persistence stack anchor** (reference CLAUDE.md §"Local Persistence & Encryption"): Document the locked technical stack as a table that DEC-02 ratifies:

    | Data | Store | Encryption |
    |------|-------|------------|
    | Transcripts, MOM, summaries, action items, meeting metadata | better-sqlite3-multiple-ciphers (SQLCipher, AES-256, full-DB) | Key generated on first run; stored via Electron safeStorage (macOS Keychain-backed) |
    | Small prefs / settings (API keys go via safeStorage) | electron-store | safeStorage for secrets |
    | Vector embeddings (cross-meeting search) | sqlite-vec table inside the same SQLCipher DB | Inherits DB encryption |

    Note that this table is already established in CLAUDE.md; DEC-02 ratifies it as the authoritative data-handling decision.

    **Section 6 — Consequences:**
    Positive:
    - Privacy-defensible: meeting content is never in plaintext on disk; the macOS Keychain backs the encryption key.
    - Data minimisation: raw audio (the largest, most sensitive intermediate) is purged by default.
    - User control: indefinite retention + explicit delete gives users full ownership of their meeting record.
    - Architectural path to on-device mode: SQLCipher + local storage means the persistence layer does not need to change when an on-device tier is introduced.

    Negative / Tradeoffs:
    - safeStorage is macOS-specific: cross-platform key management is future work if Windows/Linux support is added.
    - Data persists after uninstall: users who do not use the "Delete All Meeting Data" action leave data on disk. Mitigated by surfacing a prominent delete action.
    - On-device mode is deferred: users who want fully private recording cannot get it in the initial release.

    **Section 7 — Open Dependencies:**
    Use the verbatim note from PATTERNS.md §02-DEC-02 "Open dependency to note":

    OPEN: RSCH-03 — Vendor DPA / no-training terms for Deepgram and the chosen LLM provider(s) must be confirmed before this ADR is fully closed. Until RSCH-03 completes, the data-handling ADR operates on the reasonable assumption that enterprise/API tiers exclude training on customer data; Phase 3 RSCH-03 will verify and update this ADR if needed.

    Also cross-reference DEC-01:
    - The consent posture governing who is informed that recording is happening is governed by [DEC-01](./02-DEC-01-consent-recording-posture.md). DEC-02 governs what happens to the data once captured.

    **Cross-reference style:** Use relative markdown links (e.g., `[DEC-01](./02-DEC-01-consent-recording-posture.md)`, `[REQUIREMENTS.md DEC-02](../../REQUIREMENTS.md)`, `[RSCH-03](../../REQUIREMENTS.md)`) per PATTERNS.md §"Cross-reference linking convention".
  </action>

  <verify>
    <automated>
      grep -c "DEC-02" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "RSCH-03" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "better-sqlite3-multiple-ciphers" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "safeStorage" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "transcribe-then-delete" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "Open Dependencies" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
      grep -c "02-DEC-01" /Users/ubair/Gits/MeetingAssist/.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
    </automated>
  </verify>

  <acceptance_criteria>
    1. File exists at `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md`
    2. File contains a header block with `# DEC-02: Data-handling & Privacy`, `**Status:** Accepted`, `**Decided:** 2026-06-25`; Status line notes "pending RSCH-03 vendor confirmation"
    3. File contains all 7 MADR sections in order: `## Status`, `## Context`, `## Decision Drivers`, `## Options Considered`, `## Decision Outcome`, `## Consequences`, `## Open Dependencies`
    4. `## Options Considered` contains three separate option tables covering: raw audio retention, transcript/artifact retention, and encryption at rest
    5. `## Decision Outcome` contains the one-sentence locked verdict and explicit D-06, D-07, D-08, D-09 sub-decisions
    6. The persistence stack table (better-sqlite3-multiple-ciphers / electron-store / sqlite-vec) appears in the Decision Outcome section
    7. `## Open Dependencies` contains explicit RSCH-03 reference with the statement that vendor DPA / no-training terms must be confirmed before this ADR is fully closed
    8. `## Open Dependencies` cross-references DEC-01 via relative link `./02-DEC-01-consent-recording-posture.md`
    9. D-09 (on-device mode) is documented as "planned future capability" — not committed to scope or implementation
    10. The phrase "transcribe-then-delete-raw-audio" appears to satisfy the REQUIREMENTS.md DEC-02 definition language
    11. ROADMAP Phase 2 success criteria 3 (data-handling ADR with local-first, encryption, retention, raw-audio stance, on-device mode) and 4 (both ADRs note open dependencies) are satisfied
  </acceptance_criteria>

  <done>
    The file `02-DEC-02-data-handling-privacy.md` exists with complete 7-section MADR format encoding all four locked decisions (D-06 through D-09). The locked verdict opens the Decision Outcome section. Three options tables cover raw audio retention, transcript retention, and encryption. The RSCH-03 open dependency is explicitly named in the Open Dependencies section with the verbatim statement from PATTERNS.md. DEC-01 is cross-referenced. ROADMAP Phase 2 success criteria 3 and 4 are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document → downstream phases | ADR content is read as locked decisions by Phase 3 (RSCH-03 research) and Phase 5 (PRD storage spec); incomplete or ambiguous ADR propagates incorrect assumptions |
| vendor assumption → ADR | ADR operates on the assumption that Deepgram/LLM API tiers exclude training on customer data; this assumption is unverified until RSCH-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | ADR document content | medium | mitigate | ADR is committed to git and auto-pushed; any change is traceable via git history |
| T-02-05 | Information Disclosure | Vendor training assumption | high | mitigate | RSCH-03 open dependency is explicitly stated in the ADR; the ADR Status header flags "pending RSCH-03 vendor confirmation" so downstream readers cannot miss the caveat |
| T-02-06 | Repudiation | On-device mode scope | low | accept | D-09 explicitly states "planned future capability" with no scope commitment; this is the intended state and is recorded in CONTEXT.md |
| T-02-07 | Information Disclosure | Unencrypted residual data after uninstall | medium | mitigate | D-08 documents the persistence-after-uninstall behaviour and explicitly recommends surfacing a "Delete All Meeting Data" action in settings; PRD (Phase 5) will specify this surface |
</threat_model>

<verification>
Phase 2 Plan 02 verification — Data-handling & Privacy ADR:

1. File exists at the correct path: `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md`
2. All 4 ROADMAP Phase 2 success criteria checked against the ADR content:
   - SC-1: (DEC-01 — covered in Plan 01)
   - SC-2: (DEC-01 — covered in Plan 01)
   - SC-3: ADR fixes local-first storage, encryption at rest (SQLCipher + safeStorage), retention defaults + per-meeting delete, transcribe-then-delete-raw-audio stance, optional on-device mode noted as future capability
   - SC-4: ADR notes RSCH-03 open dependency explicitly in Open Dependencies section
3. REQUIREMENTS.md DEC-02 definition satisfied: all 5 content bullets (local-first storage, encryption, retention, transcribe-then-delete, on-device mode) present in the ADR
4. D-06, D-07, D-08, D-09 from CONTEXT.md each cited by decision ID in the Decision Outcome section
5. RSCH-03 dependency named with the verbatim note from PATTERNS.md §02-DEC-02
</verification>

<success_criteria>
- `02-DEC-02-data-handling-privacy.md` exists in the phase folder with complete 7-section MADR format
- The locked posture verdict (one-sentence declarative) opens the Decision Outcome section
- Raw audio deletion default (D-06), indefinite transcript retention (D-07), persistence after uninstall (D-08), and on-device mode as future capability (D-09) are all explicitly documented
- Three options tables are present covering raw audio, transcript retention, and encryption
- RSCH-03 open dependency is explicitly named in Open Dependencies with the verbatim statement from PATTERNS.md
- DEC-01 is cross-referenced via relative link
- ROADMAP Phase 2 success criteria 3 and 4 are fully satisfied
</success_criteria>

<output>
## Artifacts This Phase Produces

This plan produces one artifact:

| Artifact | Path | Purpose |
|----------|------|---------|
| DEC-02 Data-handling & Privacy ADR | `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md` | Locks data-handling posture; consumed by Phase 3 RSCH-03 and Phase 5 PRD; RSCH-03 open dependency flagged |

Create `.planning/phases/02-foundational-decisions-adrs/02-02-SUMMARY.md` when done.
</output>
