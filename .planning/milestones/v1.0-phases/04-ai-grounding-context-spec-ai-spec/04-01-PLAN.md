---
phase: 04-ai-grounding-context-spec-ai-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
autonomous: true
requirements:
  - GRND-01
  - GRND-02

must_haves:
  truths:
    - "04-AI-SPEC.md exists at .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md"
    - "Every D-01 through D-14 decision from 04-CONTEXT.md is reflected in the spec"
    - "The faithfulness contract defines the two-stage extraction protocol (evidence-first, then content-from-quotes)"
    - "The Zod schema for CitationAnchorSchema, ActionItemSchema, DecisionSchema, ExtractedDateSchema, MeetingArtifactsSchema, and SummaryCardSchema is included verbatim in the spec"
    - "The ContextEngine component map (RollingWindow, TokenMonitor, SummaryCardTimer, EpochCompressor, ContextComposer) is documented with data-flow diagrams"
    - "The live summary board (D-05 through D-09) is explicitly decoupled from the epoch system in the spec"
    - "The proposed-with-confirm UX contract (D-04) prohibits auto-write to any external system"
  artifacts:
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
  key_links:
    - "04-AI-SPEC.md §Faithfulness Contract → feeds ArtifactPipeline spec in Phase 5 PRD"
    - "04-AI-SPEC.md §ContextEngine Architecture → feeds SessionManager FSM and ContextEngine spec in Phase 5 PRD"
    - "CitationAnchorSchema in §Faithfulness Contract is the same schema §Adversarial Eval Harness verifies (Plan 02)"
---

<objective>
Write the first two sections of the Phase 4 AI-SPEC document: the Faithfulness/Grounding Contract (GRND-01) and the ContextEngine + Two-Speed Processing Architecture (GRND-02). This plan authors the production-grade design contract from the fully-researched content in 04-RESEARCH.md.

Purpose: The AI-SPEC is the trust foundation for all artifact extraction in MeetingAssist. It must exist before the PRD (Phase 5) can specify the ArtifactPipeline or ContextEngine components.

Output: .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md containing sections 1 (preamble + GRND-01 Faithfulness Contract) and 2 (GRND-02 ContextEngine Architecture). Section 3 (GRND-03 Adversarial Eval Harness) is completed in Plan 02.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-grounding-context-spec-ai-spec/04-CONTEXT.md
@.planning/phases/04-ai-grounding-context-spec-ai-spec/04-RESEARCH.md
</context>

## Artifacts This Phase Produces

| Artifact | Path | Produced By |
|----------|------|-------------|
| AI-SPEC design contract | `.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md` | Plan 01 (creates) + Plan 02 (completes) |

<tasks>

<task type="auto">
  <name>Task 1: Write 04-AI-SPEC.md — preamble, purpose, and GRND-01 Faithfulness Contract (D-01 through D-04)</name>
  <files>.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md</files>

  <read_first>
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-CONTEXT.md — locked decisions D-01 through D-14 (non-negotiable constraints on the spec)
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-RESEARCH.md — §Faithfulness Contract Design, §Citation Anchor Schema, §Zod Extraction Schema Design, §Conservative Date Handling, §Proposed-with-Confirm UX Contract — full authored content for this section
    - .planning/ROADMAP.md — Phase 4 goal and success criteria (the spec must satisfy all three criteria)
    - .planning/REQUIREMENTS.md — GRND-01 acceptance criteria
  </read_first>

  <action>
    Create 04-AI-SPEC.md using the Write tool. The document is structured as a formal design contract, not a research note. Begin with a YAML frontmatter block:

    phase: 04
    slug: ai-grounding-context-spec-ai-spec
    version: 1.0
    status: draft
    produced_by: Phase 4 planning
    consumed_by: Phase 5 PRD Finalization
    requirements_covered: GRND-01, GRND-02, GRND-03
    created: 2026-06-26

    Follow with a document header "# MeetingAssist AI Grounding & Context Spec (AI-SPEC)" and an executive summary paragraph explaining what this document is, what problem it solves (fabricated artifacts = #1 trust-killer), and that it is the authority for ArtifactPipeline and ContextEngine in Phase 5.

    Include a "How to use this document" table mapping each section to its downstream consumer:
    - Section 1 (Faithfulness Contract) → Phase 5 ArtifactPipeline spec
    - Section 2 (ContextEngine Architecture) → Phase 5 ContextEngine + SessionManager FSM spec
    - Section 3 (Adversarial Eval Harness) → Build milestone testing strategy

    Then write Section 1 as "## Section 1: Faithfulness/Grounding Contract (GRND-01)" with the following subsections, drawing content directly from 04-RESEARCH.md §Faithfulness Contract Design:

    **1.1 What "Faithfulness" Means for MeetingAssist**
    Define extrinsic hallucination and intrinsic hallucination as the two failure categories. State that both damage user trust and are the primary reason users reject AI-generated meeting artifacts. Cite D-01 through D-04 as the governing decisions.

    **1.2 Quote-Backed Extraction Protocol (per D-01)**
    Document the two-stage extraction protocol exactly as specified in 04-RESEARCH.md: Stage 1 (evidence extraction anchor-first — quote, speaker, timestamp_start, timestamp_end) and Stage 2 (constrained generation from anchored quotes only, no parametric memory). State explicitly that the generation prompt receives ONLY the extracted quotes, not the full transcript. Note the open question from RESEARCH.md: two separate LLM calls vs. chain-of-thought single call — recommend two separate calls in v1 for clean separation.

    **1.3 Citation Anchor Schema (per D-01, D-02)**
    Include the complete CitationAnchorSchema Zod definition from RESEARCH.md verbatim (as a TypeScript code block). Document both confidence levels: 'direct' (verbatim quote exists, normal visual weight + Verify toggle) and 'inferred' (no direct quote, reduced visual weight + "Inferred — no direct quote" badge, never suppress per D-03).

    **1.4 Conservative Date Handling**
    Document the four conservative date handling rules from RESEARCH.md: (1) ISO 8601 only in structured output, (2) unresolvable relative dates resolve to null with raw_deadline_text preserved, (3) inferred deadlines tagged confidence 'inferred', (4) meeting_date injection in every extraction prompt. Include Pitfall 5 (implicit date resolution bug — "next Friday" resolved relative to training cutoff vs. meeting date) as a warning box.

    **1.5 Proposed-with-Confirm UX Contract (per D-04)**
    Document the full UX contract: nothing auto-writes to calendar or external system; every artifact in status: 'proposed' until explicit user confirm; UI renders proposals in distinct visual state; user actions are Confirm, Edit-then-Confirm, or Dismiss; calendar export only for status: 'confirmed' items; the extraction pipeline NEVER triggers any external write without a user confirmation event.

    **1.6 Canonical Extraction Schemas**
    Include the full Zod schema block from RESEARCH.md containing ActionItemSchema, DecisionSchema, ExtractedDateSchema, KeyPointSchema, MeetingArtifactsSchema — verbatim, as a TypeScript code block. Add the 5-Min Summary Card schema (SummaryCardSchema) in a separate code block. After each schema, add a bullet list of key contract rules enforced by the schema (e.g., citations.min(1), status: z.literal('proposed'), due_date nullable, etc.).

    **1.7 Common Pitfalls (Faithfulness)**
    Include Pitfall 1 (generating content before extracting quotes) and Pitfall 5 (implicit date resolution bug) from RESEARCH.md §Common Pitfalls, in a warning-formatted section. Each pitfall: what goes wrong, why it happens, how to avoid it, warning signs.

    **1.8 Provider-Agnostic Schema Delivery**
    Explain the one-schema-two-providers pattern: Zod as single source of truth, zodToJsonSchema for OpenAI (strict: true JSON schema) and Gemini (responseJsonSchema). Include Pitfall 6 (provider-specific schema drift) as a warning. This implements the locked CLAUDE.md requirement for provider-agnostic Structured Outputs.

    End Section 1 with a "### Decision Coverage (GRND-01)" table mapping D-01 through D-04 to the subsection that implements each decision.
  </action>

  <acceptance_criteria>
    - File exists at .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
    - YAML frontmatter contains: phase: 04, requirements_covered: GRND-01, GRND-02, GRND-03, status: draft
    - Section 1 heading "## Section 1: Faithfulness/Grounding Contract (GRND-01)" is present
    - Subsections 1.1 through 1.8 are all present (grep for "### 1." returns at least 8 matches)
    - CitationAnchorSchema Zod code block is present and includes fields: quote_preview, quote_full, speaker_label, timestamp_start, timestamp_end, confidence
    - MeetingArtifactsSchema Zod code block is present and includes: action_items, decisions, dates, key_points, minutes_of_meeting
    - SummaryCardSchema Zod code block is present and includes: card_index, interval_start_seconds, interval_end_seconds, wall_time_label, topic_headline, key_points, action_items_mentioned, speaker_contributions
    - "status: z.literal('proposed')" appears in ActionItemSchema and DecisionSchema code blocks
    - "citations: z.array(CitationAnchorSchema).min(1)" appears in at least ActionItemSchema
    - Decision coverage table for D-01 through D-04 is present at the end of Section 1
    - D-03 ("Inferred — no direct quote" badge, never suppress) is documented in §1.3
    - D-04 ("nothing auto-writes to calendar") is documented in §1.5
    - Pitfall 1 (content-before-quotes) and Pitfall 5 (date resolution bug) are present
    - The file is well-formed Markdown with no broken code fences
  </acceptance_criteria>

  <verify>
    <automated>grep -c "## Section 1" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "CitationAnchorSchema" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "z.literal('proposed')" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "MeetingArtifactsSchema" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md</automated>
  </verify>

  <done>04-AI-SPEC.md exists. Section 1 is complete: preamble, faithfulness definition, two-stage extraction protocol, all Zod schemas, date handling rules, proposed-with-confirm UX contract, pitfalls, provider-agnostic schema delivery, and D-01 through D-04 decision coverage table.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 2 of 04-AI-SPEC.md — ContextEngine + Two-Speed Processing Architecture (GRND-02, D-05 through D-14)</name>
  <files>.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md</files>

  <read_first>
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — current state after Task 1 (append Section 2 to the existing file)
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-CONTEXT.md — locked decisions D-05 through D-14 (non-negotiable architecture constraints)
    - .planning/phases/04-ai-grounding-context-spec-ai-spec/04-RESEARCH.md — §ContextEngine Architecture, §Two-Speed Processing Architecture, §Token Budget Analysis, §Epoch Compression Protocol, §Embedding Model Selection, §Common Pitfalls (Pitfalls 2-4), §Architectural Responsibility Map
    - .planning/phases/03-deep-research/03-RSCH-05-DATA-MODEL.md — sqlite-vec schema (vec_chunks) that epoch RAG must be compatible with
  </read_first>

  <action>
    Append Section 2 to 04-AI-SPEC.md using the Edit tool (replace the end of the file, preserving all Section 1 content). Add "## Section 2: ContextEngine + Two-Speed Processing Architecture (GRND-02)" with the following subsections:

    **2.1 Architecture Responsibility Map**
    Include the Architectural Responsibility Map table from RESEARCH.md verbatim — mapping each capability (Faithfulness citation model, TranscriptStore, Rolling window/token monitor, 5-min summary card generation, Epoch compression, Live assistant context composition, Break assist digest generation, End-of-meeting batch extraction, Artifact proposal UX, Evaluation harness) to its primary tier and secondary tier.

    **2.2 Live Summary Board (per D-05 through D-09)**
    Document the display-layer spec for the live summary board. State explicitly (per D-09) that the summary board is architecturally separate from the ContextEngine epoch system — summary cards are time-triggered display artifacts; context epochs are token-threshold-triggered context management. They must not be conflated. Specify: (D-05) stacked card feed labeled with time range "10:00–10:05" format; (D-06) each card covers only its interval's transcript, not cumulative; (D-07) 5-minute default interval, configurability is a v2 settings knob; (D-08) summary cards persisted in SQLCipher DB and feed Break Assist and Live Assistant context directly.

    Include Pitfall 4 (summary card leaking into epoch system — epoch compressor must read from transcript_segments, not summary_cards) as a warning box.

    **2.3 ContextEngine Component Overview**
    Reproduce the ASCII component diagram from RESEARCH.md showing TranscriptStore → ContextEngine with its sub-components: RollingWindow, TokenMonitor, SummaryCardTimer, EpochCompressor (with EpochEmbedder), and ContextComposer (with Live Assistant and Break Assist variants). State that the ContextEngine lives in the Electron main process (not the renderer).

    **2.4 Rolling Window Token Budget (per D-10)**
    Include the token budget table from RESEARCH.md (30 min to 40+ hours with estimated tokens and overflow status). State the ceiling: 800,000 tokens (leaving 200K headroom in Gemini 2.5 Flash's 1M context window for system prompt + instructions + output buffer). State the epoch compression trigger: 70% of ceiling = 560,000 tokens (fires before full, not at 100%). Specify tiktoken with cl100k_base encoding (not character-based estimation) as the token counting implementation — explain why (off-by-one drift over 2-hour meetings). Include Pitfall 2 (token counter drift) as a warning box.

    **2.5 Epoch Compression Protocol (per D-11)**
    Document what triggers epoch compression (rolling window exceeds 70% trigger = 560K tokens), what gets compressed (oldest N transcript segments — enough to bring window back below 50% capacity), the structured EpochSummarySchema Zod definition (include verbatim from RESEARCH.md: epoch_id, meeting_id, covered_interval_start, covered_interval_end, decisions, action_items, key_points, speaker_attributions, raw_segment_count, token_count_compressed, created_at). Explain why structured (not narrative): structured fields give embedding better discriminative signal for retrieval. State the epoch embedding approach: embed each epoch summary as a single vector into vec_chunks in the SQLCipher DB (compatible with RSCH-05 sqlite-vec schema). Document embedding model selection: default text-embedding-3-small (1536 dims, $0.02/1M tokens); privacy mode uses local Ollama model via same baseURL adapter pattern. Include Pitfall 3 (epoch compression destroying recent context — compressor MUST evict oldest segments first).

    **2.6 Speed 1: Real-Time Passive Path**
    Reproduce the Speed 1 data-flow diagram from RESEARCH.md (Deepgram WebSocket → TranscriptStore → RollingWindow → TokenMonitor → SummaryCardTimer → CardLLMCaller → SQLCipher + renderer IPC). Note key properties: no user-visible LLM latency in passive path; cost per card (~$0.0001–$0.0003 at Gemini 2.5 Flash pricing).

    **2.7 Speed 2: On-Demand Path — Live Assistant (per D-12)**
    Reproduce the Live Assistant data-flow diagram. Document the context composition order (priority stack): system prompt → all summary cards → rolling window content → epoch summaries (top-N via sqlite-vec RAG if overflow) → chat history → user question. State that chat history accumulates and is included in subsequent assistant calls within the same session.

    **2.8 Speed 2B: End-of-Meeting Batch — ArtifactPipeline (per D-14)**
    Reproduce the map-reduce batch diagram. Document the map phase (parallel chunk extractions — one per 5-min interval) and reduce phase (single LLM call deduplicating and finalizing MeetingArtifactsSchema output). Specify chunk boundary strategy: use existing 5-min interval boundaries as natural map chunks. Document the CitationValidator step (cross-checks quote_full against transcript_segments DB before displaying). Note the open question from RESEARCH.md: mid-interval meeting end (e.g., 43 min) — process final partial interval as a full map chunk; reduce step handles deduplication. Specify that all output items carry citations and status: 'proposed' per GRND-01.

    **2.9 Break Assist Digest (per D-13)**
    Reproduce the Break Assist data-flow: user triggers "I'm back" → ContextComposer assembles transcript segments from break_start_timestamp to now + summary_cards during break window → LLM call generates "While you were away" digest → renderer IPC renders digest above interval cards. State manual-only activation (no auto-detection — v2 enhancement per deferred ideas).

    **2.10 Common Pitfalls (Architecture)**
    Include Pitfalls 2 (token counter drift), 3 (epoch compression destroying recent context), and 4 (summary card leaking into epoch system) from RESEARCH.md §Common Pitfalls in a consolidated warning section.

    **2.11 Decision Coverage (GRND-02)**
    Add a decision coverage table mapping D-05 through D-14 to the subsection that implements each decision.

    After appending Section 2, verify the file ends cleanly (no truncated content) and that Section 1 is still intact by checking the file length.
  </action>

  <acceptance_criteria>
    - "## Section 2: ContextEngine" heading is present in 04-AI-SPEC.md
    - Subsections 2.1 through 2.11 are all present (grep for "### 2." returns at least 11 matches)
    - The ASCII ContextEngine component diagram is present (contains "RollingWindow" and "SummaryCardTimer" and "EpochCompressor")
    - EpochSummarySchema Zod code block is present (contains epoch_id, covered_interval_start, decisions, speaker_attributions, token_count_compressed)
    - "architecturally separate" phrase is present in §2.2 (the D-09 decoupling statement)
    - "560,000 tokens" (the 70% epoch trigger) is present in §2.4 or §2.5
    - "800,000 tokens" (the rolling window ceiling) is present
    - "tiktoken" is mentioned as the token counting implementation
    - The map-reduce diagram keywords ("MAP phase" and "REDUCE phase") are present in §2.8
    - "CitationValidator" step is documented in §2.8
    - Decision coverage table for D-05 through D-14 is present at end of Section 2
    - Section 1 content (CitationAnchorSchema) still present — file was appended, not overwritten
    - grep -c "## Section" returns 2 (Section 1 and Section 2)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "## Section" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "EpochSummarySchema" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "architecturally separate" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md && grep -c "tiktoken" /Users/ubair/Gits/MeetingAssist/.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md</automated>
  </verify>

  <done>04-AI-SPEC.md contains both Section 1 (Faithfulness Contract, GRND-01) and Section 2 (ContextEngine Architecture, GRND-02). All D-01 through D-14 decisions from 04-CONTEXT.md are cited in the spec. Decision coverage tables present at end of each section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec author → downstream phases | The AI-SPEC is consumed verbatim by Phase 5 PRD and the build milestone; errors in the spec propagate unchecked |
| Source material → authored document | RESEARCH.md contains [ASSUMED] claims — the spec must not present them as confirmed facts without labeling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | 04-AI-SPEC.md content | medium | mitigate | Spec content is verified against all 14 locked decisions (D-01 through D-14) from 04-CONTEXT.md before plan completion; decision coverage tables are the verification mechanism |
| T-04-02 | Information Disclosure | [ASSUMED] claims in spec | low | accept | Research document already marks [ASSUMED] items; spec inherits these markers; Phase 5 readers understand the confidence levels |
| T-04-03 | Repudiation | Missing citation of locked decisions | medium | mitigate | Decision coverage table at end of each section explicitly maps every locked decision ID to the implementing subsection; Plan 02 Task 2 verifies all D-01 through D-14 are covered |
| T-04-SC | Tampering | npm/pip installs | low | accept | This is a planning-only phase; no package installs occur; no slopcheck needed |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "## Section" .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — expect 2
2. grep -c "CitationAnchorSchema" .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — expect >= 2
3. grep -c "EpochSummarySchema" .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — expect >= 1
4. grep -c "D-0[1-9]\|D-1[0-4]" .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — expect >= 14
5. grep -c "architecturally separate" .planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md — expect >= 1
</verification>

<success_criteria>
- 04-AI-SPEC.md exists with YAML frontmatter listing requirements_covered: GRND-01, GRND-02, GRND-03
- Section 1 (Faithfulness Contract) covers GRND-01: two-stage extraction protocol, CitationAnchorSchema and full Zod extraction schemas, proposed-with-confirm UX contract, conservative date handling, D-01 through D-04 decision coverage table
- Section 2 (ContextEngine Architecture) covers GRND-02: component map, rolling window token budget (800K ceiling, 70% trigger), epoch compression protocol, Speed 1 passive path, Speed 2 live assistant, Speed 2B end-of-meeting batch with map-reduce, Break Assist digest, D-05 through D-14 decision coverage table
- All D-01 through D-09 locked decisions cited in relevant sections
- Deferred ideas (configurable interval, named speaker attribution, auto-break detection) not present as v1 requirements
</success_criteria>

<output>
Create .planning/phases/04-ai-grounding-context-spec-ai-spec/04-01-SUMMARY.md when done.
</output>
