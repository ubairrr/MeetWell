---
phase: 01-dna-deep-dive-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md
  - .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03]
must_haves:
  truths:
    - "A written record confirms the SETUP baseline (private repo + auto-push + .gitignore rules) as operating conventions"
    - "A project-conventions / dev-baseline document fixes the toolchain, Node/Electron line, and proposed repo layout as DIRECTION (not final-pinned)"
  artifacts:
    - .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md
    - .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md
  key_links:
    - "SETUP-BASELINE facts match the actual /Users/ubair/Gits/MeetingAssist/.gitignore and git remote -v output"
    - "DEV-BASELINE marks exact version pinning + final main/<domain>/ layout as Phase 5 PRD / build-time decisions (D-07)"
---

<objective>
Produce the two SETUP/conventions deliverables for Phase 1: the setup-baseline record (SETUP-01, SETUP-02) and the dev-baseline / project-conventions doc (SETUP-03). Both are "record verified facts as conventions" docs — they document state that already exists; they do NOT re-run git setup or final-pin the stack.

Purpose: Make ROADMAP Phase 1 success criteria #1 and #2 TRUE — a written record of the SETUP baseline as operating conventions, and a dev-baseline doc that fixes toolchain/Node-Electron-line/repo-layout as DIRECTION (per D-07, not final pins).
Output: Two Markdown docs inside the phase folder.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md
@.planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md
@.planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md
</context>

## Artifacts this phase produces

This phase creates the following NEW files (exclude from source-grounding drift verification):
- `.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md`
- `.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md`
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md` (plan 02)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md` (plan 03)

<tasks>

<task type="auto">
  <name>Task 1: Write the setup-baseline record (SETUP-01, SETUP-02)</name>
  <files>.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md (§"SETUP Baseline Facts" lines 204-215 — the VERIFIED remote + .gitignore facts; §"Assumptions Log" A1 — the auto-push hook caveat)
    - .planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md (§"01-SETUP-BASELINE.md" Pattern Assignment + §"Shared Document Conventions" — header block, house style)
    - .planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md (D-01 separate docs, D-02 docs live in phase folder)
    - /Users/ubair/Gits/MeetingAssist/.gitignore (the actual file the doc records — verify every recorded rule against it)
    - .planning/PROJECT.md (the analog: project fact/decision register voice)
  </read_first>
  <action>
    Create `.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md` recording the verified SETUP baseline as operating conventions. This satisfies SETUP-01 and SETUP-02. Record-as-is — do NOT re-run git or hook setup (RESEARCH §"Don't Hand-Roll": repo/auto-push were wired at init).

    Follow the shared house style from 01-PATTERNS.md §"Shared Document Conventions": a header block (Title / Domain one-liner / a date line / Confidence: HIGH with the "verified from local repo state" qualifier). Analog to follow: `.planning/PROJECT.md` register voice.

    SETUP-01 content (per RESEARCH §"SETUP Baseline Facts"): record that `git remote -v` resolves origin to `https://github.com/ubairrr/MeetingAssist.git` (fetch + push) — VERIFIED. Record that auto-push is a Claude Code Stop hook that commits + pushes every turn, wired at init. Carry the A1 caveat honestly: the `.claude/` hook config was sandbox-read-denied during research, so this fact is documented from PROJECT.md / STATE.md / CLAUDE.md and corroborated by remote + commit history, not from reading the hook script itself. State that SETUP-01 is satisfied at init and this doc records it as a convention rather than re-wiring it.

    SETUP-02 content (verify each rule against the actual `.gitignore` you read): record that `DNA/` is ignored; GSD tooling dirs are ignored (`.claude/`, `.agents/`, `.gsd/`, `.codex/`, `.gemini/`, `.cursor/`); secrets are ignored (`.env`, `.env.*` with `!.env.example` un-ignored, `*.pem`, `*.key`, `.secrets`); build/OS junk is ignored (`node_modules/`, `dist/`, `build/`, `out/`, `release/`, `.DS_Store`); and `.planning/` is NOT listed in `.gitignore`, therefore tracked (intentional — the research and PRD deliverables belong on GitHub). Frame these as the operating conventions for the project.

    Cite the requirement IDs this doc satisfies (SETUP-01, SETUP-02) in the doc body. Exact filename/section ordering is Claude's Discretion (D-01/D-02 only lock: separate doc, inside the phase folder).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md && grep -q 'ubairrr/MeetingAssist' .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md && grep -qi 'planning' .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md && grep -q 'SETUP-01' .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md && grep -q 'SETUP-02' .planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md` exists.
    - Doc records remote origin `https://github.com/ubairrr/MeetingAssist.git` and the Stop-hook auto-push as conventions (SETUP-01).
    - Doc records the `.gitignore` rules: `DNA/` ignored, GSD tooling dirs ignored, secrets (`.env`/`.env.*` with `!.env.example`) ignored, and `.planning/` tracked (NOT in `.gitignore`) (SETUP-02).
    - Every recorded `.gitignore` rule matches the actual `/Users/ubair/Gits/MeetingAssist/.gitignore`.
    - Doc carries the A1 caveat: the auto-push hook config under `.claude/` was not directly read; the fact is documented from project files + corroborating evidence, not asserted from a hook-script read.
    - Doc cites the requirement IDs SETUP-01 and SETUP-02.
  </acceptance_criteria>
  <done>Setup-baseline record exists, records repo + auto-push + `.gitignore` rules as conventions matching verified facts, carries the A1 caveat, and cites SETUP-01/02.</done>
</task>

<task type="auto">
  <name>Task 2: Write the dev-baseline / project-conventions doc (SETUP-03)</name>
  <files>.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md (§"Dev-Baseline Inputs" lines 170-202 — the VERIFIED DNA stack table + proposed src/main/<domain>/ layout; §"Doc-vs-Code Gaps" line 225 — DNA app version is 1.0.0 not 1.1.0)
    - .planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md (§"01-DEV-BASELINE.md" Pattern Assignment — analog is .planning/research/STACK.md, exact match; copy its DNA-choice→verdict→action table layout and "re-verify at build time" posture)
    - .planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md (D-07 — record DIRECTION + rationale, do NOT final-pin)
    - .claude/CLAUDE.md (§"Executive Verdict on the Inherited DNA" + §"Recommended Stack" — the direction the doc ratifies-as-direction)
    - DNA/package.json (the actual stack versions the doc records — verify the version numbers against this file)
  </read_first>
  <action>
    Create `.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md` fixing the toolchain, the Node/Electron line, and a PROPOSED repo layout as DIRECTION for the future app. This satisfies SETUP-03. Analog to copy: `.planning/research/STACK.md` (table-driven "DNA choice → verdict → action" layout + the "re-verify at build time" posture).

    Use the DNA stack table from RESEARCH §"Dev-Baseline Inputs" (lines 174-188) directly. Verify each version against `DNA/package.json` as you record it. The table records each layer's DNA version as-is plus the recommended direction (not final): Electron `^40.6.1` → bump to the 41.x LTS line (Chromium 146 / Node 24 LTS); React `^19.2.4` → keep 19.x; Vite `^7.3.1` → keep 7.x; `@deepgram/sdk` `^4.11.3` (model `nova-2`) → keep SDK, upgrade Nova-2 → Nova-3; `openai` `^6.25.0` → keep, add Structured Outputs + Zod; `electron-store` `^8.2.0` → keep for small prefs only (secrets → safeStorage, transcripts → SQLCipher); `electron-builder` `^26.8.1` → keep. Node line: Electron 40 → Node 20 ABI today; Electron 41 → Node 24 LTS direction.

    CRITICAL accuracy fixes to honor (RESEARCH §"Doc-vs-Code Gaps"):
    - Record the DNA app version as **1.0.0** (from `DNA/package.json` + all release artifacts), NOT 1.1.0. Note that `DNA/VERSION` (1.4.0) and `DNA/CHANGELOG.md` belong to vendored GSD tooling, not the app.

    Reproduce the proposed `src/main/<domain>/` repo-layout fence from RESEARCH lines 191-201 (the `main/{capture,stt,llm,store,session}`, `preload/`, `renderer/` direction).

    HONOR D-07 (the phase's defining posture): record DIRECTION + rationale only. Explicitly mark exact version pinning AND the final `main/<domain>/` layout as **Phase 5 PRD (PRD-02) / build-time decisions**. Mirror STACK.md's "re-verify and pin at build time" framing. This aligns with REQUIREMENTS.md Out-of-Scope ("Final tech-stack version pinning — re-verify and pin at build time"). Do NOT write language that locks the stack.

    Cite the requirement ID this doc satisfies (SETUP-03) in the doc body. Filename/section ordering is Claude's Discretion.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md && grep -q 'SETUP-03' .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md && grep -q '1.0.0' .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md && grep -qi 'electron' .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md && grep -qi 'build[- ]time\|Phase 5\|PRD' .planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md` exists.
    - Doc captures the toolchain stack table (Electron / React / Vite / Deepgram SDK / openai / electron-store / electron-builder) with DNA's actual versions and the recommended direction, version numbers matching `DNA/package.json`.
    - Doc records the Node/Electron line (Electron 40 → 41 LTS / Node 24 direction).
    - Doc reproduces the proposed `src/main/<domain>/` repo layout.
    - Doc records the DNA app version as 1.0.0 (NOT 1.1.0) and notes `DNA/VERSION` 1.4.0 is GSD tooling.
    - Doc explicitly marks exact version pinning AND the final layout as Phase 5 PRD / build-time decisions (D-07) — no language that final-pins the stack.
    - Doc cites the requirement ID SETUP-03.
  </acceptance_criteria>
  <done>Dev-baseline doc exists, records toolchain + Node/Electron line + proposed layout as DIRECTION, records DNA app version 1.0.0, explicitly defers final pinning to Phase 5 PRD / build time, and cites SETUP-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces Markdown documentation only. No product code, no runtime, no external service, no package installs, no datastore. There is no attack surface introduced this phase (per 01-RESEARCH.md §"Security Domain").

| Boundary | Description |
|----------|-------------|
| (none this phase) | No untrusted input crosses any boundary — docs-only deliverables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-DOC | Information disclosure | Doc accuracy | low | mitigate | Document the `.gitignore` rules accurately so secrets (`.env`, `*.key`, `*.pem`) and the git-ignored `DNA/` reference (which holds live `.env`) remain excluded from the tracked repo — the doc must not contradict or weaken the verified ignore rules |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur this phase (RESEARCH §"Package Legitimacy Audit": zero packages). No [ASSUMED]/[SUS]/[SLOP] packages; legitimacy checkpoint not applicable |

Security findings discovered in the DNA (plaintext API keys in electron-store, etc.) are captured as inputs for DEC-02 / Phase 4 — not fixed this phase. The DEV-BASELINE doc records the "secrets → safeStorage, transcripts → SQLCipher" direction accordingly.
</threat_model>

<verification>
Phase-level checks for this plan (manual `doc-check` per 01-VALIDATION.md — this is a docs-only phase with no test framework):
- Both deliverable files exist in the phase folder.
- SETUP-BASELINE facts match the actual `/Users/ubair/Gits/MeetingAssist/.gitignore` and `git remote -v`.
- DEV-BASELINE records direction-not-pins and marks final pinning as Phase 5 PRD / build-time (spot-check the posture markers).
- No doc repeats a Doc-vs-Code gap as fact (e.g., DNA app version must read 1.0.0, not 1.1.0).
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #1 is TRUE: a written record confirms the SETUP baseline (repo + auto-push + `.gitignore` rules) as operating conventions.
- ROADMAP Phase 1 success criterion #2 is TRUE: a project-conventions / dev-baseline document exists fixing the toolchain, Node/Electron line, and repo layout as DIRECTION.
- Requirements SETUP-01, SETUP-02, SETUP-03 are covered with `doc-check` verification.
</success_criteria>

<output>
Create `.planning/phases/01-dna-deep-dive-project-setup/01-01-SUMMARY.md` when done.
</output>
