---
phase: 01-dna-deep-dive-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md
autonomous: true
requirements: [DNA-01, DNA-02, DNA-03]
must_haves:
  truths:
    - "A selective-adoption catalogue lists each proven DNA technique with an explicit borrow-and-adapt / design-reference / leave-behind verdict — explicitly NOT a wholesale port"
    - "Every borrow-and-adapt technique carries code-level file:line evidence resolving to DNA/src/..."
    - "The catalogue reconciles the DNA's doc-vs-code overstatements (provider seam location, sharp, LSUIElement, NSWindowSharingNone, app version, audio.js) rather than repeating the README"
  artifacts:
    - .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md
  key_links:
    - "Each of the 5 techniques uses the D-04 5-field structure (what DNA does / why valuable / what to change / risk-effort / verdict)"
    - "Every verdict is from the D-03 taxonomy (borrow-and-adapt / design-reference / leave-behind, or defer/undecided only if truly unclassifiable)"
    - "The stealth/overlay entry documents the MECHANISM only and DEFERS the keep-vs-drop ethics call to Phase 2 DEC-01 (D-05)"
---

<objective>
Produce the selective-adoption catalogue (DNA-01, DNA-02, DNA-03) — the durable, code-backed analysis of which proven Interview Helper DNA techniques to borrow-and-adapt vs. design-reference vs. leave behind for MeetingAssist. This is the heaviest Phase 1 deliverable: 5 techniques, each in the D-04 5-field structure, with code-level `file:line` evidence (D-06) and the Doc-vs-Code reconciliations.

Purpose: Make ROADMAP Phase 1 success criterion #3 TRUE — a selective-adoption catalogue with an explicit verdict per technique, explicitly NOT a wholesale port. The catalogue's value is being MORE accurate than the DNA's own README.
Output: One Markdown doc inside the phase folder.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md
@.planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md
@.planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md
</context>

## Artifacts this phase produces

This phase creates the following NEW files (exclude from source-grounding drift verification):
- `.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md` (plan 01)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md` (plan 01)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md`
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md` (plan 03)

<tasks>

<task type="auto">
  <name>Task 1: Write the selective-adoption catalogue — 5 techniques, 5-field entries, verdicts (DNA-01, DNA-02, DNA-03)</name>
  <files>.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md</files>
  <read_first>
    - .planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md (§"Catalogue Source Map" lines 105-145 — the 5 techniques with mechanisms + file:line + recommended verdicts; §"Doc-vs-Code Gaps" lines 217-226 — copy verbatim; §"Architectural Responsibility Map" lines 48-63 — keep mechanism tiers accurate; §"Explicit leave-behind candidates" lines 137-145)
    - .planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md (§"01-DNA-CATALOGUE.md" Pattern Assignment lines 76-136 — analog is .planning/research/ARCHITECTURE.md; the verified DNA excerpts to quote; §"Shared Document Conventions" for house style + ⚠ Correction callout pattern)
    - .planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md (D-03 verdict taxonomy, D-04 5-field structure, D-05 the 5 techniques + stealth-ethics deferral, D-06 deep evidence for borrow-and-adapt / conceptual for leave-behind)
    - DNA/src/main.js (verify the cited mechanisms against real code: provider seam 22-26, speech_final 102-109, stealth flags 247-253, dock.hide 286, vision resize 303, window flags 234-238)
    - DNA/src/preload.js (verify the contextBridge allowlist at 10-29)
    - DNA/src/renderer/audio.js (verify the dual-channel separate-streams capture, 1-69)
  </read_first>
  <action>
    Create `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md` — the selective-adoption catalogue. This satisfies DNA-01 (modules read with evidence), DNA-02 (catalogue + leave-behind list), DNA-03 (explicit verdicts). Analog to copy: `.planning/research/ARCHITECTURE.md` ("map the proven DNA, flag what changes" framing with `file:line`). Use the house-style header block from 01-PATTERNS.md §"Shared Document Conventions".

    Catalogue exactly 5 techniques (the D-05 set), each as a D-04 5-field entry — *what DNA does · why it's valuable · what to change for MeetingAssist · risk/effort · verdict*. Verdict must come from the D-03 taxonomy: `borrow-and-adapt` / `design-reference` / `leave-behind` (use `defer/undecided` only if a technique genuinely cannot be classified). Use the recommended verdicts from RESEARCH §"Catalogue Source Map" and the Executive Verdict framing.

    The 5 techniques, with the citations they MUST carry (verify each against the DNA source you read; cite `DNA/src/...` paths ONLY — never root `main.js`, never `src/audio.js` as canonical, never `adapters/`):
    1. Dual-channel real-time STT + `speech_final` accumulation state machine — verdict `borrow-and-adapt`, DEEP evidence (D-06). Mechanism: TWO independent capture streams (mic via getUserMedia, system via desktopCapturer) each through its own AudioWorklet; main converts Float32→Int16 16kHz mono and feeds two separate Deepgram listen.live sockets labeled mic/system; only the system channel accumulates and fires triggerLLM on `speech_final` with length>10. Cite `DNA/src/main.js:102-109` (state machine), `DNA/src/main.js:28-35, 79-123, 125-181`, `DNA/src/renderer/audio.js:1-69`, `DNA/src/renderer/public/audio-processor.js:1-16`. What to change: Nova-2→Nova-3, generalize "Interviewer" → N speakers, move trigger from interview-answer to artifact pipeline.
    2. OpenAI-`baseURL` provider seam — verdict `borrow-and-adapt`, DEEP evidence. Mechanism: single factory `getLLMClient()` returns `new OpenAI({apiKey, baseURL})` from electron-store (env fallback); every LLM call routes through it; provider swapped purely by Settings UI fields. Cite `DNA/src/main.js:22-26, 46-69, 309-332`, `DNA/src/renderer/components/SettingsPanel.jsx:3-9`, `DNA/src/main.js:260-271`. ⚠ Correction (MUST carry): the seam is in `src/main.js`, NOT in `DNA/adapters/` (that dir is GSD AI-assistant docs). What to change: add strict Structured Outputs + Zod (DNA lacks them).
    3. Hardened `contextBridge` IPC allowlist — verdict `borrow-and-adapt`, DEEP evidence. Mechanism: BrowserWindow with nodeIntegration:false, contextIsolation:true, preload; preload exposes a single `window.electron` with a hard-coded inbound channel allowlist (13 channels) checked before subscribing, plus fixed outbound invoke/send wrappers; no raw ipcRenderer leaks. Cite `DNA/src/preload.js:10-29` (allowlist), `DNA/src/main.js:234-238` (window flags). Contrast cite for the leave-behind list: legacy `DNA/src/audio.js:3` assumes nodeIntegration:true — the insecure path the shipped app abandoned. What to change: redefine the channel set for MeetingAssist's domains.
    4. Vision screenshot → downscale → model round-trip — verdict `design-reference`, conceptual depth acceptable (D-06). Mechanism: global hotkey captures a screen thumbnail via desktopCapturer, downscales to half width with Electron's native `nativeImage.resize()`, toDataURLs it, sends as image_url in a multimodal chat.completions.create through the same provider seam. Cite `DNA/src/main.js:291-346` (resize at 303, LLM call 310-332). ⚠ Correction (MUST carry): the DNA does NOT use `sharp` (absent from package.json deps, never imported); it uses native `thumbnail.resize()`. The D-05 "screenshot→sharp→model" phrasing describes the RECOMMENDED MeetingAssist path, not the DNA's actual implementation. State it as: DNA uses native resize; MeetingAssist recommends `sharp` for finer control.
    5. Overlay / stealth window setup — verdict `borrow-and-adapt` for the TECHNIQUE; the keep-vs-drop ethics call is DEFERRED to Phase 2 DEC-01 (D-05). Mechanism: transparent/frameless/no-shadow BrowserWindow with setVisibleOnAllWorkspaces, setAlwaysOnTop('screen-saver'), setContentProtection(true), setIgnoreMouseEvents(forward), plus app.dock.hide(); a hotkey toggles stealth vs normal mode. Cite `DNA/src/main.js:247-253` (flags), `DNA/src/main.js:286` (dock hide), `DNA/src/main.js:183-199` (mode toggle). ⚠ Corrections (MUST carry): (a) `setContentProtection(true)` maps internally to NSWindowSharingNone but the code never names that constant — describe it as "via setContentProtection → NSWindowSharingNone"; (b) `LSUIElement` is documented-but-NOT-implemented (no key in the shipped Info.plist; only app.dock.hide() runs). The entry documents MECHANISM only; explicitly state the consent/ethics posture (hide-own-panel vs conceal-recording) is decided in Phase 2 DEC-01, not here.

    Include the explicit leave-behind list (conceptual depth only, per D-06) from RESEARCH lines 137-145: legacy `src/audio.js` (mixed-stream + renderer-side Deepgram + nodeIntegration), root `DNA/main.js` duplicate, interview-specific prompts (`DNA/src/main.js:51-61`), plaintext `electron-store` API keys (hand to DEC-02), Nova-2 model literal, `DNA/adapters/` + GSD tooling.

    Copy the "Doc-vs-Code Gaps" table from RESEARCH lines 217-226 VERBATIM into the catalogue so the reconciliations are explicit and the build milestone cannot be misled.

    State up front that this is a selective-adoption analysis, explicitly NOT a wholesale port of Interview Helper. Cite the requirement IDs this doc satisfies (DNA-01, DNA-02, DNA-03). Do NOT paste large code blocks beyond the short verified excerpts already identified in 01-PATTERNS.md; favor mechanism prose + inline `file:line` citations.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'borrow-and-adapt' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'design-reference' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'leave-behind' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'src/main.js:22-26' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'speech_final' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && grep -q 'DEC-01' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md && [ "$(grep -c 'src/main.js:' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md)" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md` exists.
    - Catalogue contains all 5 techniques (dual-channel STT/speech_final, OpenAI-baseURL provider seam, hardened contextBridge IPC, vision round-trip, overlay/stealth), each with the D-04 5-field structure.
    - Every technique carries a verdict from the D-03 taxonomy (borrow-and-adapt / design-reference / leave-behind / defer-undecided).
    - Every borrow-and-adapt entry carries at least one resolvable `DNA/src/...:line` citation (provider seam cites `DNA/src/main.js:22-26`; state machine cites `DNA/src/main.js:102-109`; IPC cites `DNA/src/preload.js:10-29`; stealth cites `DNA/src/main.js:247-253`).
    - No citation points to root `DNA/main.js`, legacy `DNA/src/audio.js` as canonical, or `DNA/adapters/` as the provider seam.
    - The four Doc-vs-Code reconciliations are present and stated as corrections (provider seam NOT in adapters/; DNA uses native resize not sharp; setContentProtection maps to NSWindowSharingNone; LSUIElement documented-but-not-implemented) — the catalogue does NOT repeat any of them as fact.
    - The stealth/overlay entry documents mechanism only and explicitly defers the keep-vs-drop ethics call to Phase 2 DEC-01.
    - An explicit leave-behind list is present (legacy audio.js, root main.js, interview prompts, plaintext electron-store keys, Nova-2 literal, adapters/).
    - Doc states it is selective adoption, NOT a wholesale port, and cites DNA-01, DNA-02, DNA-03.
  </acceptance_criteria>
  <done>The selective-adoption catalogue exists with 5 techniques in the D-04 5-field structure, D-03 verdicts, deep `file:line` evidence for borrow-and-adapt entries, the four Doc-vs-Code reconciliations, the stealth-ethics deferral to DEC-01, the leave-behind list, and citations to DNA-01/02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces Markdown documentation only. No product code, no runtime, no external service, no package installs. No attack surface introduced this phase (01-RESEARCH.md §"Security Domain").

| Boundary | Description |
|----------|-------------|
| (none this phase) | No untrusted input crosses any boundary — docs-only deliverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SEC1 | Information disclosure | Catalogue must capture, not hide, DNA security weaknesses | low | mitigate | The catalogue records the DNA's plaintext-electron-store-keys weakness as a leave-behind and hands it to DEC-02 (use safeStorage/Keychain + SQLCipher); the no-Structured-Output/Zod-validation gap is handed to Phase 4 GRND — so downstream build work inherits the security inputs rather than copying the weak pattern |
| T-01-SEC2 | Tampering | Wrong-file citation propagates an insecure pattern | medium | mitigate | The catalogue cites only `src/...` packaged paths and flags legacy `src/audio.js` (nodeIntegration:true) as the insecure leave-behind, preventing the build milestone from copying the abandoned insecure path |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH §"Package Legitimacy Audit": zero packages). No [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
Phase-level checks for this plan (manual `doc-check` per 01-VALIDATION.md — docs-only phase, no test framework):
- Catalogue file exists in the phase folder.
- Spot-check 2-3 `file:line` citations resolve to the claimed mechanism in `DNA/src/...` (e.g. open `DNA/src/main.js:22-26` and confirm `getLLMClient()`; `DNA/src/main.js:102-109` and confirm the speech_final block).
- Confirm the catalogue does NOT repeat the four README overstatements (sharp, LSUIElement, adapters/ seam, app version 1.1.0) as fact.
- Confirm every borrow-and-adapt entry has code-level evidence (D-06).
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #3 is TRUE: a selective-adoption catalogue lists each proven DNA technique with an explicit borrow-and-adapt / design-reference / leave-behind verdict — explicitly not a wholesale port.
- Requirements DNA-01, DNA-02, DNA-03 are covered with `doc-check` verification and resolvable code-level evidence.
</success_criteria>

<output>
Create `.planning/phases/01-dna-deep-dive-project-setup/01-02-SUMMARY.md` when done.
</output>
