---
phase: 05-prd-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-prd-finalization/05-FEATURE-SPEC.md
autonomous: true
requirements:
  - PRD-01
must_haves:
  truths:
    - "05-FEATURE-SPEC.md exists with a three-column MVP boundary table (Table Stakes v1 / Differentiators / Deferred v2+)"
    - "All 10 table-stakes features (D-01 through D-10) are listed under the Table Stakes column"
    - "All 4 deferred features (D-11 through D-14) are listed under the Deferred v2+ column"
    - "The RSCH-04 capture path correction is documented: audiotee (Core Audio Taps) as primary, Chromium loopback as fallback"
    - "Differentiators section distinguishes MeetingAssist from cloud-bot competitors using RSCH-01 5-pillar framework"
    - "Diarization spec matches RSCH-02: Speaker 1/2/3 labels, v1 cap at 8 speakers, v2 named attribution"
    - "Proposed-with-confirm UX contract is stated (no auto-write to calendar)"
    - "DEC-01 consent gate is listed as a table-stakes requirement, not an optional feature"
    - "D-09 (consent gate) is documented as a hard precondition: audio capture cannot start until consent is explicitly confirmed (references DEC-01 ADR)"
  artifacts:
    - .planning/phases/05-prd-finalization/05-FEATURE-SPEC.md
  key_links:
    - "FEATURE-SPEC.md MVP boundary table is the authoritative feature scope for ARCHITECTURE.md (Plan 02) and BUILD-ORDER.md (Plan 03)"
    - "D-10 capture feature must cite RSCH-04 spike override of CLAUDE.md default"
---

<objective>
Write 05-FEATURE-SPEC.md — the feature specification with explicit MVP boundary table.

Purpose: Establish the authoritative feature scope for v1, locking what ships vs. what is deferred. This document feeds directly into ARCHITECTURE.md (Plan 02) and BUILD-ORDER.md (Plan 03). Every architecture decision and build phase must be justified by a feature in this spec.

Output: `.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md` — a self-contained, cross-linked feature specification covering all features locked in D-01 through D-14 from CONTEXT.md, enriched with research context from RSCH-01, RSCH-02, RSCH-04, RSCH-06, DEC-01, DEC-02, and the 04-AI-SPEC.md proposed-with-confirm contract.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-prd-finalization/05-CONTEXT.md
@.planning/phases/05-prd-finalization/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 05-FEATURE-SPEC.md</name>
  <files>.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md</files>

  <read_first>
    - `.planning/phases/05-prd-finalization/05-CONTEXT.md` — Decisions D-01 through D-14 (the complete locked feature boundary); D-15 and D-16 (document structure)
    - `.planning/phases/05-prd-finalization/05-RESEARCH.md` — "Feeds Into PRD-01 (FEATURE-SPEC.md)" section (V1 Feature Classification synthesized table); Standard Stack; Pitfall 7 (capture path inconsistency); Architectural Responsibility Map
    - `.planning/phases/03-deep-research/03-RSCH-01-REPORT.md` — persona definition, 5 differentiator pillars, monetization model, competitive positioning
    - `.planning/phases/03-deep-research/03-RSCH-02-REPORT.md` — diarization approach: Speaker 1/2/3, v1 cap 8 speakers, v2 named attribution rationale
    - `.planning/phases/03-deep-research/03-RSCH-04-SPIKE-REPORT.md` — capture path decision: audiotee primary, macOS 14.2+ floor, Chromium loopback fallback
    - `.planning/phases/03-deep-research/03-RSCH-06-USE-CASES.md` — meeting-type taxonomy, competitive gap analysis, feature confidence levels
    - `.planning/phases/02-foundational-decisions-adrs/02-DEC-01-consent-recording-posture.md` — consent gate design, all-party-consent, per-meeting confirmation
    - `.planning/phases/02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md` — local-first storage, transcribe-then-delete-raw-audio stance
    - `.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md` §1.5 — proposed-with-confirm UX contract (no auto-write to calendar; all items status:'proposed')
  </read_first>

  <action>
Create `.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md` as a complete, self-contained feature specification document. The document must include the following sections in order:

**Front matter (YAML):**
- `phase: 05`, `document: FEATURE-SPEC`, `version: 1.0`, `status: final`, `requirements_covered: PRD-01`, `created: 2026-06-26`

**Section 1: Purpose and Scope**
State that this document fixes the MVP boundary for MeetingAssist v1. Note that no new decisions are made here — all feature classifications derive from locked decisions in Phases 1–4. Reference the four locked-decision sources: CONTEXT.md D-01 through D-14, RSCH-04 (capture path), RSCH-02 (diarization), DEC-01 (consent gate). State that ARCHITECTURE.md (05-ARCHITECTURE.md) and BUILD-ORDER.md (05-BUILD-ORDER.md) are the consumers of this spec.

**Section 2: MVP Boundary Table**
A three-column markdown table: "Feature" | "Classification" | "Rationale / Source". Include all of the following rows in this exact classification:

Table Stakes — v1 (must ship):
1. Consent gate — per-meeting, all-party disclosure + checkbox UI — DEC-01 hard precondition to capture
2. Dual-channel audio capture — mic (AudioWorklet) + system audio (audiotee Core Audio Taps primary, macOS 14.2+; Chromium loopback fallback, macOS 15.0+ tested) — D-10, RSCH-04 spike override of CLAUDE.md default
3. Real-time transcription with Speaker 1/2/3 diarization — Deepgram Nova-3, v1 cap 8 speakers — D-02, RSCH-02
4. Persistent side overlay — always-on-top, no dock icon, screen-share-invisible panel — D-01, DNA Technique 5
5. Live summary board — 5-minute interval cards stacked in overlay — D-08, 04-AI-SPEC §2.3
6. TranscriptStore — full meeting record encrypted at rest (SQLCipher AES-256) — D-02, DEC-02
7. End-of-meeting artifact batch — MOM, key points, summary, action items, dates/events — D-03, D-04, D-05, D-06
8. Citation-backed artifact extraction — proposed-with-confirm UX, verbatim quote anchors, "Verify" toggle — D-03 through D-06, 04-AI-SPEC §1.2–1.5
9. .ics export — confirmed action items exported as iCalendar; zero OAuth required — D-06
10. Break assist — manual "Going on Break" / "I'm Back" trigger; shows interval cards missed + "While you were away" digest — D-07, 04-AI-SPEC §2.9

Differentiators (competitive advantages, still v1):
- No visible bot — local-first, macOS-native capture; no meeting room invite, no bot account — RSCH-01
- Pre-mixer system audio via Core Audio Taps — volume-independent, clean PCM; no persistent purple screen-recording indicator — RSCH-04
- Faithfulness-grounded extraction — citation anchors, two-stage extraction, CitationValidator 90% token overlap — 04-AI-SPEC §1.2
- Live summary board with break assist — the combination does not exist in any bot-free competitor — RSCH-06 competitive gap
- Encrypted local-first storage — SQLCipher + safeStorage; addresses the #1 adoption barrier (46–50% of workers cite privacy as top concern per RSCH-01)

Deferred v2+ (explicitly NOT in v1):
1. Live assistant interactive chat UI — D-11; ContextEngine architecture is v1, interactive chat UI is v2
2. Meeting-type-specific templates — D-12; one universal template in v1; template variety needs usage data
3. Cross-meeting search UX — D-13; sqlite-vec DB schema is v1 infrastructure; search UX is v2
4. Named speaker attribution ("Alice" / "Bob") — D-14; v1 ships Speaker 1/2/3 labels; name confirmation UX is v2
5. Google Calendar / Outlook direct API — v2; .ics export covers all calendars in v1; OAuth adds complexity
6. Slack, Notion, CRM integrations — v2 per RSCH-06 competitive analysis; post-launch

**Section 3: Feature Details — Table Stakes**
For each of the 10 table-stakes features, write a subsection with:
- Feature name (heading)
- One-sentence description
- User story (who does what, what they get)
- Key behaviors: 3–5 bullet points stating observable behaviors — not implementation details
- Source decisions: cite D-NN or RSCH-NN references

Feature ordering: consent gate first (it gates everything), then capture, then transcription, then overlay, then summary board, then transcript store, then artifact batch, then citation UX, then .ics export, then break assist.

For the capture feature (Dual-channel audio capture): explicitly note that RSCH-04 spike supersedes the CLAUDE.md default recommendation. Primary path: audiotee (Core Audio Taps, macOS 14.2+, no purple indicator, pre-mixer audio). Fallback path: native Chromium loopback flags (macOS 15.0+ as tested in spike, purple indicator, post-mixer). Do not list electron-audio-loopback as a named package — it is superseded.

For the artifact extraction feature: explicitly state the proposed-with-confirm contract (per 04-AI-SPEC §1.5): all extracted items are status:'proposed'; the user must confirm before anything is written to any external system; no auto-write to calendar.

**Section 4: Feature Details — Deferred v2+**
For each deferred feature, write a subsection with:
- Feature name (heading)
- Why it is deferred (one sentence — the reason must match CONTEXT.md, not invent a new reason)
- What is built in v1 as infrastructure (where applicable): e.g., "ContextEngine architecture is built in v1; the interactive chat UI is v2"

**Section 5: Platform and System Requirements**
- macOS 14.2+ required for primary capture path (audiotee); macOS 15.0+ tested for fallback; v1 does not support macOS < 14.2
- Single-user local app; no server-side components in v1
- Meeting data stored locally, encrypted at rest (SQLCipher AES-256); no cloud storage
- Deepgram mip_opt_out=true required (RSCH-03); Gemini paid plan only (RSCH-03 critical warning: free tier allows training on submitted data)

**Section 6: Cross-References**
A table: "Document" | "Relationship" | "Link"
- 05-ARCHITECTURE.md | Architecture spec that implements this feature scope | ./05-ARCHITECTURE.md
- 05-BUILD-ORDER.md | Build phase sequence derived from this scope | ./05-BUILD-ORDER.md
- 05-PRD.md | Executive hub document that summarizes this spec | ./05-PRD.md
- 04-AI-SPEC.md | Faithfulness contract governing artifact extraction | ../04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
- 02-DEC-01 | Consent gate ADR | ../02-foundational-decisions-adrs/02-DEC-01-consent-recording-posture.md
- 02-DEC-02 | Data-handling/privacy ADR | ../02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
  </action>

  <verify>
    <automated>
      ls /Users/ubair/Gits/MeetingAssist/.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md && echo "FILE_EXISTS"
    </automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md` exists
    - File contains a heading "MVP Boundary Table" (or equivalent table with Table Stakes / Differentiators / Deferred v2+ columns)
    - File contains the string "D-10" (D-10 capture feature citation)
    - File contains the string "D-11" (D-11 deferred live assistant)
    - File contains the string "RSCH-04" (capture path spike override)
    - File contains the string "audiotee" (primary capture library)
    - File contains the string "proposed" (proposed-with-confirm UX contract)
    - File contains the string "mip_opt_out" (Deepgram DPA requirement from RSCH-03)
    - File contains the string "DEC-01" (consent gate source citation)
    - File contains the string "Speaker 1" (diarization label format per RSCH-02)
    - File contains "05-ARCHITECTURE.md" (cross-reference link to sibling document)
  </acceptance_criteria>

  <done>
    05-FEATURE-SPEC.md exists, contains a three-column MVP boundary table with all 10 table-stakes features (D-01 through D-10), all 4 deferred features (D-11 through D-14), the RSCH-04 capture path correction, the proposed-with-confirm UX contract, and cross-references to all sibling PRD documents.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → PRD documents | Prior phase documents are authoritative sources; executor must not introduce new decisions not in those documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | FEATURE-SPEC.md authoring | low | mitigate | Executor must trace every feature classification to a CONTEXT.md D-NN decision or prior phase source; no new scope decisions may be introduced in this plan |
| T-05-02 | Information Disclosure | Cross-references section | low | accept | Links are relative paths within .planning/ — no sensitive data; public planning artifacts |
</threat_model>

<verification>
After task completion, verify:
1. `ls .planning/phases/05-prd-finalization/05-FEATURE-SPEC.md` — file exists
2. Open file and confirm: three-column MVP boundary table is present
3. All 10 table-stakes items from D-01 through D-10 appear in the table
4. All 4 deferred items from D-11 through D-14 appear in the table
5. The document cites RSCH-04 for the audiotee/capture path decision
6. The document does not introduce any feature not in CONTEXT.md D-01 through D-14
</verification>

<success_criteria>
- 05-FEATURE-SPEC.md written and committed
- MVP boundary is explicit: 10 table-stakes features, 5 differentiators, 6 deferred v2+ items
- Every feature classification is traceable to a locked Phase 1–4 decision
- No new decisions introduced (synthesis only)
- Cross-references to all three sibling PRD documents are present
</success_criteria>

<output>
Create `.planning/phases/05-prd-finalization/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Plan | Requirement |
|------|------|-------------|
| `.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md` | 05-01 (this plan) | PRD-01 |
| `.planning/phases/05-prd-finalization/05-ARCHITECTURE.md` | 05-02 | PRD-02 |
| `.planning/phases/05-prd-finalization/05-BUILD-ORDER.md` | 05-03 | PRD-03 |
| `.planning/phases/05-prd-finalization/05-PRD.md` | 05-04 | PRD-04 |
</output>
