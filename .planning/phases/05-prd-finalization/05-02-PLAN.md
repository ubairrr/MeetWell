---
phase: 05-prd-finalization
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .planning/phases/05-prd-finalization/05-ARCHITECTURE.md
autonomous: true
requirements:
  - PRD-02
must_haves:
  truths:
    - "05-ARCHITECTURE.md exists with a named module map for every service in src/main/<domain>/"
    - "TypeScript interface is defined for each of the 7 service modules: CaptureService, STTAdapter, LLMAdapter, TranscriptStore, ContextEngine, ArtifactPipeline, SessionManager"
    - "SessionManager FSM defines all 6 states (Idle/PreCapture/Capturing/OnBreak/Processing/Complete) and all transitions"
    - "The consent gate is specified as a hard FSM precondition: PreCapture → Capturing ONLY on consent-confirmed IPC event"
    - "Full IPC channel surface is documented with typed payload shapes for all 12 inbound + outbound channels"
    - "Database schema specifies exactly 7 tables: meetings, transcript_segments, artifacts, action_items, vec_chunks, summary_cards, epoch_summaries"
    - "Electron BrowserWindow configuration for side overlay is specified (always-on-top, frame:false, no-dock, NSWindowSharingNone)"
    - "React component tree lists all 6 top-level UI components"
    - "Gemini paid-plan validation requirement is explicitly documented (free tier disqualified per DEC-02/RSCH-03)"
    - "audiotee is named as primary capture library; Chromium loopback flags named as fallback"
    - "04-AI-SPEC.md compatibility is explicitly stated and all Zod schemas from §1.6 are referenced"
  artifacts:
    - .planning/phases/05-prd-finalization/05-ARCHITECTURE.md
  key_links:
    - "ARCHITECTURE.md module interfaces are the scaffolding contracts the build milestone implements"
    - "IPC channel table (typed payload shapes) constrains both the preload script and renderer API surface"
    - "DB DDL section (7 tables) constrains TranscriptStore, ArtifactStore, and SummaryCardStore implementations"
    - "SessionManager FSM state diagram constrains all capture start/stop logic in the build milestone"
---

<objective>
Write 05-ARCHITECTURE.md — the production-grade modular architecture specification.

Purpose: Provide prescriptive module interfaces, IPC contracts, DB DDL, FSM state diagram, and React component tree precise enough that a contractor can scaffold the codebase without needing to make design decisions. This document consumes the feature scope from 05-FEATURE-SPEC.md (Plan 01) and the ContextEngine / ArtifactPipeline contracts from 04-AI-SPEC.md. It feeds BUILD-ORDER.md (Plan 03) and PRD.md (Plan 04).

Output: `.planning/phases/05-prd-finalization/05-ARCHITECTURE.md` covering both the backend service layer (TranscriptStore, SessionManager FSM, ContextEngine, ArtifactPipeline, CaptureService) and the UI/IPC layer (overlay BrowserWindow configuration, contextBridge channel surface, React component tree). Must be compatible with 04-AI-SPEC.md — the ContextEngine and ArtifactPipeline interfaces extend, not contradict, the AI-SPEC.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-prd-finalization/05-CONTEXT.md
@.planning/phases/05-prd-finalization/05-RESEARCH.md
@.planning/phases/05-prd-finalization/05-FEATURE-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 05-ARCHITECTURE.md</name>
  <files>.planning/phases/05-prd-finalization/05-ARCHITECTURE.md</files>

  <read_first>
    - `.planning/phases/05-prd-finalization/05-CONTEXT.md` — D-17 (module map + interface contracts), D-18 (full-stack scope), D-19, D-20; Claude's Discretion items (SessionManager FSM states, Zod schemas)
    - `.planning/phases/05-prd-finalization/05-RESEARCH.md` — "Feeds Into PRD-02 (ARCHITECTURE.md)" section; Architecture Patterns (all 3 patterns: SessionManager FSM, IPC Channel Contract, Quote-Backed Extraction); Architectural Responsibility Map; System Architecture Diagram; Recommended Project Structure; Database Schema code example; Standard Stack table; Pitfalls 1–7; Anti-Patterns to Avoid; Don't Hand-Roll table; Open Questions 1–4
    - `.planning/phases/04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md` — §1.2 Quote-Backed Extraction Protocol; §1.3 CitationAnchorSchema; §1.6 all Zod schemas (ActionItemSchema, MeetingArtifactsSchema, SummaryCardSchema); §2.1–2.9 ContextEngine architecture; §2.8 ArtifactPipeline map-reduce; §2.9 Break Assist digest data flow
    - `.planning/phases/03-deep-research/03-RSCH-05-DATA-MODEL.md` — 5-table DB schema DDL (meetings, transcript_segments, artifacts, action_items, vec_chunks); the ARCHITECTURE.md must add summary_cards and epoch_summaries
    - `.planning/phases/03-deep-research/03-RSCH-04-SPIKE-REPORT.md` — audiotee primary path details; macOS floor; AudioWorklet + IPC architecture for mic capture
    - `.planning/phases/03-deep-research/03-RSCH-02-REPORT.md` — diarization pipeline: mic channel always "You"; system audio separate speaker registry
    - `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md` — Technique 3 (hardened contextBridge IPC allowlist pattern); Technique 5 (overlay window setup: setAlwaysOnTop, setVisibleOnAllWorkspaces, setIgnoreMouseEvents, app.dock.hide)
    - `.planning/phases/02-foundational-decisions-adrs/02-DEC-01-consent-recording-posture.md` — consent gate as hard FSM precondition
    - `.planning/phases/03-deep-research/03-RSCH-03-VENDOR-TERMS.md` — Deepgram mip_opt_out=true; Gemini paid plan critical warning
    - `.claude/CLAUDE.md` — full stack table; Electron 41.x LTS; React 19; Vite 7; all package choices; macOS entitlements
  </read_first>

  <action>
Create `.planning/phases/05-prd-finalization/05-ARCHITECTURE.md` as a complete, self-contained production-grade architecture specification. The document must include the following sections in order:

**Front matter (YAML):**
- `phase: 05`, `document: ARCHITECTURE`, `version: 1.0`, `status: final`, `requirements_covered: PRD-02`, `ai_spec_version: 1.0`, `created: 2026-06-26`

**Section 1: Purpose and Scope**
State that this document specifies the production-grade modular architecture for MeetingAssist v1. It is prescriptive enough that a contractor can scaffold the codebase without design decisions. All technology choices are locked in CLAUDE.md; all AI/extraction contracts are locked in 04-AI-SPEC.md. This document fills in: the module map, TypeScript interfaces for each service, the SessionManager FSM, the full IPC channel surface with typed payloads, the complete DB DDL (7 tables), the Electron overlay window configuration, and the React component tree. Reference 05-FEATURE-SPEC.md as the feature scope this architecture implements.

**Section 2: Technology Stack**
A table: "Layer" | "Technology" | "Version" | "Purpose". Include:
- Desktop shell: Electron 41.x LTS (Chromium 146, Node 24 LTS)
- UI framework: React 19.x (hooks-only) + Vite 7.x
- STT: @deepgram/sdk (nova-3, diarize:true, mip_opt_out:true hardcoded)
- LLM adapter: openai SDK via baseURL (Gemini 2.5 Flash default; Gemini paid plan only — free tier disqualified by DEC-02/RSCH-03)
- Schema validation: zod + zod-to-json-schema
- Persistence: better-sqlite3-multiple-ciphers (SQLCipher AES-256) + sqlite-vec 0.1.9
- System audio capture (primary): audiotee 0.0.7 (Core Audio Taps, macOS 14.2+)
- System audio capture (fallback): native Chromium loopback flags MacLoopbackAudioForScreenShare / MacSckSystemAudioLoopbackOverride (macOS 15.0+ tested)
- Token counting: tiktoken cl100k_base
- Calendar export: ics library
- Key storage: Electron safeStorage → macOS Keychain
- Small prefs: electron-store (non-sensitive settings only)

**Section 3: Directory Structure**
The complete recommended project structure as a tree view, drawn from 05-RESEARCH.md "Recommended Project Structure" section. Include every directory and key file name, with inline comments.

**Section 4: Architectural Responsibility Map**
A table mapping every capability to its tier (Electron main / Renderer / Preload / Both). Source: 05-RESEARCH.md Architectural Responsibility Map. Ensure all 15 capabilities from the research map are included.

**Section 5: SessionManager FSM (per D-17, D-18; Claude's Discretion)**
The finite state machine governing the meeting lifecycle. Include:
- A prose description stating that ALL capture start/stop must go through this FSM; direct calls to CaptureService bypassing the FSM are forbidden
- The 6 states: Idle, PreCapture, Capturing, OnBreak, Processing, Complete — with a one-sentence description of each
- The complete transition table: "From State" | "Event" | "To State" | "Guard / Precondition"
  - Idle → PreCapture: user fires start-meeting IPC
  - PreCapture → Capturing: ONLY on consent-confirmed IPC event (hard precondition per DEC-01; transition is blocked if this event has not been received)
  - Capturing → OnBreak: user fires start-break IPC
  - OnBreak → Capturing: user fires end-break IPC (records break_start_timestamp to break_end_timestamp for digest window)
  - Capturing → Processing: user fires end-meeting IPC
  - Processing → Complete: ArtifactPipeline emits pipeline-complete internal event
  - Complete → Idle: user dismisses artifact review or fires start-meeting IPC (new meeting)
- The TypeScript interface `SessionManagerPort` from 05-RESEARCH.md Pattern 1
- A note: The consent gate is a hard FSM guard, not a UI-only check. The main process enforces it.

**Section 6: Module Map and TypeScript Interfaces**
For each service module, provide: module directory path, responsibility description, and the TypeScript interface (port definition). Modules to cover:

6.1 CaptureService (src/main/capture/)
- Responsibility: Manages AudioWorklet mic bridge + audiotee system audio tap + Deepgram WebSocket lifecycle for both channels
- Interface: start(), stop(), onAudioChunk(cb), onCaptureHealthUpdate(cb), getHealth(): CaptureHealth
- Note: mip_opt_out must be hardcoded true in Deepgram SDK init (not a user setting, per DEC-02/RSCH-03)
- Note: mic channel always maps speaker 0 to "You"; system audio has independent speaker ID space (RSCH-02)

6.2 STTAdapter (src/main/stt/)
- Responsibility: Provider seam for streaming STT; default Deepgram Nova-3 dual WebSocket; swappable to AssemblyAI behind the seam
- Interface: connect(channel: 'mic' | 'system'), disconnect(channel), onSpeechFinal(cb: (segment: TranscriptSegment) => void), onError(cb)

6.3 LLMAdapter (src/main/llm/)
- Responsibility: Provider-agnostic LLM calls via openai SDK baseURL; handles both OpenAI structured outputs and Gemini responseJsonSchema via zod-to-json-schema
- Interface: complete(messages, schema: ZodSchema): Promise<z.infer<ZodSchema>>, stream(messages): AsyncIterable<string>
- Note: Gemini free tier is disqualified for meeting data (DEC-02 / RSCH-03). The settings validator must refuse to accept a Gemini API key without a billing confirmation flag set by the user.

6.4 EmbeddingAdapter (src/main/llm/)
- Responsibility: Provider-agnostic embedding generation for epoch RAG; default text-embedding-3-small (OpenAI, 1536 dims); falls back to Gemini embedding model if only Gemini key configured
- Interface: embed(text: string): Promise<Float32Array>

6.5 TranscriptStore (src/main/store/)
- Responsibility: SQLCipher DB writes for meetings, transcript_segments; reads for meeting history
- Interface: createMeeting(meta: MeetingMeta): string, appendSegment(segment: TranscriptSegment): void, getMeeting(id: string): Meeting, getSegments(meetingId: string, window?: {start: number, end: number}): TranscriptSegment[]

6.6 ArtifactStore (src/main/store/)
- Responsibility: SQLCipher DB writes for artifacts, action_items; status lifecycle (proposed → confirmed / dismissed)
- Interface: saveArtifacts(artifacts: MeetingArtifactsSchema): void, confirmArtifact(id: string): void, editArtifact(id: string, data: Partial<ArtifactPayload>): void, dismissArtifact(id: string): void, getConfirmedActionItems(meetingId: string): ActionItem[]

6.7 SummaryCardStore (src/main/store/)
- Responsibility: Persists SummaryCard and EpochSummary records; provides reads for BreakAssist digest
- Interface: saveCard(card: SummaryCardSchema): void, saveEpoch(epoch: EpochSummarySchema): void, getCardsSince(meetingId: string, sinceSeconds: number): SummaryCard[]

6.8 ContextEngine (src/main/context/)
- Responsibility: Manages rolling transcript window (token-counted via tiktoken cl100k_base), SummaryCardTimer (5-min intervals), EpochCompressor (fires at 560K tokens), ContextComposer (assembles context window for LLM calls)
- Interface: ingest(segment: TranscriptSegment): void, getContext(): ContextWindow, onCardReady(cb: (card: SummaryCard) => void): void, onEpochCompressed(cb: (epoch: EpochSummary) => void): void
- Key invariants: 800K ceiling; evict oldest segments; never use summary_cards as epoch input (reads from transcript_segments only, per AI-SPEC §2.2 Pitfall 4)

6.9 ArtifactPipeline (src/main/pipeline/)
- Responsibility: End-of-meeting batch extraction using map-reduce over 5-min chunk intervals; CitationValidator; writes proposed artifacts to DB
- Interface: run(meetingId: string): Promise<MeetingArtifacts>, onProgress(cb: (pct: number) => void): void
- Two-stage extraction: Stage 1 (evidence anchors, separate LLM call) → Stage 2 (constrained generation from anchors only); per 04-AI-SPEC §1.2

6.10 CalendarExportService (src/main/calendar/)
- Responsibility: Generates .ics file from confirmed action items; opens native file-save dialog; uses ics library
- Interface: exportConfirmedItems(meetingId: string): Promise<{filePath: string}>, previewIcs(items: ActionItem[]): string

**Section 7: IPC Channel Contract (contextBridge Allowlist)**
State that this is the complete, typed IPC channel surface. No channel outside this list may be exposed via contextBridge. No raw ipcRenderer may be exposed to the renderer. Source: DNA Technique 3 (hardened contextBridge pattern).

Present the inbound channels (main → renderer) as a table: "Channel" | "Direction" | "Payload TypeScript Interface" | "Trigger"
All 6 inbound channels from 05-RESEARCH.md Pattern 2 with typed payloads:
- session-state-changed → {state: SessionState, previous: SessionState}
- transcript-segment → {id: string, meetingId: string, text: string, speakerLabel: string, channel: 'mic' | 'system', timestampStart: number, timestampEnd: number, isFinal: true}
- summary-card-ready → {card: SummaryCard} (SummaryCard per AI-SPEC §2.3 SummaryCardSchema)
- break-assist-digest-ready → {digest: BreakAssistDigest, cardsMissed: SummaryCard[]}
- artifact-proposals-ready → {artifacts: MeetingArtifacts, meetingId: string}
- capture-health-update → {status: 'healthy' | 'silent' | 'error', channel: 'mic' | 'system', message?: string}

Present the outbound channels (renderer → main) as a table:
All 10 outbound channels from 05-RESEARCH.md Pattern 2 with typed payloads:
- consent-confirmed → {meetingId: string, timestamp: number}
- mic-audio-chunk → {frames: Float32Array, sampleRate: number}
- start-meeting → {meetingTitle?: string}
- end-meeting → {} (no payload)
- start-break → {} (timestamp recorded by main)
- end-break → {} (timestamp recorded by main)
- confirm-artifact → {id: string, type: 'action_item' | 'decision' | 'date'}
- edit-artifact → {id: string, type: 'action_item' | 'decision' | 'date', updates: Record<string, unknown>}
- dismiss-artifact → {id: string, type: 'action_item' | 'decision' | 'date'}
- export-ics → {meetingId: string}
- get-settings → {} (returns Settings)
- set-setting → {key: string, value: unknown}

**Section 8: Database Schema (7 Tables)**
Full DDL for all 7 SQLCipher tables. Header note: "RSCH-05 defines 5 tables (meetings, transcript_segments, artifacts, action_items, vec_chunks). Two additional tables (summary_cards, epoch_summaries) are required by 04-AI-SPEC.md §2.2 and §2.5. Total: 7 tables."

Include the complete SQL CREATE TABLE statements for all 7 tables. For the 5 original RSCH-05 tables, source the DDL from 03-RSCH-05-DATA-MODEL.md. For summary_cards and epoch_summaries, use the DDL from 05-RESEARCH.md "Database Schema" code example (which is sourced from AI-SPEC §2.2/§2.5). Each table's DDL must include a comment line citing its source: either "-- Source: RSCH-05" or "-- Source: AI-SPEC §2.2/§2.5 additions".

Include a subsection on DB initialization:
- DB key generated via Electron safeStorage on first run; stored in macOS Keychain
- DB opened with PRAGMA key using the safeStorage-decrypted key
- sqlite-vec loaded as extension immediately after open
- better-sqlite3-multiple-ciphers must be rebuilt against Electron's Node ABI (electron-rebuild)
- The .node file and audiotee Swift binary must both be in asarUnpack

**Section 9: Electron Overlay Window Configuration**
The BrowserWindow configuration for the side overlay panel. Source: DNA Technique 5 + CLAUDE.md overlay section.
Specify:
- window dimensions: width 380px, height 100% of screen height, x positioned at screen.width - 380
- BrowserWindow options: frame: false, transparent: true, alwaysOnTop: true with level 'screen-saver', hasShadow: false, skipTaskbar: true, focusable: false (mouse events off by default; on when user interacts)
- webPreferences: contextIsolation: true, nodeIntegration: false, sandbox: false, preload: path to preload/index.js
- setVisibleOnAllWorkspaces(true, {visibleOnFullScreen: true})
- setAlwaysOnTop(true, 'screen-saver')
- setIgnoreMouseEvents(true, {forward: true}) — toggle off when user needs to interact with overlay
- app.dock.hide() — no dock icon (LSUIElement behavior; the app is a background overlay assistant)
- NSWindowSharingNone equivalent: setContentProtection(true) — hides overlay from own screen-share (DNA Technique 5 verdict: keep for the mechanism, not for concealment ethics)
- Note: setContentProtection hides the overlay from the user's own screen-share so they can screen-share the meeting content without the assistant UI showing up. This is ethically acceptable (DEC-01 §2). It does not conceal the fact of recording.

**Section 10: React Component Tree**
The top-level React component hierarchy for the overlay renderer. Include:
- App.tsx — root component; IPC event wiring; session state management via useSessionState hook
  - ConsentGateScreen — shown in PreCapture state; checkbox + disabled-until-checked Start button; fires consent-confirmed IPC
  - LiveSummaryBoard — shown in Capturing/OnBreak/Processing states; stacked SummaryCard components; receives summary-card-ready IPC
    - SummaryCard — individual 5-minute interval card; topic headline + key points + speaker contributions
  - BreakAssistPanel — always rendered in Capturing state; "Going on Break" button; "I'm Back" button; when I'm Back pressed shows digest modal
    - BreakAssistDigest — modal showing interval cards missed + digest text; receives break-assist-digest-ready IPC
  - ArtifactReviewPanel — shown in Complete state; renders proposals from artifact-proposals-ready IPC
    - ArtifactItem — individual proposal with confirm/edit/dismiss actions; "Verify" toggle to show citations
    - CitationPanel — shows verbatim quote anchors for a given artifact (hidden by default per AI-SPEC §1.4 D-02)
  - SettingsPanel — accessible from any state; provider configuration; note: Gemini free tier shows a warning banner requiring paid-plan confirmation before enabling
  - AudioWorkletHost — hidden component; manages mic AudioWorklet lifecycle; sends mic-audio-chunk IPC; source: DNA Technique 1 adapted

**Section 11: Zod Schemas (Shared, src/shared/schemas/index.ts)**
State: All Zod schemas are the single source of truth for both TypeScript types and LLM structured output formats (both OpenAI and Gemini paths via zod-to-json-schema). Schemas are locked in 04-AI-SPEC.md §1.6 and must not be modified without a phase-level decision. List the canonical schema names: CitationAnchorSchema, ActionItemSchema, DecisionSchema, ExtractedDateSchema, KeyPointSchema, MeetingArtifactsSchema, SummaryCardSchema, EpochSummarySchema, BreakAssistDigestSchema. Reference the code examples in 05-RESEARCH.md for the full definitions. State that zod-to-json-schema is used for provider-agnostic schema delivery (single call produces both OpenAI json_schema and Gemini responseJsonSchema formats, per AI-SPEC §1.8).

**Section 12: Security Controls**
A table of security controls with their implementation locations. Source: 05-RESEARCH.md Security Domain section and RSCH-03.
- DB encryption at rest: SQLCipher AES-256 via better-sqlite3-multiple-ciphers; key from safeStorage → macOS Keychain (DEC-02, V6)
- Deepgram data privacy: mip_opt_out: true hardcoded in Deepgram SDK init; never a user-configurable setting (RSCH-03, DEC-02)
- Gemini paid-plan gate: SettingsPanel validates Gemini key type; warns user if billing confirmation not set; free-tier Gemini is disqualified (RSCH-03 critical warning; free tier allows training on submitted data)
- Contextbridge hardening: no raw ipcRenderer exposed; typed channel allowlist only (DNA Technique 3, V5)
- LLM output validation: all LLM responses validated against Zod schema; responses failing validation are rejected and surfaced as extraction-failed events (V5)
- Prompt injection mitigation: transcript content passed as user/data role; extraction instructions in system role; the two-stage extraction protocol ensures Stage 2 only receives extracted quotes, not the full transcript (AI-SPEC §1.7)
- Citation forgery detection: CitationValidator 90% token overlap check; any artifact failing CitationValidator is rejected, not shown as proposed (AI-SPEC §2.8)
- Secrets in Keychain: API keys stored via safeStorage; electron-store is for non-sensitive prefs only (DNA leave-behind applied)
- Transcript retention: local-only; indefinite until user deletes; raw audio deleted after transcription (DEC-02)
- Content protection: setContentProtection(true) prevents overlay from appearing in own screen-share; does not conceal the fact of recording (DEC-01)

**Section 13: Cross-References**
- 05-FEATURE-SPEC.md | Feature scope this architecture implements | ./05-FEATURE-SPEC.md
- 05-BUILD-ORDER.md | Build phase sequence for implementing this architecture | ./05-BUILD-ORDER.md
- 05-PRD.md | Executive hub document | ./05-PRD.md
- 04-AI-SPEC.md | Faithfulness contract; ContextEngine + ArtifactPipeline source of truth | ../04-ai-grounding-context-spec-ai-spec/04-AI-SPEC.md
- 03-RSCH-05-DATA-MODEL.md | Original 5-table DB schema | ../03-deep-research/03-RSCH-05-DATA-MODEL.md
- 03-RSCH-04-SPIKE-REPORT.md | Capture path spike results | ../03-deep-research/03-RSCH-04-SPIKE-REPORT.md
- 02-DEC-01 | Consent gate ADR | ../02-foundational-decisions-adrs/02-DEC-01-consent-recording-posture.md
- 02-DEC-02 | Data-handling/privacy ADR | ../02-foundational-decisions-adrs/02-DEC-02-data-handling-privacy.md
  </action>

  <verify>
    <automated>
      ls /Users/ubair/Gits/MeetingAssist/.planning/phases/05-prd-finalization/05-ARCHITECTURE.md && echo "FILE_EXISTS"
    </automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/05-prd-finalization/05-ARCHITECTURE.md` exists
    - File contains the string "SessionManagerPort" (TypeScript interface for FSM)
    - File contains the string "consent-confirmed" (IPC channel)
    - File contains "summary_cards" (7th table, AI-SPEC addition)
    - File contains "epoch_summaries" (AI-SPEC §2.5 addition)
    - File contains "mip_opt_out" (Deepgram DPA requirement hardcoded)
    - File contains "Gemini" and "paid" in proximity (Gemini paid-plan gate requirement from RSCH-03)
    - File contains "audiotee" (primary capture library)
    - File contains "MacLoopbackAudioForScreenShare" (fallback capture flag)
    - File contains "setContentProtection" (overlay screen-share hiding from DNA Technique 5)
    - File contains "contextIsolation" (BrowserWindow webPreferences)
    - File contains "ArtifactPipeline" (service module)
    - File contains "CitationValidator" (citation integrity check)
    - File contains "7 tables" or lists all 7 table names (meetings, transcript_segments, artifacts, action_items, vec_chunks, summary_cards, epoch_summaries)
    - File contains "05-FEATURE-SPEC.md" (cross-reference link)
  </acceptance_criteria>

  <done>
    05-ARCHITECTURE.md exists with: module map covering all services in src/main/<domain>/, TypeScript interfaces for each service, SessionManager FSM with all 6 states and transitions (consent gate as hard precondition), full IPC channel surface with typed payloads, 7-table DB DDL, Electron overlay BrowserWindow configuration, React component tree, Zod schema index, security controls, and cross-references to all sibling documents.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prior phase specs → Architecture contracts | AI-SPEC §1.6 Zod schemas and §2 ContextEngine architecture are locked; this plan references them, does not redefine them |
| Architecture spec → Build milestone | The interfaces defined here become contracts the build milestone implements; errors here propagate forward |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | ARCHITECTURE.md — LLM adapter section | high | mitigate | The Gemini free-tier disqualification (per RSCH-03) must be explicitly documented in the LLM adapter spec and SettingsPanel section; omitting it creates a path for meeting data to reach Gemini free tier (which allows training per RSCH-03) |
| T-05-04 | Information Disclosure | DB schema section | medium | mitigate | Verify the summary_cards and epoch_summaries tables are present; missing tables means the build milestone will not have correct DB contracts for the AI-SPEC features |
| T-05-05 | Tampering | SessionManager FSM | medium | mitigate | The consent gate must be specified as a main-process FSM guard, not a renderer-only UI check; a renderer-only check can be bypassed by a malicious extension or injected script |
| T-05-06 | Tampering | IPC channel surface | medium | mitigate | All channels must be in the typed allowlist; document that no raw ipcRenderer may be exposed; any unlisted channel is a potential privilege escalation path in the renderer context |
</threat_model>

<verification>
After task completion, verify:
1. `ls .planning/phases/05-prd-finalization/05-ARCHITECTURE.md` — file exists
2. Open file and confirm: SessionManager FSM section is present with all 6 states
3. Confirm: 7 tables are listed in the DB schema section (including summary_cards and epoch_summaries)
4. Confirm: IPC channel table is present with typed payload shapes
5. Confirm: Gemini paid-plan gate is documented
6. Confirm: audiotee named as primary; Chromium loopback flags named as fallback
7. Confirm: all Zod schema names from AI-SPEC §1.6 are referenced
</verification>

<success_criteria>
- 05-ARCHITECTURE.md written and committed
- Every service module in src/main/<domain>/ has a named TypeScript interface
- SessionManager FSM is complete: 6 states, 7 transitions, consent gate as hard precondition
- IPC channel surface: 6 inbound + 10 outbound channels, all with typed payloads
- DB schema: 7 tables (5 from RSCH-05 + 2 AI-SPEC additions), all with DDL
- Electron overlay config is fully specified
- React component tree covers all 6 top-level components
- Gemini free-tier disqualification documented
- No architecture decisions introduced that contradict prior phases
</success_criteria>

<output>
Create `.planning/phases/05-prd-finalization/05-02-SUMMARY.md` when done
</output>
