---
phase: 01-dna-deep-dive-project-setup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md
autonomous: true
requirements: [DNA-04]
must_haves:
  truths:
    - "The DNA's real audio-capture approach is written down: Chromium ScreenCaptureKit loopback via desktopCapturer + the MacLoopbackAudioForScreenShare flag, with channels kept separate end-to-end"
    - "The effective minimum macOS version is assessed and recorded as 12.0 (from the shipped binary Info.plist), framed as RSCH-04's starting hypothesis not a final guarantee"
  artifacts:
    - .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md
  key_links:
    - "The assessment is explicitly handed off to Phase 3 RSCH-04 as input to the capture spike and the supported-OS floor decision"
    - "The macOS 12.0 floor is attributed to the shipped binary LSMinimumSystemVersion, not asserted as a real-world reliability guarantee"
---

<objective>
Produce the DNA audio-capture assessment (DNA-04) — a focused, single-topic deep-dive on the DNA's REAL system-audio capture method and its effective minimum macOS version, written as input to the Phase 3 RSCH-04 capture spike and the supported-OS floor decision.

Purpose: Make ROADMAP Phase 1 success criterion #4 TRUE — the DNA's real audio-capture approach and effective minimum macOS version are assessed and written down for RSCH-04 and the supported-OS floor.
Output: One Markdown doc inside the phase folder.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md
@.planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md
@.planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md
</context>

## Artifacts this phase produces

This phase creates the following NEW files (exclude from source-grounding drift verification):
- `.planning/phases/01-dna-deep-dive-project-setup/01-SETUP-BASELINE.md` (plan 01)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DEV-BASELINE.md` (plan 01)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-CATALOGUE.md` (plan 02)
- `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md`

<tasks>

<task type="auto">
  <name>Task 1: Write the DNA audio-capture assessment (DNA-04)</name>
  <files>.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md</files>
  <read_first>
    - .planning/phases/01-dna-deep-dive-project-setup/01-RESEARCH.md (§"DNA-04 Audio-Capture Assessment" lines 147-168 — the complete method, flag, channel handling, permission model, and the macOS-floor evidence table; §"Open Questions" #2 lines 296-299 — the real-floor caveat for RSCH-04)
    - .planning/phases/01-dna-deep-dive-project-setup/01-PATTERNS.md (§"01-DNA-AUDIO-ASSESSMENT.md" Pattern Assignment lines 140-154 — analog is .planning/research/PITFALLS.md focused-single-topic voice + STACK.md §macOS audio table shape; the facts to record + RSCH-04 handoff)
    - .planning/phases/01-dna-deep-dive-project-setup/01-CONTEXT.md (D-01 separate citable doc; the audio assessment feeds RSCH-04)
    - .claude/CLAUDE.md (§"macOS System-Audio Capture" — the electron-audio-loopback vs AudioTee.js framing the assessment compares the DNA's real approach against; AudioTee premium path needs 14.2+)
    - DNA/src/renderer/audio.js (verify the real capture: getUserMedia with chromeMediaSource:'desktop', video tracks discarded — lines 11-37)
    - DNA/src/main.js (verify the get-desktop-source-id IPC at 202-207 and the permission failure path at 209-223)
    - DNA/package.json (verify the --enable-features=MacLoopbackAudioForScreenShare flag on the Electron launch line — lines 8,11)
  </read_first>
  <action>
    Create `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md` — the focused assessment of the DNA's real audio-capture method and effective minimum macOS version. This satisfies DNA-04. Analog to follow: `.planning/research/PITFALLS.md` (focused "here is the real method + the handoff" voice) layered with `STACK.md` §"macOS System-Audio Capture" table shape. Use the house-style header block from 01-PATTERNS.md §"Shared Document Conventions".

    Record these facts (verify each against the DNA source you read; backbone is RESEARCH §"DNA-04 Audio-Capture Assessment"):
    - Real capture method: Chromium **ScreenCaptureKit loopback** via Electron's `desktopCapturer`. The renderer obtains a screen source id (`get-desktop-source-id` IPC → `desktopCapturer.getSources({types:['screen']})`, cite `DNA/src/main.js:202-207`) and calls `getUserMedia` with `mandatory:{ chromeMediaSource:'desktop', chromeMediaSourceId }` for BOTH audio and video, then DISCARDS the video tracks and keeps only audio (cite `DNA/src/renderer/audio.js:11-37`). This is the post-mixer loopback path (the `electron-audio-loopback`-equivalent in CLAUDE.md), hand-rolled rather than via the npm package.
    - The mandatory enabling flag: capture silently fails (no error) without the Chromium flag `--enable-features=MacLoopbackAudioForScreenShare`, passed on the Electron launch line (cite `DNA/package.json:8,11`).
    - Channel handling: mic and system audio stay SEPARATE end-to-end (two streams → two worklets → two IPC channels → two Deepgram sockets). No diarization; "You vs Other" labeling is purely which physical channel the audio came from. Note this is the cheap, reliable baseline RSCH-02 should build "You vs Others" on.
    - Permission model as shipped: triggers the macOS **Screen Recording** permission (the purple indicator path); on failure the renderer requests permission → main shows a dialog deep-linking to Privacy_ScreenCapture (cite `DNA/src/main.js:209-223`).
    - Effective minimum macOS version: reproduce the evidence table from RESEARCH lines 159-166 — shipped binary `Info.plist` `LSMinimumSystemVersion` = **12.0** (HIGH confidence, read from the actual built .app); Electron 40.6.1 baseline; README claims "macOS only" with no numeric floor. Declare the effective floor = **macOS 12.0 (Monterey)** as stamped in the shipped binary.
    - Entitlements as shipped: `build/entitlements.mac.plist` has only cs.allow-jit, cs.allow-unsigned-executable-memory, device.audio-input; the built Info.plist carries NSMicrophoneUsageDescription / NSAudioCaptureUsageDescription / NSCameraUsageDescription but NO NSScreenCaptureUsageDescription and no disable-library-validation (the DNA bundles no Swift binary; the AudioTee path would add that entitlement).

    CRITICAL framing (RSCH-04 handoff): state explicitly that 12.0 is the PACKAGING floor / starting hypothesis ONLY — not a real-world reliability guarantee. The loopback feature's reliability differs by OS (materially better on 13.x+), and CLAUDE.md's premium AudioTee path needs 14.2+. The DNA's 1:1 interview use never stress-tested multi-speaker full-meeting capture. Hand the validation to Phase 3 RSCH-04: the spike must validate the loopback path across 12 / 13 / 14 / 15 under multi-speaker load and DECLARE the supported floor, plus a silent-audio / capture-health detection approach. Name RSCH-04 explicitly as the consumer.

    Cite the requirement ID this doc satisfies (DNA-04). Cite `DNA/src/...` paths only. The doc is prose + the evidence table; no large code excerpt required.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md && grep -q 'DNA-04' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md && grep -q 'MacLoopbackAudioForScreenShare' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md && grep -qi 'desktopCapturer' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md && grep -q '12.0' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md && grep -q 'RSCH-04' .planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-dna-deep-dive-project-setup/01-DNA-AUDIO-ASSESSMENT.md` exists.
    - Doc states the real capture method: Chromium ScreenCaptureKit loopback via `desktopCapturer` + `getUserMedia({chromeMediaSource:'desktop'})` with video tracks discarded, citing `DNA/src/renderer/audio.js:11-37` and `DNA/src/main.js:202-207`.
    - Doc names the mandatory `--enable-features=MacLoopbackAudioForScreenShare` flag and notes capture silently fails without it, citing `DNA/package.json`.
    - Doc records that mic and system audio stay separate end-to-end (no diarization; channel = physical source).
    - Doc records the Screen Recording (purple indicator) permission model and the failure/deep-link path.
    - Doc declares the effective macOS floor = 12.0, attributed to the shipped binary `Info.plist` `LSMinimumSystemVersion`, and frames it as RSCH-04's starting hypothesis, not a real-world guarantee.
    - Doc explicitly hands the cross-OS (12/13/14/15) multi-speaker validation and silent-audio/capture-health detection to Phase 3 RSCH-04, naming RSCH-04.
    - Doc cites the requirement ID DNA-04 and uses only `DNA/src/...` citation paths.
  </acceptance_criteria>
  <done>The audio-capture assessment exists, records the real loopback method + mandatory flag + separate-channel handling + permission model, declares the 12.0 packaging floor framed as a starting hypothesis, hands cross-OS validation + capture-health detection to RSCH-04, and cites DNA-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces Markdown documentation only. No product code, no runtime, no external service, no package installs. No attack surface introduced this phase (01-RESEARCH.md §"Security Domain").

| Boundary | Description |
|----------|-------------|
| (none this phase) | No untrusted input crosses any boundary — docs-only deliverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-AUD1 | Information disclosure | Capture-permission posture as a downstream input | low | mitigate | The assessment records that the DNA's loopback path triggers the visible macOS Screen Recording (purple) indicator — a disclosure-positive property — and hands the permission/consent implications to RSCH-04 and Phase 2 DEC-01 rather than recommending any covert path |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH §"Package Legitimacy Audit": zero packages). No [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
Phase-level checks for this plan (manual `doc-check` per 01-VALIDATION.md — docs-only phase, no test framework):
- Assessment file exists in the phase folder.
- Spot-check 2-3 `file:line` citations resolve to the claimed mechanism (e.g. `DNA/src/renderer/audio.js:11-37` chromeMediaSource:'desktop' + discard video; `DNA/package.json` MacLoopbackAudioForScreenShare flag).
- Confirm the 12.0 floor is attributed to the shipped binary Info.plist and framed as RSCH-04's starting hypothesis, not a final guarantee.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #4 is TRUE: the DNA's real audio-capture approach and effective minimum macOS version are assessed and written down as input to the RSCH-04 spike and the supported-OS floor.
- Requirement DNA-04 is covered with `doc-check` verification and resolvable code-level evidence.
</success_criteria>

<output>
Create `.planning/phases/01-dna-deep-dive-project-setup/01-03-SUMMARY.md` when done.
</output>
