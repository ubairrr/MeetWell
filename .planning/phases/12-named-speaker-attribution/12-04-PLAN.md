---
phase: 12
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/renderer/src/components/RenameSpeakersModal.tsx
  - src/renderer/src/components/ArtifactReview.tsx
autonomous: true
requirements: [SPKR-01, SPKR-02]
must_haves:
  truths:
    - "User can open a Rename Speakers modal from ArtifactReview (the Complete-state screen) and see one row per distinct speaker_label, including 'You', each with a representative transcript excerpt"
    - "User can stage edits to multiple rows and commit them in a single Save action — no per-row auto-save (D-03)"
    - "After Save succeeds, ArtifactReview's MOM/Summary/Key Points/Action Items/Citation panel re-render with the new names immediately, with no app restart (Pitfall 6)"
    - "Save is disabled until at least one row has a non-empty, changed value; Cancel discards all staged edits with no confirmation prompt"
  artifacts:
    - "src/renderer/src/components/RenameSpeakersModal.tsx (NEW)"
    - "'Rename Speakers' button + modal wiring in ArtifactReview.tsx"
  key_links:
    - "RenameSpeakersModal invokes 'get-speaker-roster' on mount and 'rename-speakers' on Save (both allowlisted in Plan 12-03)"
    - "ArtifactReview lifts the artifacts prop into local state (useState(artifacts)) so the rename IPC response can replace it directly — the first place in this codebase where an IPC response, not a Set/boolean toggle, drives a content re-render"
---

## Artifacts This Phase Produces (this plan's share)

- `src/renderer/src/components/RenameSpeakersModal.tsx` — new component: roster fetch on mount, staged local edits, batch Save/Cancel, loading/empty/saving/error states
- `ArtifactReview.tsx`: new `localArtifacts` state (lifted from the `artifacts` prop), new `showRenameModal` state, a "Rename Speakers" footer button, and the `onRenamed` handler that replaces `localArtifacts` with the IPC response

<objective>
Build the user-facing rename flow: a dedicated modal (per CONTEXT.md D-01/D-02/D-03/D-08) that lists every distinct speaker label with a representative excerpt, lets the user stage renames across multiple rows, and commits them in one Save call — then wire ArtifactReview to replace its displayed content with the response, since no other reload path exists in this app.

Purpose: This is the only user-visible surface of Phase 12 — SPKR-01 ("user can rename a speaker label") and the observable half of SPKR-02 ("renamed attribution appears across MOM/summary/key points/action items/citations for that meeting") are proven here, not in the backend alone.
Output: `RenameSpeakersModal.tsx` and the `ArtifactReview.tsx` wiring that makes a completed rename visible without restarting the app.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-named-speaker-attribution/12-CONTEXT.md
@.planning/phases/12-named-speaker-attribution/12-RESEARCH.md
@.planning/phases/12-named-speaker-attribution/12-PATTERNS.md
@.planning/phases/12-named-speaker-attribution/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RenameSpeakersModal component</name>
  <files>src/renderer/src/components/RenameSpeakersModal.tsx</files>
  <read_first>
  src/renderer/src/components/ArtifactItem.tsx (isEditing / textarea / Save-Cancel button-pair styling to adapt for a single-line input)
  src/renderer/src/components/CitationPanel.tsx (bordered sub-panel + italic quote styling to reuse for the excerpt line under each row)
  src/renderer/src/components/ArtifactReview.tsx (the meeting-title staged-value + invoke-on-save flow at lines ~49-84, the closest existing "stage locally, commit on save" precedent in this exact screen)
  .planning/phases/12-named-speaker-attribution/12-UI-SPEC.md (full token/copy contract — spacing scale 4/8/16/24, typography 12px/11px/14px, color tokens including the accent/destructive reservations, and every locked copy string)
  </read_first>
  <action>
  Create src/renderer/src/components/RenameSpeakersModal.tsx exporting `RenameSpeakersModal({ meetingId, onClose, onRenamed }: { meetingId: string; onClose: () => void; onRenamed: (updated: Record&lt;string, unknown&gt;) => void })`. On mount (useEffect), invoke `window.electronAPI.invoke('get-speaker-roster', { meetingId })`; on a response without an `error` key, store the roster array (`{ label: string; excerpt: string | null; currentName: string | null }[]`) in state and seed a `staged: Record<string,string>` state from `Object.fromEntries(roster.map(r => [r.label, r.currentName ?? '']))`; on an `{ error }` response or thrown rejection, store the error string in a `loadError` state instead and render nothing else.

  Render one row per roster entry: read-only label text (e.g. "Speaker 1"), the excerpt rendered in a CitationPanel-style bordered block (`background: 'rgba(255,255,255,0.05)'`, `borderLeft: '2px solid #4b5563'`, `fontStyle: 'italic'`, `color: '#d1d5db'`), falling back to the literal copy `"(no transcript excerpt available)"` when `excerpt` is null, and a single-line `<input>` (not a textarea — per UI-SPEC, names are single-line) bound to `staged[label]` with placeholder `"Enter name…"`. Render the empty-roster state (heading `"No speakers to rename"`, body `"This meeting has no transcript segments yet, so there's nothing to relabel."`) when `roster` is an empty array, with no Save button rendered in that case.

  Derive `changedCount` (rows where `staged[label].trim()` is non-empty AND differs from `roster.find(r => r.label === label)?.currentName ?? ''`). Enable the "Save Names" button (`background: '#2563eb'`, `color: '#fff'`) only when `changedCount > 0`; otherwise render it disabled (`background: '#1f2937'`, `color: '#4b5563'`), matching ArtifactReview's title-save button's exact disabled styling. On Save: build `mapping` from only the changed entries, set `isSaving` true, invoke `window.electronAPI.invoke('rename-speakers', { meetingId, mapping })`, show the busy label `"Saving..."` on the Save button while pending (button disabled during this window so a double-click cannot fire a second invoke). On success (response has no `error` key), call `onRenamed(response)` then `onClose()`. On failure (`{ error }` response or thrown rejection): if the `error` string is the session-gate rejection, show `"Renaming is only available after the meeting ends."`; otherwise show `"Couldn't save these names. Your changes weren't lost — try Save again."`; render this in an inline banner using the destructive token (`border: '1px solid #dc2626'`, `background: 'rgba(239,68,68,0.15)'`, `color: '#fca5a5'`, matching ArtifactReview's existing error-banner styling) and keep the modal open with all staged edits intact. "Cancel" always calls `onClose()` directly with no confirmation, discarding all staged edits.

  Hold this component's own layout to the strict 4/8/16/24 spacing scale (row padding, gaps, modal outer padding) — do not introduce 2px/6px/10px off-scale values even though the surrounding overlay uses them elsewhere. Use 12px/400/1.5 body text and 11px/600/1.4 for the roster label text, matching the UI-SPEC Typography table. Constrain the modal's own content width to roughly 348px (380px overlay minus the 16px md padding token on each side), per UI-SPEC's layout constraint.
  </action>
  <acceptance_criteria>
  - RenameSpeakersModal.tsx exports a component accepting `{ meetingId, onClose, onRenamed }` props
  - The component invokes `'get-speaker-roster'` on mount and `'rename-speakers'` on Save (both string literals appear as arguments to `window.electronAPI.invoke`)
  - The literal copy strings `"Save Names"`, `"Cancel"`, `"Enter name…"`, `"(no transcript excerpt available)"`, `"No speakers to rename"`, `"Couldn't save these names. Your changes weren't lost — try Save again."`, and `"Renaming is only available after the meeting ends."` all appear verbatim in the file
  - No `2px`/`6px`/`10px` literal spacing values appear in this new file's own `style={{...}}` blocks
  - `test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS')" = "1"` (this project's renderer-side pre-existing baseline as of Phase 12 planning is unchanged)
  </acceptance_criteria>
  <verify>
    <automated>test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS')" = "1" && grep -q "Save Names" src/renderer/src/components/RenameSpeakersModal.tsx && grep -q "get-speaker-roster" src/renderer/src/components/RenameSpeakersModal.tsx && grep -q "rename-speakers" src/renderer/src/components/RenameSpeakersModal.tsx</automated>
  </verify>
  <done>RenameSpeakersModal renders the roster with excerpts, stages edits locally, and commits via one batch rename-speakers call, matching every UI-SPEC token and copy string exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Rename Speakers into ArtifactReview</name>
  <files>src/renderer/src/components/ArtifactReview.tsx</files>
  <read_first>
  src/renderer/src/components/ArtifactReview.tsx (full current file — props/state declarations at top, the error-state early return, and the footer button block at the bottom)
  src/renderer/src/components/RenameSpeakersModal.tsx (Task 1 of this plan — the `{ meetingId, onClose, onRenamed }` props contract)
  </read_first>
  <action>
  In ArtifactReview.tsx, add `const [localArtifacts, setLocalArtifacts] = useState(artifacts)` immediately after the existing useState declarations. Replace every subsequent render-body reference to the `artifacts` prop (`artifacts.error`, `artifacts.errorMessage`, `artifacts.actionItems.action_items`, `artifacts.summary.summary_text`, `artifacts.keyPoints.key_points`, `artifacts.mom.markdown_content`) with the equivalent `localArtifacts.` reference — the component now renders exclusively from local state, seeded once from the prop on mount.

  Add `const [showRenameModal, setShowRenameModal] = useState(false)`. Add a `"Rename Speakers"` button to the footer button group (alongside the existing "Start New Meeting"/"Dismiss" buttons, not inside the error-state early-return branch), styled with the accent token (`background: '#2563eb'`, `color: '#fff'`) per UI-SPEC's explicit accent reservation for this trigger button; clicking it calls `setShowRenameModal(true)`. When `showRenameModal` is true, render `<RenameSpeakersModal meetingId={meetingId} onClose={() => setShowRenameModal(false)} onRenamed={(updated) => { setLocalArtifacts(updated); setShowRenameModal(false) }} />` as an overlay positioned to cover the existing 380px-wide content column, per UI-SPEC's layout constraint. Import `RenameSpeakersModal` from `./RenameSpeakersModal`.
  </action>
  <acceptance_criteria>
  - ArtifactReview.tsx declares `localArtifacts` local state seeded from the `artifacts` prop
  - Zero occurrences of `artifacts.actionItems`/`artifacts.summary`/`artifacts.keyPoints`/`artifacts.mom` remain in the render body outside the prop destructuring and the `useState(artifacts)` seed line (all replaced with `localArtifacts.`)
  - A `"Rename Speakers"` button is present and conditionally renders `<RenameSpeakersModal .../>` based on `showRenameModal`
  - The `onRenamed` handler calls `setLocalArtifacts` before calling `setShowRenameModal(false)`
  - `test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS')" = "1"` (unchanged renderer-side baseline)
  </acceptance_criteria>
  <verify>
    <automated>test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS')" = "1" && grep -q "Rename Speakers" src/renderer/src/components/ArtifactReview.tsx && grep -q "setLocalArtifacts" src/renderer/src/components/ArtifactReview.tsx</automated>
    <human-check>Open ArtifactReview for a Complete-state meeting with 2+ distinct speaker labels. Click "Rename Speakers". Verify the modal lists every distinct speaker_label (including "You") with a representative excerpt. Rename 2+ speakers, click "Save Names". Verify the modal closes and MOM/Summary/Key Points/Action Items/Citation panel immediately show the new names with no restart. Re-open the modal and rename one of the same speakers again to a third name — verify the second rename also takes effect (idempotent re-rename, per RESEARCH.md Pitfall 3). Confirm exporting to .ics afterward shows the renamed owner (SPKR-03). Confirm a different meeting's speaker labels are unaffected (SPKR-05).</human-check>
  </verify>
  <done>"Rename Speakers" button opens the modal; a successful save replaces localArtifacts with the IPC response so every section re-renders with the new names immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-typed display name (renderer form input) -> React DOM render | Standard React trust boundary — text interpolated into JSX |
| Renderer -> Main via allowlisted IPC | Enforced by preload's INVOKE_CHANNELS allowlist (Plan 12-03); this plan only calls channels already added there |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Information Disclosure | RenameSpeakersModal.tsx / ArtifactReview.tsx | low | accept | Renamed display name values are rendered exclusively via JSX text interpolation (no `dangerouslySetInnerHTML` anywhere in either file) — React's default escaping neutralizes standard injection; no additional mitigation code needed |
| T-12-10 | Tampering (double-submit) | RenameSpeakersModal.tsx Save button | low | mitigate | Save button is disabled while `isSaving` is true, preventing a rapid double-click from firing a duplicate `rename-speakers` invoke |
| T-12-11 | Elevation of Privilege | ArtifactReview.tsx "Rename Speakers" button visibility | low | mitigate | The button only renders inside the `Complete`-state screen (App.tsx only mounts ArtifactReview when `sessionState === 'Complete'`) — this is defense-in-depth UI convenience only; the actual authorization boundary is the server-side `session.getState() === 'Complete'` gate enforced in Plan 12-03's IPC handlers, per CLAUDE.md's "consent gate enforced in main process, not just UI" convention |
</threat_model>

<verification>
`npm test` remains green (no main-process files touched by this plan). Renderer components have zero automated test coverage in this project's convention (vitest.config.ts explicitly excludes `src/renderer/**`), so the TypeScript-baseline checks above plus the end-of-phase manual UAT (per `human_verify_mode: end-of-phase` — see 12-VALIDATION.md's Manual-Only Verifications table) are this plan's verification path.
</verification>

<success_criteria>
A user can rename every distinct speaker in a completed meeting via one modal and one Save action, and sees every artifact section (MOM, summary, key points, action items, citations) immediately reflect the new names without restarting the app.
</success_criteria>

<output>
Create `.planning/phases/12-named-speaker-attribution/12-04-SUMMARY.md` when done
</output>
