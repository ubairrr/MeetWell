---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/calendar/__tests__/CalendarExportService.test.ts
autonomous: true
requirements: [SPKR-03]
must_haves:
  truths:
    - "CalendarExportService's exported .ics description reflects whatever value is currently stored in action_items.assignee_label — including a renamed display name — with no separate export-time resolution step"
    - "The pre-existing 'Owner: You' fallback (null assignee_label) and skipped-item counting behavior (non-calendar-event items, unparseable due_date) continue to work exactly as before"
  artifacts:
    - "src/main/calendar/__tests__/CalendarExportService.test.ts — first-ever automated coverage for this service"
  key_links:
    - "CalendarExportService.export() reads action_items.assignee_label directly via ArtifactStore.getConfirmedActionItems() — the same column SpeakerAliasStore.applyRenames (Plan 12-01) mutates in place — so SPKR-03 is satisfied by column reuse, not by new export code"
---

## Artifacts This Phase Produces (this plan's share)

- `src/main/calendar/__tests__/CalendarExportService.test.ts` — the first automated test file for `CalendarExportService`, closing a pre-existing regression gap and locking in the SPKR-03 guarantee (renamed `assignee_label` flows straight into the exported `.ics` description)

<objective>
Add first-ever automated regression coverage for `CalendarExportService`, proving that its `.ics` export already satisfies SPKR-03 (renamed speaker attribution appears in exported calendar events) purely because it reads `action_items.assignee_label` directly — the exact column Plan 12-01's `SpeakerAliasStore.applyRenames` mutates in place. No production code changes are made or needed in this plan; per 12-PATTERNS.md's analysis, `CalendarExportService.ts` requires zero modification.

Purpose: RESEARCH.md flags that this service has zero test coverage today — a silent regression here (e.g. someone later "optimizing" the export to resolve names via a join instead of reading the column directly) would break SPKR-03 without any test catching it. This plan closes that gap.
Output: `src/main/calendar/__tests__/CalendarExportService.test.ts` with passing regression assertions.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-named-speaker-attribution/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarExportService regression test suite</name>
  <files>src/main/calendar/__tests__/CalendarExportService.test.ts</files>
  <read_first>
  src/main/calendar/CalendarExportService.ts (full file — confirms `description: item.assignee_label ? \`Owner: ${item.assignee_label}\` : 'Owner: You'` reads the column directly, and the skippedCount/stampIcsExported logic to regression-test)
  src/main/store/ArtifactStore.ts (getConfirmedActionItems / stampIcsExported — the exact row shape and methods this test seeds against and calls)
  tests/unit/TranscriptStore.test.ts (the ':memory:' + direct ALL_DDLS import setup to reuse for the DB portion of this test)
  tests/db.test.ts (the `vi.mock('electron', ...)` shape to reuse for mocking `dialog.showSaveDialog` / `app.getPath`, since CalendarExportService.ts imports `dialog`/`app` from 'electron' at module load time)
  </read_first>
  <action>
  Create src/main/calendar/__tests__/CalendarExportService.test.ts. Mock the `electron` module (same shape as tests/db.test.ts's `vi.mock('electron', ...)`): `dialog.showSaveDialog` resolves `{ canceled: false, filePath: '/tmp/mock-meeting-actions.ics' }`; `app.getPath` returns `/tmp`. Mock `fs`'s `writeFileSync` via `vi.mock('fs', async (importOriginal) => ({ ...(await importOriginal()), writeFileSync: vi.fn() }))` so no real file I/O occurs, and capture the ICS content string passed as the mock's second call argument for assertions. Build a `:memory:` `Database` using `ALL_DDLS` imported directly from `../../store/db` (same proven pattern as tests/unit/TranscriptStore.test.ts — no SQLCipher pragma key, no temp file) and instantiate a real `ArtifactStore(db)`. Seed a `meetings` row and `action_items` rows via raw `db.prepare(...).run(...)` inserts (status='confirmed', is_calendar_event=1, a valid `due_date` in YYYY-MM-DD format unless the case specifically tests a missing/invalid date) for these cases: (a) `assignee_label = 'Jane Doe'` — simulates a post-rename value already written by Plan 12-01's SpeakerAliasStore.applyRenames (this test does not call SpeakerAliasStore itself; it directly seeds the already-mutated column to test CalendarExportService in isolation); (b) `assignee_label = null`; (c) a confirmed item with `is_calendar_event = 0`; (d) a confirmed item with `due_date = null`. Instantiate `new CalendarExportService(artifactStore)` and call `await service.export(meetingId)` for each scenario grouping needed.

  Assert: (1) case (a)'s captured ICS content string contains the literal substring `Owner: Jane Doe`; (2) case (b)'s captured ICS content string contains `Owner: You`; (3) case (c)'s item is excluded from the exported events and counted in the returned `skippedCount`; (4) case (d)'s item is excluded and counted in `skippedCount`; (5) after a successful export, `action_items.ics_exported_at` is non-null for the exported item's id (query the DB directly after calling export, since `stampIcsExported` runs as part of `export()`).
  </action>
  <acceptance_criteria>
  - src/main/calendar/__tests__/CalendarExportService.test.ts exists (first test file ever created for this service)
  - npx vitest run src/main/calendar/__tests__/CalendarExportService.test.ts passes with all 5 assertions above green
  - The test file makes zero real filesystem writes (writeFileSync is mocked) and zero real save-dialog prompts (dialog.showSaveDialog is mocked)
  - npx tsc --noEmit -p tsconfig.node.json reports exactly 8 errors (unchanged pre-existing baseline — confirms this new test file introduces no type errors)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/calendar/__tests__/CalendarExportService.test.ts && test "$(npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS')" = "8"</automated>
  </verify>
  <done>CalendarExportService has first-ever automated coverage proving a renamed assignee_label flows into the exported .ics description, alongside the pre-existing fallback/skip-counting behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Confirmed action_items DB rows -> exported .ics file on the user's filesystem | Data crosses from the encrypted DB to a plaintext file the user chooses via a native save dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering (regression risk) | src/main/calendar/CalendarExportService.ts | medium | mitigate | Add first-ever automated regression coverage asserting the exported .ics `Owner:` field reflects whatever is currently in `action_items.assignee_label` (including a renamed value) and that the pre-existing null-fallback/skip-counting behavior is unchanged — closes a silent-breakage gap that predates this phase |
</threat_model>

<verification>
`npx vitest run src/main/calendar/__tests__/CalendarExportService.test.ts` passes in isolation; `npm test` (full suite) remains green with this file added.
</verification>

<success_criteria>
CalendarExportService's SPKR-03 guarantee (renamed labels appear in .ics exports) is proven by an automated test, not just code inspection.
</success_criteria>

<output>
Create `.planning/phases/12-named-speaker-attribution/12-02-SUMMARY.md` when done
</output>
