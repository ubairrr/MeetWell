---
phase: 10-contextengine-break-assist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/llm/EmbeddingAdapter.ts
autonomous: false
requirements:
  - CTX-02
must_haves:
  truths:
    - EmbeddingAdapter calls Gemini text-embedding via the openai SDK + baseURL pattern (same as LLMAdapter)
    - EmbeddingAdapter.embed() asserts embedding.length === 1536 before returning; throws if dimension mismatches
    - EmbeddingAdapter constructor takes (apiKey: string, baseURL?: string) so tests can inject a no-op mock without a real key
    - The Gemini embedding model and dimension parameter are confirmed to produce 1536-dim output via human probe
  artifacts:
    - src/main/llm/EmbeddingAdapter.ts (new file)
  key_links:
    - EpochCompressor (10-03) imports EmbeddingAdapter from src/main/llm/EmbeddingAdapter
    - ContextEngine (10-04) instantiates EmbeddingAdapter and injects it into EpochCompressor
    - 60-minute test (10-06) mocks EmbeddingAdapter via constructor injection (no real API call in tests)
user_setup:
  - service: gemini
    why: "Confirm Gemini text-embedding model + dimension parameter via the openai SDK + baseURL adapter before writing EmbeddingAdapter"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Already stored in safeStorage from Phase 8 — retrieve from electron-store or paste directly into the probe script"
    dashboard_config: []
---

<objective>
Implement EmbeddingAdapter — the single-responsibility module that converts text into a 1536-dimensional Float32Array via Gemini text-embedding — after a human-verified probe confirms the dimension parameter works through the openai SDK baseURL adapter.

Purpose: The Gemini embedding model + dimension parameter combination is LOW confidence (RESEARCH.md finding) — it must be verified empirically before implementation commits to a call shape. This plan uses a blocking checkpoint to confirm before the code is written.

Output: src/main/llm/EmbeddingAdapter.ts with confirmed call shape and dimension assertion guard (per D-05, D-06).
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ubair/Gits/MeetingAssist/.planning/PROJECT.md
@/Users/ubair/Gits/MeetingAssist/.planning/ROADMAP.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-CONTEXT.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-RESEARCH.md
@/Users/ubair/Gits/MeetingAssist/src/main/llm/LLMAdapter.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify Gemini text-embedding produces 1536-dim output via openai SDK adapter (D-05)</name>
  <what-built>
Claude will prepare a Node.js probe script to test Gemini text-embedding dimensions through the openai SDK + baseURL adapter. The probe must be run with a real GEMINI_API_KEY before EmbeddingAdapter.ts is implemented.
  </what-built>
  <how-to-verify>
Run the following probe in a terminal (requires GEMINI_API_KEY set or pasted inline):

```
GEMINI_API_KEY="your-key-here" node --input-type=module &lt;&lt;'EOF'
import OpenAI from 'openai'
const client = new OpenAI({
  apiKey: process.env.GEMINI_API_KEY,
  baseURL: 'https://generativelanguage.googleapis.com/v1beta/openai'
})

// Attempt 1: dimensions parameter (OpenAI native)
try {
  const r1 = await client.embeddings.create({
    model: 'text-embedding-004',
    input: 'probe sentence for dimension test',
    dimensions: 1536
  })
  console.log('text-embedding-004 dimensions:1536 =>', r1.data[0].embedding.length)
} catch (e) { console.error('Attempt 1 failed:', e.message) }

// Attempt 2: extra_body output_dimensionality
try {
  const r2 = await client.embeddings.create({
    model: 'text-embedding-004',
    input: 'probe sentence for dimension test',
    extra_body: { output_dimensionality: 1536 }
  })
  console.log('text-embedding-004 extra_body:1536 =>', r2.data[0].embedding.length)
} catch (e) { console.error('Attempt 2 failed:', e.message) }

// Attempt 3: gemini-embedding-001 default dims
try {
  const r3 = await client.embeddings.create({
    model: 'gemini-embedding-001',
    input: 'probe sentence for dimension test'
  })
  console.log('gemini-embedding-001 default =>', r3.data[0].embedding.length)
} catch (e) { console.error('Attempt 3 failed:', e.message) }
EOF
```

Expected: at least one attempt prints `1536`. Record which model + parameter combination produced 1536 dims.

If none produce exactly 1536, check the vec_chunks DDL — the float[1536] dimension is fixed and no schema migration is permitted (D-05).
  </how-to-verify>
  <resume-signal>Type the probe output (e.g., "text-embedding-004 dimensions:1536 => 1536") and which attempt succeeded, or describe what happened if all failed.</resume-signal>
  <files>N/A — no file written by this checkpoint</files>
  <action>Human runs the probe script above and reports which model + parameter combination produced 1536-dim output. Claude reads the resume signal before proceeding to Task 2.</action>
  <verify>Human confirms probe output shows a vector length of exactly 1536 for at least one attempt.</verify>
  <done>Probe result recorded: confirmed Gemini model name and dimension parameter that produces 1536-dim embeddings via the openai SDK + baseURL adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EmbeddingAdapter — Gemini text-embedding via openai SDK (D-05, D-06)</name>
  <files>src/main/llm/EmbeddingAdapter.ts</files>
  <behavior>
    - new EmbeddingAdapter('fake-key').embed('hello') calls client.embeddings.create with the confirmed model + parameter
    - embed() returns a Float32Array of length 1536
    - embed() throws EmbeddingDimensionError if the returned vector length is not exactly 1536
    - Constructor accepts (apiKey: string, baseURL?: string) — tests inject a mock baseURL to avoid real API calls
    - EmbeddingAdapter does NOT depend on electron-store, safeStorage, or any Electron API — it is pure Node
  </behavior>
  <action>
Create src/main/llm/EmbeddingAdapter.ts after the probe checkpoint confirms which model + parameter produces 1536 dimensions.

Use the CONFIRMED model name and parameter from the human probe (the checkpoint result specifies this). Substitute the actual model name and call shape into the implementation below.

Import OpenAI from 'openai'. The constructor signature matches LLMAdapter:
  constructor(apiKey: string, baseURL = 'https://generativelanguage.googleapis.com/v1beta/openai')

Internal client: new OpenAI({ apiKey, baseURL })

Method: async embed(text: string): Promise&lt;Float32Array&gt;
  1. Call client.embeddings.create with the confirmed model + dimension parameter (from the checkpoint result).
  2. Extract the embedding array: response.data[0].embedding (number[])
  3. Assert array.length === 1536. If not, throw new Error(`EmbeddingDimensionError: expected 1536, got ${array.length}`) — this guards against silent model changes returning different dimensions.
  4. Return new Float32Array(array)

Note per D-06: callers pass a single concatenated text string (all epoch structured fields joined); embed() does not split or chunk. The text_preview field for vec_chunks is the caller's responsibility to derive (key_points bullets).

Export EmbeddingAdapter as named export.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>EmbeddingAdapter.ts compiles; embed() asserts embedding.length === 1536; constructor accepts (apiKey, baseURL?) matching LLMAdapter pattern; no Electron imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Gemini embedding API | Text from epoch summaries is sent to an external embedding service |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-A | Information Disclosure | EmbeddingAdapter | high | mitigate | Gemini PAID plan key only (RSCH-03); free-tier would allow training on meeting content. Key is stored in safeStorage from Phase 8 — never in env files or electron-store plaintext |
| T-10-02-B | Tampering | vec_chunks INSERT | medium | mitigate | embedding.length === 1536 assertion before INSERT prevents dimension mismatch corrupting the virtual table schema |
| T-10-02-C | Tampering | npm/pip/cargo installs | high | mitigate | openai SDK already installed from Phase 8 — no new package installs required in this plan |
</threat_model>

<verification>
1. Checkpoint output confirms which Gemini model + parameter produces 1536-dim output
2. `npx tsc --noEmit` exits 0
3. `grep -c 'Float32Array' src/main/llm/EmbeddingAdapter.ts` returns at least 1
4. `grep -c '1536' src/main/llm/EmbeddingAdapter.ts` returns at least 2 (assertion + dimension parameter)
5. `grep -c 'electron' src/main/llm/EmbeddingAdapter.ts` returns 0 (no Electron imports)
</verification>

<success_criteria>
- Human probe confirms Gemini text-embedding produces exactly 1536 dimensions via openai SDK + baseURL
- EmbeddingAdapter.ts implements embed(text): Promise&lt;Float32Array&gt; using the confirmed call shape
- Dimension assertion throws on mismatch
- Constructor is injectable (apiKey + optional baseURL) for test mocking
- tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/10-contextengine-break-assist/10-02-SUMMARY.md` when done
</output>
