---
phase: 10-contextengine-break-assist
plan: 05
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - src/main/index.ts
autonomous: true
requirements:
  - CTX-03
  - CTX-04
  - CTX-05
must_haves:
  truths:
    - index.ts no longer holds a direct SummaryCardTimer instance; ContextEngine manages SummaryCardTimer internally (D-08)
    - contextEngine.start(currentMeetingId) is called on the Capturing entry when previous !== 'OnBreak' (same guard as before)
    - contextEngine.stop() is called on both the Processing entry and the Idle entry
    - breakStartMs tracking in the start-break handler is unchanged (D-11 — the in-memory timestamp is already correct)
    - end-break handler is unchanged (D-09 — cards-only digest, no LLM narrative, ContextComposer not used here)
    - EmbeddingAdapter is instantiated with the geminiApiKey already present in the file
  artifacts:
    - src/main/index.ts (modified — SummaryCardTimer replaced with ContextEngine)
  key_links:
    - ContextEngine (10-04) constructor requires (db, win, summaryCardStore, llmAdapter, embeddingAdapter) — all five are available in index.ts after this plan
    - EmbeddingAdapter (10-02) must be imported from './llm/EmbeddingAdapter'
    - SummaryCardTimer import is removed from index.ts after ContextEngine takes over its lifecycle
---

<objective>
Wire ContextEngine into src/main/index.ts, replacing the direct SummaryCardTimer instance so the full context engine stack (SummaryCardTimer + TokenMonitor + EpochCompressor) is active during every meeting session.

Purpose: Phase 10's context infrastructure is inert until index.ts calls contextEngine.start/stop at the correct FSM transitions. This plan makes the ContextEngine live in the running application. Break Assist (CTX-04, CTX-05) is already complete in index.ts — this plan verifies the wiring does not regress it.

Output: src/main/index.ts with ContextEngine replacing the direct SummaryCardTimer reference; all existing FSM handlers and IPC channels remain unchanged.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ubair/Gits/MeetingAssist/.planning/PROJECT.md
@/Users/ubair/Gits/MeetingAssist/.planning/ROADMAP.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-CONTEXT.md
@/Users/ubair/Gits/MeetingAssist/src/main/index.ts
@/Users/ubair/Gits/MeetingAssist/.planning/phases/10-contextengine-break-assist/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SummaryCardTimer with ContextEngine in index.ts (D-08, D-09, D-11, CTX-03)</name>
  <files>src/main/index.ts</files>
  <action>
Read src/main/index.ts before editing. Apply the following five targeted changes using Edit (scoped replacement), one change at a time.

Change 1 — Replace SummaryCardTimer import with ContextEngine + EmbeddingAdapter imports:
  Remove: import { SummaryCardTimer } from './context/SummaryCardTimer'
  Add: import { ContextEngine } from './context/ContextEngine'
  Add: import { EmbeddingAdapter } from './llm/EmbeddingAdapter'
  Both lines go in the same position as the removed import.

Change 2 — Replace SummaryCardTimer instantiation with EmbeddingAdapter + ContextEngine:
  Find the line: const summaryCardTimer = new SummaryCardTimer(db!, win!, summaryCardStore, llmAdapter)
  Replace with:
    const embeddingAdapter = new EmbeddingAdapter(geminiApiKey)
    const contextEngine = new ContextEngine(db!, win!, summaryCardStore, llmAdapter, embeddingAdapter)
  The EmbeddingAdapter is constructed with the same geminiApiKey variable already present two lines above. No new key retrieval needed.

Change 3 — Replace summaryCardTimer.start() with contextEngine.start() in the Capturing entry guard:
  Find: summaryCardTimer.start(currentMeetingId)
  Replace: contextEngine.start(currentMeetingId)
  This is inside the `if (state === 'Capturing' && previous !== 'OnBreak')` block. The previous !== 'OnBreak' guard is preserved exactly as-is.

Change 4 — Replace summaryCardTimer.stop() in Processing entry:
  Find the summaryCardTimer.stop() call inside `if (state === 'Processing')` block.
  Replace: contextEngine.stop()
  The stop() call must remain the first statement in that block (before the meetingId capture and pipeline launch), same as the original.

Change 5 — Replace summaryCardTimer.stop() in Idle entry:
  Find the summaryCardTimer.stop() call inside `if (state === 'Idle')` block.
  Replace: contextEngine.stop()

Do NOT change any of the following (they are already correct and must stay untouched):
  - breakStartMs variable declaration and the start-break handler (lines 233–241, confirmed CTX-05 complete per D-11)
  - end-break handler (lines 243–261, confirmed CTX-04 complete per D-09/D-10)
  - All other IPC handlers
  - Mouse event control block
  - ArtifactPipeline launch in Processing handler
  - captureService.startCapture / stopCapture calls
  - currentMeetingId assignment
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>tsc --noEmit exits 0; index.ts has no SummaryCardTimer import or instance; contextEngine.start(currentMeetingId) is called in the Capturing entry; contextEngine.stop() is called in Processing and Idle entries; breakStartMs tracking and end-break handler are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSM state change → ContextEngine | Session lifecycle events trigger context subsystem start/stop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-A | Tampering | ContextEngine.start() during OnBreak | low | mitigate | previous !== 'OnBreak' guard preserved from original summaryCardTimer usage — ContextEngine does not restart on break resume, preserving meeting continuity (CTX-05 invariant) |
| T-10-05-B | Denial of Service | Double-start protection | low | mitigate | ContextEngine.start() is idempotent (stops previous session first) — even if FSM fires duplicate Capturing entries, context state does not fork |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `grep -c 'SummaryCardTimer' src/main/index.ts` returns 0 (no remaining import or instance)
3. `grep -c 'ContextEngine' src/main/index.ts` returns at least 2 (import + instantiation)
4. `grep -c 'contextEngine.start' src/main/index.ts` returns at least 1
5. `grep -c 'contextEngine.stop' src/main/index.ts` returns at least 2 (Processing + Idle)
6. `grep -c 'breakStartMs' src/main/index.ts` returns at least 2 (declaration + assignment in start-break handler + reset in end-break handler)
7. `grep -c 'EmbeddingAdapter' src/main/index.ts` returns at least 2 (import + instantiation)
</verification>

<success_criteria>
- index.ts compiles (tsc --noEmit exits 0)
- No SummaryCardTimer import or instance in index.ts
- ContextEngine wired at Capturing entry (with previous !== 'OnBreak' guard) and stopped at Processing + Idle
- Break assist handlers (start-break, end-break) unchanged from their Phase 9 implementation
- EmbeddingAdapter instantiated with geminiApiKey before ContextEngine construction
</success_criteria>

<output>
Create `.planning/phases/10-contextengine-break-assist/10-05-SUMMARY.md` when done
</output>
