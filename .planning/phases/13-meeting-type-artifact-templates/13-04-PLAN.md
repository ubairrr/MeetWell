---
phase: 13-meeting-type-artifact-templates
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/main/pipeline/ArtifactPipeline.ts
  - tests/unit/ArtifactPipeline.test.ts
autonomous: true
requirements: [TMPL-03, TMPL-04, TMPL-05]
must_haves:
  truths:
    - "A Standup/1:1/Planning meeting's generated MOM shows genuinely different section headings than a General meeting's — not just relabeled fields (D-02/D-03/D-04, SC-3/SC-4)"
    - "The Stage 1 verbatim-quote extraction prompt is byte-for-byte identical regardless of the meeting's type (D-06/TMPL-05)"
    - "The Summary, Key Points, and Action Items generation prompts are byte-for-byte identical regardless of the meeting's type (D-05) — only the MOM varies"
    - "The MOM artifact's meeting_type field always matches the meeting's actual stored meeting_type, including in the empty-transcript and no-anchors early-return paths, and is stamped programmatically rather than relying on the LLM to echo it back (D-08)"
  artifacts:
    - "src/main/pipeline/ArtifactPipeline.ts — exported `MOM_SECTION_SPECS` lookup (one markdown section block per MeetingType), exported `MoMGenerationSchema` (the LLM-facing schema that does NOT include meeting_type), new private `getMeetingType()`, `runStage2Mom()` extended with a `meetingType` parameter"
  key_links:
    - "run() calls getMeetingType() to read the meetings.meeting_type column (Plan 13-01) and threads it into runStage2Mom() only — runStage2Summary/runStage2KeyPoints/runStage2ActionItems calls are byte-for-byte unchanged (D-05)"
    - "runStage2Mom() interpolates MOM_SECTION_SPECS[meetingType] into the shared prompt skeleton, then stamps meeting_type onto the LLM's response programmatically rather than including it in the schema sent to the LLM (D-08)"
---

## Artifacts This Phase Produces (this plan's share)

- `MOM_SECTION_SPECS: Record<MeetingType, string>` — exported module-level lookup, one markdown section-block per meeting type, the concrete implementation of D-03's structural distinction
- `MoMGenerationSchema` — exported module-level zod schema (`{ markdown_content: string }`, no `meeting_type`) used only for the LLM call inside `runStage2Mom`, so the LLM is never relied upon to echo back a value the system already knows
- `ArtifactPipeline.getMeetingType()` — new private method, mirrors `getMeetingDate()`'s exact shape
- `ArtifactPipeline.runStage2Mom(anchors, meetingDate, meetingType)` — extended signature (3rd parameter)
- `run()`'s `mom.meeting_type` field, populated on every return path (normal generation, empty-transcript, empty-anchors, top-level error fallback)

<objective>
Make `runStage2Mom` genuinely vary its OUTPUT FORMAT section list by meeting type (D-02/D-03/D-04), while `runStage1`, `runStage2Summary`, `runStage2KeyPoints`, and `runStage2ActionItems` remain completely untouched (D-05/D-06) — and stamp the meeting's `meeting_type` onto every returned `MoM` object, including the empty/error fallback paths, so `content_json` always carries it (D-08).

Purpose: This is the plan that actually satisfies TMPL-03/TMPL-04's "distinctly structured artifact" bar and TMPL-05's "Stage 1 stays template-agnostic" bar — the storage (13-01) and propagation (13-02/13-03) plans only get the meeting_type value to this point; this plan is where it changes what gets generated.
Output: A type-conditional `runStage2Mom` proven, via direct behavioral tests, to (a) produce genuinely different section headings per type and (b) leave every other Stage 1/Stage 2 prompt byte-identical regardless of type.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-meeting-type-artifact-templates/13-CONTEXT.md
@.planning/phases/13-meeting-type-artifact-templates/13-PATTERNS.md
@.planning/phases/13-meeting-type-artifact-templates/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type-conditional runStage2Mom + meeting_type stamping</name>
  <files>src/main/pipeline/ArtifactPipeline.ts</files>
  <read_first>
  src/main/pipeline/ArtifactPipeline.ts (full file — `getMeetingDate()` at lines 66-70 as the exact shape to mirror for `getMeetingType()`; `runStage2Mom()`'s current full prompt at lines 111-152, especially the OUTPUT FORMAT block from `# Minutes of Meeting` through `## Next Steps`; the sibling `runStage2Summary`/`runStage2KeyPoints`/`runStage2ActionItems` methods at lines 154-240, which must remain completely unedited; `run()` at lines 280-342, specifically the `Promise.all` block at lines 309-314 and the two early-return `empty` objects at lines 286-294 and 299-307, and the top-level catch block's fallback at lines 330-341)
  </read_first>
  <action>
  Import `type { MeetingType } from '../../shared/schemas'` alongside the existing schema imports.

  Add a module-level `export const MoMGenerationSchema = z.object({ markdown_content: z.string() })` (using the already-imported `z`), placed after the imports and before `export class ArtifactPipeline`. This is the schema actually sent to the LLM for the MOM call — it intentionally omits `meeting_type`, since the system already knows the value and stamping it programmatically after the call is safer than relying on the LLM to echo it back correctly (D-08).

  Add a module-level `export const MOM_SECTION_SPECS: Record&lt;MeetingType, string&gt;` mapping each of the 4 `MeetingType` values to a markdown section-block string — the type-conditional portion of the OUTPUT FORMAT, interpolated between `## Attendees` and `## Action Items` in the shared skeleton (D-02). Content per type:
  - `general`: today's existing 4 sections, verbatim and unchanged — `## Agenda Items Discussed` (bullet list of topics, each grounded in at least one quote), `## Key Discussion Points` (bullet list of significant discussion points, grounded in quotes, cite the speaker label), `## Decisions Made` (numbered list, "None recorded" if none), `## Next Steps` (brief paragraph synthesizing confirmed action items and follow-up dates) — copy this block's guidance text exactly from the current prompt (lines 135-149, excluding the `## Action Items` sub-block which moves to the shared skeleton).
  - `standup`: `## Yesterday` (bullet list of what each speaker reported completing since the last update, grounded in quotes, attributed by speaker label, "None recorded" if none), `## Today` (bullet list of what each speaker stated they plan to work on, grounded in quotes, attributed by speaker label, "None recorded" if none), `## Blockers` (bullet list of blockers or impediments raised, grounded in quotes, attributed by speaker label, "None recorded" if none).
  - `1:1`: `## Discussion Topics` (bullet list of topics discussed, each grounded in at least one quote), `## Feedback Themes` (bullet list of feedback given or received, grounded in quotes, attributed by speaker label, "None recorded" if none), `## Growth Notes` (bullet list of development/growth/career-related points raised, grounded in quotes, "None recorded" if none), `## Follow-ups` (bullet list of follow-up items or commitments to revisit, grounded in quotes, "None recorded" if none).
  - `planning`: `## Decisions` (numbered list of decisions made, grounded in quotes, "None recorded" if none), `## Next Steps` (bullet list of next steps or planned actions, grounded in quotes, "None recorded" if none), `## Open Questions` (bullet list of unresolved questions or items requiring follow-up, grounded in quotes, "None recorded" if none).

  Change `runStage2Mom`'s signature from `(anchors: QuoteAnchor[], meetingDate: string)` to `(anchors: QuoteAnchor[], meetingDate: string, meetingType: MeetingType)`. Restructure the systemPrompt's OUTPUT FORMAT block to keep the shared skeleton exactly as today (`# Minutes of Meeting`, `**Date:**`, `**Generated:**`, `## Attendees` with its guidance line), then interpolate `${MOM_SECTION_SPECS[meetingType]}`, then keep the `## Action Items` table block exactly as today (its table format and "None recorded" guidance are unchanged — this block simply moves to after the type-conditional sections instead of before `## Next Steps`, since `## Next Steps` itself is now inside `MOM_SECTION_SPECS.general` rather than the shared skeleton). Keep every ABSOLUTE RULES line, the "Meeting date" line, and the INPUT FORMAT line completely unchanged. Change the LLM call from `this.llmAdapter.generate(MoMSchema, 'minutes_of_meeting', systemPrompt, JSON.stringify({ anchors }))` to use `MoMGenerationSchema` instead of `MoMSchema`, then construct and return the final `MoM` object by combining the LLM result's `markdown_content` with `meeting_type: meetingType` — do not return the raw LLM result directly.

  Do not modify `runStage1`, `runStage2Summary`, `runStage2KeyPoints`, or `runStage2ActionItems` in any way — their systemPrompt strings, signatures, and call shapes must remain exactly as they are today (D-05/D-06).

  In `run()`, add `const meetingType = this.getMeetingType()` immediately after `const meetingDate = this.getMeetingDate()`. Add a new private method `getMeetingType(): MeetingType` mirroring `getMeetingDate()`'s exact shape: select `meeting_type` from the `meetings` table by `this.meetingId`, falling back to `'general'` when the row is missing or when the returned value is not a key of `MOM_SECTION_SPECS` (a defensive fallback covering any future/unrecognized value read back from the DB). In the `Promise.all` block, change only the `runStage2Mom` call to pass the new 3rd argument (`this.runStage2Mom(anchors, meetingDate, meetingType)`) — leave the `runStage2Summary`, `runStage2KeyPoints`, and `runStage2ActionItems` calls exactly as they are today, with no added argument.

  Update the two early-return `empty` `MeetingArtifacts` objects (transcript-empty and anchors-empty paths) to include `meeting_type: meetingType` in their `mom` sub-object, using the `meetingType` computed at the top of `run()` (both early returns happen after that computation). Update the top-level catch block's fallback `mom` object to include `meeting_type: 'general'` as a safe hardcoded default (the actual meeting type is not reliably available if the surrounding try block itself failed before reaching the DB read).
  </action>
  <acceptance_criteria>
  - `MOM_SECTION_SPECS` is exported, keyed by all 4 `MeetingType` values
  - `MoMGenerationSchema` is exported and does not include a `meeting_type` field
  - `getMeetingType()` exists as a private method with the same SELECT-by-`this.meetingId` shape as `getMeetingDate()`
  - `runStage2Mom`'s signature includes a 3rd `meetingType: MeetingType` parameter
  - `run()`'s `Promise.all` block calls `runStage2Summary`, `runStage2KeyPoints`, and `runStage2ActionItems` with no changes to their argument lists
  - All 4 `mom` object-literal sites in `run()` (2 early returns + catch fallback, plus the stamped `runStage2Mom` return) include a `meeting_type` value
  - `src/main/pipeline/ArtifactPipeline.ts` remains at exactly its 2 pre-existing baseline errors under `tsconfig.node.json` (unused `MeetingArtifactsSchema` import, unused `win` property) — no new errors introduced
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'export const MOM_SECTION_SPECS' src/main/pipeline/ArtifactPipeline.ts && grep -q 'export const MoMGenerationSchema' src/main/pipeline/ArtifactPipeline.ts && grep -q 'getMeetingType' src/main/pipeline/ArtifactPipeline.ts && test "$(npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep 'pipeline/ArtifactPipeline.ts' | grep -c 'error TS')" = "2"</automated>
  </verify>
  <done>runStage2Mom's OUTPUT FORMAT genuinely varies by meeting type via MOM_SECTION_SPECS, meeting_type is stamped programmatically on every return path, and every other Stage 1/Stage 2 method is untouched — structural correctness confirmed by the file-scoped compile gate; behavioral correctness is proven by Task 2's tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ArtifactPipeline behavioral tests</name>
  <files>tests/unit/ArtifactPipeline.test.ts</files>
  <behavior>
  - `MOM_SECTION_SPECS.standup` contains `## Yesterday`, `## Today`, `## Blockers` and does not contain `## Agenda Items Discussed` (proves per-key distinctness without a file-wide grep, sidestepping the fact that `## Agenda Items Discussed` legitimately exists elsewhere in the same file inside `MOM_SECTION_SPECS.general`)
  - `MOM_SECTION_SPECS['1:1']` contains `## Discussion Topics`, `## Feedback Themes`, `## Growth Notes`, `## Follow-ups`
  - `MOM_SECTION_SPECS.planning` contains `## Decisions`, `## Next Steps`, `## Open Questions`
  - `MOM_SECTION_SPECS.general` contains `## Agenda Items Discussed`, `## Key Discussion Points`, `## Decisions Made`, `## Next Steps`
  - `ArtifactPipeline.run()` against a seeded meeting with `meeting_type = 'standup'`: the captured systemPrompt for the `'minutes_of_meeting'`-named LLM call contains the standup headings and not the general headings
  - `ArtifactPipeline.run()` against a seeded meeting with `meeting_type = 'general'`: the captured systemPrompt for the `'minutes_of_meeting'`-named call contains the general headings (regression: today's behavior preserved)
  - Across two `run()` calls on meetings with different `meeting_type` but identical transcript content, the captured `'quote_anchors'`-named systemPrompt is byte-identical (TMPL-05/D-06)
  - Across the same two calls, the captured `'meeting_summary'`, `'key_points'`, and `'action_items'`-named systemPrompts are each byte-identical (D-05)
  - `run()`'s returned `mom.meeting_type` equals the seeded meeting's actual `meeting_type`, even though the mocked LLM response for the `'minutes_of_meeting'` call contains no `meeting_type` field at all (proves programmatic stamping, not LLM-echo — D-08)
  - `run()` against a seeded non-general meeting with zero `transcript_segments` rows (the empty-transcript early return): the returned `mom.meeting_type` matches the seeded type, not a hardcoded `'general'`
  </behavior>
  <read_first>
  tests/unit/CaptureService.test.ts (full file — the `vi.mock(...)` pattern for a class instantiated internally by the unit under test, used here as the precedent for mocking `LLMAdapter`)
  tests/unit/TranscriptStore.test.ts (the `openTestDb()` helper — the precedent for an in-memory DB seeded via `ALL_DDLS`/`runMigrations` imported directly from `src/main/store/db`)
  src/main/pipeline/ArtifactPipeline.ts (as modified by Task 1 of this plan — the exact exported symbol names `MOM_SECTION_SPECS`/`MoMGenerationSchema`, and the `llmAdapter.generate(schema, schemaName, systemPrompt, userContent)` call shape used by every Stage 1/Stage 2 method, needed to know which `schemaName` string identifies each captured call)
  src/main/llm/LLMAdapter.ts (the `generate&lt;T&gt;(schema, schemaName, systemPrompt, userContent): Promise&lt;z.output&lt;T&gt;&gt;` signature being mocked)
  </read_first>
  <action>
  Create `tests/unit/ArtifactPipeline.test.ts`. Mock `../../src/main/llm/LLMAdapter` with `vi.fn().mockImplementation(...)` returning an object whose `generate` method is a `vi.fn()` that inspects its `schemaName` argument and returns a canned response per name: `'quote_anchors'` returns `{ anchors: [ ... a couple of representative QuoteAnchor objects with concrete quote_full/speaker_label/artifact_hint values ... ] }`; `'minutes_of_meeting'` returns `{ markdown_content: '# stub mom' }` (deliberately no `meeting_type` key, to prove D-08's programmatic-stamping requirement); `'meeting_summary'` returns `{ summary_text: 'stub summary' }`; `'key_points'` returns `{ key_points: [] }`; `'action_items'` returns `{ action_items: [] }`. Record every call's `(schemaName, systemPrompt)` pair into an array so tests can inspect exactly what prompt was sent for each named call, across multiple `run()` invocations.

  Build an in-memory DB helper mirroring `TranscriptStore.test.ts`'s `openTestDb()` (import `ALL_DDLS`, `runMigrations` from `../../src/main/store/db`, `new Database(':memory:')`, `sqliteVec.load(db)`, `db.exec(ALL_DDLS)`, `runMigrations(db)`). Seed `meetings` rows directly via `db.prepare('INSERT INTO meetings (id, started_at, meeting_type) VALUES (?, ?, ?)')` with distinct ids/types, and seed `transcript_segments` rows for meetings that need non-empty transcripts. Construct `ArtifactPipeline` with a minimal mock `BrowserWindow` (`{ webContents: { send: vi.fn() } }`, matching `CaptureService.test.ts`'s `makeMockWin()` pattern) — `ArtifactStore.saveArtifacts` writes to the same in-memory `db`, so no separate mock is needed for it.

  Write the test cases described in the `&lt;behavior&gt;` block above, organized as: a `describe('MOM_SECTION_SPECS', ...)` block covering the 4 pure content-shape assertions (no DB/LLM mocking needed — direct import and `.toContain()`/`.not.toContain()` checks); a `describe('run() — type-conditional MOM generation', ...)` block covering the remaining behavioral assertions, each calling `pipeline.run()` against a freshly-constructed `ArtifactPipeline` scoped to a specific seeded meeting id.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ArtifactPipeline.test.ts</automated>
    <human-check>Start a meeting, select 'Standup' in ConsentGate, speak a sentence describing a blocker (e.g. "I'm blocked on the API review"), end the meeting. In the Complete-state ArtifactReview screen, open the MOM section and verify it shows `## Yesterday`, `## Today`, `## Blockers` headings — not `Agenda Items Discussed`/`Key Discussion Points`/`Decisions Made`. Verify Summary, Key Points, and Action Items sections still render normally, unaffected by the meeting type. Start a second meeting, leave 'General' selected (no extra click), complete it, and verify its MOM still shows the original `Agenda Items Discussed`/`Key Discussion Points`/`Decisions Made`/`Next Steps` headings (regression check).</human-check>
  </verify>
  <done>ArtifactPipeline.test.ts proves, independent of any live LLM call, that MOM generation is genuinely type-conditional (TMPL-03/TMPL-04), Stage 1 and every other Stage 2 prompt are unaffected by meeting type (TMPL-05/D-05/D-06), and meeting_type is stamped programmatically and correctly on every return path (D-08) — plus a human-check proving the same end-to-end through the real UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `meetings.meeting_type` (DB read) -> LLM prompt construction | The value read back from the DB (already CHECK-constrained and IPC-validated by Plans 13-01/13-03) drives which section spec gets interpolated into the Stage 2 MOM prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Tampering | `ArtifactPipeline.getMeetingType()` / `MOM_SECTION_SPECS` lookup | low | mitigate | `getMeetingType()` falls back to `'general'` when the DB value is missing or is not a recognized key of `MOM_SECTION_SPECS`, preventing an `undefined` section-spec from being interpolated into the LLM prompt (e.g. from a manually edited DB file, or a future schema value not yet added to this lookup) |
| T-13-07 | Information Disclosure | `mom.meeting_type` in `content_json` | low | accept | The stamped value duplicates data already present in the `meetings` row (no new exposure); the MOM markdown content itself remains governed by the existing Stage 1 -> Stage 2 grounding contract, which this plan does not weaken (D-05/D-06 keep Stage 1 and every other extraction prompt unchanged) |
</threat_model>

<verification>
`npm test` remains green (142 pre-existing + this plan's new `tests/unit/ArtifactPipeline.test.ts` suite). Run `npx vitest run tests/unit/ArtifactPipeline.test.ts` for a fast targeted check, then `npm test` for the full-suite regression pass, then the end-of-phase human-check for the real end-to-end flow (requires Plans 13-02 and 13-03 already executed, which wave-sequencing guarantees by the time this phase's UAT runs).
</verification>

<success_criteria>
Standup/1:1/Planning meetings produce a MOM with genuinely distinct section headings from General's; Stage 1 and the Summary/Key Points/Action Items prompts are provably unaffected by meeting type; every `mom.meeting_type` in every code path (normal, empty-transcript, empty-anchors, error fallback) reflects the correct value.
</success_criteria>

<output>
Create `.planning/phases/13-meeting-type-artifact-templates/13-04-SUMMARY.md` when done
</output>
