---
phase: 13-meeting-type-artifact-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ConsentGate.tsx
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "User sees a row of 4 segmented buttons (General / Standup / 1:1 / Planning) above the existing consent checkbox on the ConsentGate screen, with General pre-selected by default (D-01)"
    - "Selecting a meeting type requires no extra click beyond what starting a meeting already required — TMPL-02's 'optional, non-blocking' bar is met by construction, since General is already selected and Start Meeting is unaffected"
    - "The `consent-confirmed` IPC payload includes the selected meetingType alongside the existing meetingId/timestamp fields"
  artifacts:
    - "src/renderer/src/components/ConsentGate.tsx — new `meetingType` component state, 4-button segmented selector, extended `consent-confirmed` invoke payload"
  key_links:
    - "ConsentGate's `handleConfirm()` sends `meetingType` as part of the `consent-confirmed` invoke call — Plan 13-03's IPC handler is the consumer on the other end of this channel"
---

## Artifacts This Phase Produces (this plan's share)

- `ConsentGate.tsx`'s new `meetingType` component state (`useState<'general' | 'standup' | '1:1' | 'planning'>('general')`)
- The 4-button segmented meeting-type selector rendered above the consent checkbox
- `consent-confirmed` IPC invoke payload's new `meetingType` key

<objective>
Add the session-start meeting-type selector to `ConsentGate.tsx` per D-01: a row of 4 segmented buttons (General / Standup / 1:1 / Planning) above the existing consent checkbox, General pre-selected, and thread the selection into the existing `consent-confirmed` IPC payload.

Purpose: This is the only user-visible entry point for TMPL-01/TMPL-02 — without it, a meeting type can never be selected, no matter what the backend does with the value.
Output: A working segmented selector in `ConsentGate.tsx` whose selection is included in every `consent-confirmed` invoke call.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-meeting-type-artifact-templates/13-CONTEXT.md
@.planning/phases/13-meeting-type-artifact-templates/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meeting-type segmented selector in ConsentGate</name>
  <files>src/renderer/src/components/ConsentGate.tsx</files>
  <read_first>
  src/renderer/src/components/ConsentGate.tsx (full file — the `agreed` useState declaration at line 59, `handleConfirm()`'s current `consent-confirmed` payload at lines 66-73, the `PermissionWarningCard`'s "Fix in System Preferences" button style block at lines 31-48, and the "Start Meeting" button's disabled/enabled toggle style at lines 105-117 — both are the inline-style-object precedent to follow for the new buttons)
  </read_first>
  <action>
  Add `const [meetingType, setMeetingType] = useState&lt;'general' | 'standup' | '1:1' | 'planning'&gt;('general')` immediately after the existing `const [agreed, setAgreed] = useState(false)` line. Use this inline literal union type directly (do not import `MeetingType` from `shared/schemas` — this keeps the component self-contained and matches the file's existing convention of zero cross-boundary type imports).

  Render a row of 4 buttons with the exact visible labels "General", "Standup", "1:1", and "Planning" (in that order), positioned after the existing disclosure `&lt;p&gt;` paragraph (ending "...without your explicit action.") and before the existing consent `&lt;label&gt;` checkbox block. Each button's `onClick` calls `setMeetingType` with its corresponding literal value (`'general'`, `'standup'`, `'1:1'`, `'planning'`). Follow the file's existing flat inline-`style={{...}}`-object convention (no CSS framework, no external classnames) — apply a visually distinct "selected" treatment (e.g. a filled/accent background and border) when `meetingType` equals that button's value, and a neutral/outline treatment otherwise, matching the spacing/fontSize/borderRadius tokens already used elsewhere in this file (`fontSize: '11px'`-`'13px'` range, `borderRadius: '4px'`, `padding` in the 3px-8px range for compact buttons). Lay the 4 buttons out in a flex row with a small gap, matching the existing `display: 'flex'`/`gap` pattern used by the consent `&lt;label&gt;` block.

  Update `handleConfirm()`'s `consent-confirmed` invoke payload object to add a third key: `meetingType` (the current state value), alongside the existing `meetingId: crypto.randomUUID()` and `timestamp: Date.now()` keys.
  </action>
  <acceptance_criteria>
  - `useState` for `meetingType` is declared, initialized to `'general'`
  - The visible button label strings General, Standup, 1:1, and Planning each appear in the file (as JSX text content, not necessarily quoted)
  - The `consent-confirmed` invoke call's payload object includes a `meetingType` key
  - `src/renderer/src/components/ConsentGate.tsx` introduces zero new TypeScript errors under `tsconfig.web.json` (this file has zero pre-existing baseline errors)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'General' src/renderer/src/components/ConsentGate.tsx && grep -q 'Standup' src/renderer/src/components/ConsentGate.tsx && grep -q '1:1' src/renderer/src/components/ConsentGate.tsx && grep -q 'Planning' src/renderer/src/components/ConsentGate.tsx && grep -q 'meetingType' src/renderer/src/components/ConsentGate.tsx && test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep 'ConsentGate.tsx' | grep -c 'error TS')" = "0"</automated>
    <human-check>Open the app to the ConsentGate screen (Idle -> Start Meeting -> PreCapture). Verify 4 segmented buttons labeled General/Standup/1:1/Planning appear above the consent checkbox, with General visually pre-selected. Click each of the other 3 buttons in turn and verify the visual selection updates each time (only one selected at a time). Verify the disclosure text, permission warnings (if any), consent checkbox, and Start Meeting button all still render and behave exactly as before — no extra required click was introduced (TMPL-02).</human-check>
  </verify>
  <done>ConsentGate renders the 4-way segmented meeting-type selector above the consent checkbox, General is pre-selected by default, and every `consent-confirmed` invoke includes the currently selected meetingType.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer -> Main via `consent-confirmed` IPC | The `meetingType` value crosses this boundary as part of an already-allowlisted invoke channel (no new channel added) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Tampering | ConsentGate.tsx meeting-type selector | low | mitigate | The selector exposes only 4 hardcoded button-triggered state transitions (`setMeetingType('general'\|'standup'\|'1:1'\|'planning')`) — there is no free-text input anywhere in this control, eliminating injection surface at the UI layer by construction. The main-process IPC handler (Plan 13-03) independently validates the received value regardless, since a compromised or stale renderer build could still send an arbitrary string. |
</threat_model>

<verification>
`npm test` remains green (this plan touches only a renderer file, which `vitest.config.ts` explicitly excludes from automated coverage). The TypeScript-baseline check plus the human-check above are this plan's verification path, matching this project's established renderer-component convention (see Phase 12's `12-04-PLAN.md`).
</verification>

<success_criteria>
A user sees the 4-way meeting-type selector on the ConsentGate screen, General pre-selected, no extra required interaction, and the selection reaches the main process via the existing `consent-confirmed` channel.
</success_criteria>

<output>
Create `.planning/phases/13-meeting-type-artifact-templates/13-02-SUMMARY.md` when done
</output>
