---
phase: 13-meeting-type-artifact-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/store/db.ts
  - src/shared/schemas/index.ts
  - src/main/transcript/TranscriptStore.ts
  - tests/unit/TranscriptStore.test.ts
  - tests/unit/schemas-meeting-type.test.ts
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "A `meetings` row always has a valid `meeting_type` value — either explicitly set at creation or defaulting to 'general' (satisfies TMPL-02's non-blocking default)"
    - "An already-installed database (created before this phase shipped) upgrades safely via migration — no data loss, no crash, existing rows read back with meeting_type = 'general'"
    - "The 4 meeting-type values ('general', 'standup', '1:1', 'planning') are the single source of truth for both the DB CHECK constraint and the TypeScript type system — no risk of drift between the two"
  artifacts:
    - "src/main/store/db.ts — `meetings` DDL gains a `meeting_type` column; `runMigrations()` gains a third column-guard block for pre-existing installs"
    - "src/shared/schemas/index.ts — `MeetingTypeSchema`/`MeetingType` export; `MoMSchema` gains a required `meeting_type` field"
    - "src/main/transcript/TranscriptStore.ts — `createMeeting()` gains a third `meetingType` parameter (default `'general'`)"
  key_links:
    - "TranscriptStore.createMeeting()'s new 3rd parameter binds directly to the `meeting_type` column added to the `meetings` DDL in this same plan — Plan 13-03 threads the actual user-selected value through this call"
    - "MoMSchema.meeting_type (this plan) is the field ArtifactPipeline.runStage2Mom (Plan 13-04) stamps after generation — the shared type MeetingType is the contract both plans build against"
---

## Artifacts This Phase Produces (this plan's share)

- `meetings.meeting_type` DB column — `TEXT NOT NULL DEFAULT 'general' CHECK (meeting_type IN ('general','standup','1:1','planning'))`, added to both the fresh-install DDL and a `runMigrations()` guard for existing installs
- `MeetingTypeSchema` (zod) / `MeetingType` (TS type) — exported from `src/shared/schemas/index.ts`, the single source of truth for the 4 meeting-type values, consumed by Plans 13-02/13-03/13-04
- `MoMSchema.meeting_type` — new required field on the existing `MoMSchema`
- `TranscriptStore.createMeeting(meetingId, startedAt, meetingType = 'general')` — extended signature; the 3rd parameter is new

<objective>
Lay the storage foundation for meeting-type variance: add the `meeting_type` column to the `meetings` table (fresh install + migration for existing installs), export a shared `MeetingType` zod type as the single source of truth for the 4 allowed values, extend `MoMSchema` to carry `meeting_type` (per D-08), and extend `TranscriptStore.createMeeting()` to persist it.

Purpose: Every other plan in this phase (UI selector, IPC propagation, MOM generation) depends on this column existing, this type being importable, and `createMeeting()` accepting the value — this is the leaf-first foundation the rest of the phase builds on (per D-07/D-08).
Output: A `meeting_type`-aware `meetings` table (new + migrated installs), a shared `MeetingType` type, and `TranscriptStore.createMeeting()` able to persist any of the 4 values.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-meeting-type-artifact-templates/13-CONTEXT.md
@.planning/phases/13-meeting-type-artifact-templates/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: meetings.meeting_type DDL + migration</name>
  <files>src/main/store/db.ts, tests/unit/TranscriptStore.test.ts</files>
  <read_first>
  src/main/store/db.ts (full file — the `meetings` table block inside `ALL_DDLS` at lines 12-20, and the `runMigrations()` function at lines 126-145, which already has two precedent column-guard blocks: `transcript_segments.confidence` and `action_items.is_calendar_event`)
  tests/unit/TranscriptStore.test.ts (full file — the `openTestDb()` helper at the top which imports `ALL_DDLS`/`runMigrations` directly from `src/main/store/db`, and the existing `describe('speaker_aliases DDL', ...)` block at the bottom, which is the closest precedent for a raw-DDL-level test in this file)
  </read_first>
  <action>
  In `src/main/store/db.ts`'s `ALL_DDLS` string, add a new column to the `meetings` table definition, immediately after the `raw_audio_path TEXT,` line and before `created_at`: `meeting_type TEXT NOT NULL DEFAULT 'general' CHECK (meeting_type IN ('general','standup','1:1','planning'))`. This has been empirically verified against this project's exact SQLite build (3.53.2, bundled with `better-sqlite3-multiple-ciphers` 12.11.1) to work correctly both as an inline `CREATE TABLE` column and via `ALTER TABLE ADD COLUMN` — the `'1:1'` value requires no escaping (a colon has no special meaning inside a single-quoted SQL string literal).

  In `runMigrations()`, add a third column-guard block immediately after the existing `action_items`/`is_calendar_event` block, following the identical `table_info` pragma + `.some()` + `runSafe(...)` shape used by the two existing blocks: read `table_info(meetings)` into a `meetingCols` array, check whether any entry's `name` equals `meeting_type`, and if not, call `runSafe` with the exact ALTER statement `ALTER TABLE meetings ADD COLUMN meeting_type TEXT NOT NULL DEFAULT 'general' CHECK (meeting_type IN ('general','standup','1:1','planning'))` — the same column definition as the fresh-install DDL, so both paths converge on an identical schema.

  In `tests/unit/TranscriptStore.test.ts`, add a new top-level `describe('meeting_type column (DDL + migration)', ...)` block, placed immediately after the existing `describe('speaker_aliases DDL', ...)` block (same nesting level, using the same `db`/`store` variables already set up by the outer `beforeEach`). Cover: (1) a fresh install via `ALL_DDLS` — insert a `meetings` row without specifying `meeting_type` (raw `db.prepare('INSERT INTO meetings (id, started_at) VALUES (?, ?)')`), read `meeting_type` back, assert it equals `'general'`; (2) the CHECK constraint accepts all 4 values — insert 4 rows, one per value in `['general','standup','1:1','planning']`, each with a distinct `id`, asserting none throw; (3) the CHECK constraint rejects an invalid value — assert that inserting a row with `meeting_type = 'invalid-type'` throws; (4) migration idempotency, mirroring the existing `'runMigrations is idempotent...'` test's exact shape — call `runMigrations(db)` a second time (it already ran once inside `openTestDb()`), assert it does not throw, and assert `table_info(meetings)` contains exactly one column named `meeting_type`.
  </action>
  <acceptance_criteria>
  - `ALL_DDLS`'s `meetings` table definition contains the exact substring `meeting_type TEXT NOT NULL DEFAULT 'general' CHECK (meeting_type IN ('general','standup','1:1','planning'))`
  - `runMigrations()` contains a `table_info(meetings)` pragma call and a `meeting_type` column-name check
  - `npx vitest run tests/unit/TranscriptStore.test.ts` passes, including the 4 new test cases described above
  - `src/main/store/db.ts` introduces zero new TypeScript errors (this file has zero pre-existing errors under `tsconfig.node.json`)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/TranscriptStore.test.ts && test "$(npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep 'store/db.ts' | grep -c 'error TS')" = "0"</automated>
  </verify>
  <done>Fresh-install and migrated databases both have a `meeting_type` column on `meetings`, defaulting to 'general', CHECK-constrained to the 4 allowed values, proven by tests that exercise both the fresh-DDL path and the migration path independently.</done>
</task>

<task type="auto">
  <name>Task 2: MeetingTypeSchema + MoMSchema.meeting_type</name>
  <files>src/shared/schemas/index.ts, tests/unit/schemas-meeting-type.test.ts</files>
  <read_first>
  src/shared/schemas/index.ts (full file — the `TranscriptSegmentSchema`/`QuoteAnchorSchema` schema-const-plus-z.infer-type-alias pairing convention used throughout, `QuoteAnchorSchema.confidence`'s `z.enum([...])` usage as the closest analog for a closed string-set schema, and the current `MoMSchema` definition)
  tests/unit/schemas-epoch.test.ts (full file — the exact structure to replicate: `describe` block per schema, `.parse()` success cases, `.parse()`-throws-`ZodError` failure cases via `expect(() => Schema.parse(...)).toThrow(ZodError)`)
  </read_first>
  <action>
  In `src/shared/schemas/index.ts`, add `export const MeetingTypeSchema = z.enum(['general', 'standup', '1:1', 'planning'])` and `export type MeetingType = z.infer&lt;typeof MeetingTypeSchema&gt;` immediately after `TranscriptSegmentSchema`'s type export (before `QuoteAnchorSchema`), matching the file's established schema-const-plus-type-alias pairing convention. This becomes the single source of truth for the 4 meeting-type values — Plan 13-01's Task 1 DDL/migration SQL strings are hand-written to match this list exactly, and Plans 13-02/13-03/13-04 all import `MeetingType` (and, where runtime validation is needed, `MeetingTypeSchema`) from here rather than redefining the union.

  Extend the existing `MoMSchema` (currently `z.object({ markdown_content: z.string() })`) to also include `meeting_type: MeetingTypeSchema` as a required field (per D-08 — the artifact's `content_json` must carry the `meeting_type` that produced it).

  Create `tests/unit/schemas-meeting-type.test.ts` (new file), following `tests/unit/schemas-epoch.test.ts`'s exact import/structure pattern. Cover: `MeetingTypeSchema.parse(...)` accepts each of the 4 literal values `'general'`, `'standup'`, `'1:1'`, `'planning'`; `MeetingTypeSchema.safeParse('bogus').success` is `false`; `MoMSchema.parse({ markdown_content: '...', meeting_type: 'standup' })` succeeds and returns the expected shape; `MoMSchema.parse({ markdown_content: '...' })` (missing `meeting_type`) throws `ZodError`; `MoMSchema.parse({ markdown_content: '...', meeting_type: 'invalid' })` throws `ZodError`.
  </action>
  <acceptance_criteria>
  - `MeetingTypeSchema` is exported as `z.enum(['general', 'standup', '1:1', 'planning'])` and `MeetingType` is exported via `z.infer`
  - `MoMSchema.shape.meeting_type` exists and is required (not `.optional()`)
  - `npx vitest run tests/unit/schemas-meeting-type.test.ts` passes with all 5 cases described above
  - `src/shared/schemas/index.ts` introduces zero new errors under `tsconfig.node.json` (baseline 0) and remains at exactly its existing 1 pre-existing baseline error under `tsconfig.web.json` (an unrelated `SessionState` re-export file-list issue, not caused by this change)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/schemas-meeting-type.test.ts && test "$(npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep 'shared/schemas' | grep -c 'error TS')" = "0" && test "$(npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep 'shared/schemas' | grep -c 'error TS')" = "1"</automated>
  </verify>
  <done>MeetingTypeSchema/MeetingType are the single exported source of truth for the 4 values; MoMSchema requires meeting_type; both are proven by direct schema-level unit tests independent of any consumer.</done>
</task>

<task type="auto">
  <name>Task 3: TranscriptStore.createMeeting(meetingType)</name>
  <files>src/main/transcript/TranscriptStore.ts, tests/unit/TranscriptStore.test.ts</files>
  <read_first>
  src/main/transcript/TranscriptStore.ts (full file — the constructor's `insertMeetingStmt` preparation at lines 22-24 and the `createMeeting()` method at lines 33-35)
  tests/unit/TranscriptStore.test.ts (the existing `it('createMeeting inserts a meetings row', ...)` test at lines 51-56 — the insertion point for the two new tests described below)
  </read_first>
  <action>
  In `src/main/transcript/TranscriptStore.ts`, import `type { MeetingType } from '../../shared/schemas'`. Update the `insertMeetingStmt` prepared statement from `'INSERT INTO meetings (id, started_at, created_at) VALUES (?, ?, ?)'` to `'INSERT INTO meetings (id, started_at, created_at, meeting_type) VALUES (?, ?, ?, ?)'`. Update `createMeeting(meetingId: string, startedAt: number)` to `createMeeting(meetingId: string, startedAt: number, meetingType: MeetingType = 'general')`, passing `meetingType` as the 4th bound parameter in the `.run(...)` call, after `Date.now()`.

  In `tests/unit/TranscriptStore.test.ts`, add two new `it` blocks immediately after the existing `'createMeeting inserts a meetings row'` test: `'createMeeting stores an explicit meetingType'` — call `store.createMeeting('mtg-standup', Date.now(), 'standup')`, read the `meeting_type` column back for that id, assert it equals `'standup'`; `'createMeeting without a meetingType argument defaults to general'` — call `store.createMeeting('mtg-default', Date.now())` (2 args, matching the existing test's call shape), read `meeting_type` back, assert it equals `'general'`.
  </action>
  <acceptance_criteria>
  - `createMeeting`'s signature is `createMeeting(meetingId: string, startedAt: number, meetingType: MeetingType = 'general'): void`
  - `insertMeetingStmt`'s SQL string includes `meeting_type` as a 4th inserted column
  - `npx vitest run tests/unit/TranscriptStore.test.ts` passes, including the 2 new tests plus the 4 from Task 1 (6 new test cases total in this file across both tasks, on top of the pre-existing suite)
  - `src/main/transcript/TranscriptStore.ts` introduces zero new errors under `tsconfig.node.json` (baseline 0)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/TranscriptStore.test.ts && test "$(npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep 'transcript/TranscriptStore.ts' | grep -c 'error TS')" = "0"</automated>
  </verify>
  <done>TranscriptStore.createMeeting persists an explicit meetingType or defaults to 'general' when omitted, proven by tests exercising both call shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DB layer (SQLite CHECK constraint) | Final backstop against any malformed `meeting_type` value reaching persisted storage, regardless of what calls `TranscriptStore.createMeeting()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | `meetings.meeting_type` column | low | mitigate | `CHECK (meeting_type IN ('general','standup','1:1','planning'))` on both the fresh-install DDL and the migration ALTER statement rejects any value outside the 4 allowed types at the DB layer — a defense-in-depth backstop beneath the IPC-layer validation added in Plan 13-03 |
| T-13-02 | Information Disclosure | `meetings.meeting_type` value | low | accept | Stored only inside the existing local SQLCipher AES-256 encrypted database (same encryption boundary as every other column in this table) — no new data exposure surface introduced |
</threat_model>

<verification>
`npm test` remains fully green (142 pre-existing tests + 10 new: 4 DDL/migration + 5 schema + 2 createMeeting, minus overlap already counted). Run `npx vitest run tests/unit/TranscriptStore.test.ts tests/unit/schemas-meeting-type.test.ts` for a fast targeted check, then `npm test` for the full-suite regression pass.
</verification>

<success_criteria>
The `meetings` table (fresh or migrated) always has a valid, CHECK-constrained `meeting_type` defaulting to `'general'`; `MeetingTypeSchema`/`MeetingType` is the single shared source of truth for the 4 values; `MoMSchema` requires `meeting_type`; `TranscriptStore.createMeeting()` can persist any of the 4 values or default to `'general'` — all proven by direct, LLM-independent unit tests.
</success_criteria>

<output>
Create `.planning/phases/13-meeting-type-artifact-templates/13-01-SUMMARY.md` when done
</output>
