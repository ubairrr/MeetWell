---
phase: 11-packaging-eval-harness
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - electron-builder.yml
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-03a
must_haves:
  truths:
    - electron-builder.yml asarUnpack entries resolve to the correct native binary paths (better-sqlite3-multiple-ciphers .node + audiotee binary); verified by inspecting the built .app bundle's app.asar.unpacked/ directory
    - electron-builder --mac --dir produces a .app bundle without build errors
    - The .app bundle launches without a DB initialization error or entitlement error (verified by running it manually or via open command)
    - npmRebuild is true in electron-builder.yml so native modules are rebuilt against Electron's Node ABI during packaging
    - The mac.identity field is NOT hardcoded — electron-builder auto-discovers the code-signing certificate from the macOS keychain
  artifacts:
    - electron-builder.yml (modified if asarUnpack or other entries need correction)
  key_links:
    - 11-07 (final DMG build) depends on this plan's .app bundle verification passing
---

<objective>
Audit and finalize the electron-builder configuration, rebuild native modules against Electron's Node ABI, and run the packaging smoke test (`electron-builder --mac --dir`). This verifies the .app bundle compiles and launches correctly before the full signed DMG is produced in 11-07.

This plan does NOT produce a signed or notarized DMG — just a local `.app` directory build. The `--dir` flag skips DMG creation and code-signing, making it faster and suitable for smoke testing.

Output: verified working .app bundle; electron-builder.yml finalized; 11-03-SUMMARY.md documenting any config corrections made.
</objective>

<execution_context>
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/workflows/execute-plan.md
@/Users/ubair/Gits/MeetingAssist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/ubair/Gits/MeetingAssist/.planning/phases/11-packaging-eval-harness/11-CONTEXT.md
@/Users/ubair/Gits/MeetingAssist/.planning/phases/11-packaging-eval-harness/11-RESEARCH.md
@/Users/ubair/Gits/MeetingAssist/electron-builder.yml
@/Users/ubair/Gits/MeetingAssist/package.json
@/Users/ubair/Gits/MeetingAssist/build/entitlements.mac.plist
@/Users/ubair/Gits/MeetingAssist/scripts/notarize.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit asarUnpack entries and extraResources</name>
  <files>electron-builder.yml</files>
  <behavior>
    - All asarUnpack entries correctly resolve to binaries that are actually in node_modules
    - audiotee binary is in extraResources (from node_modules/audiotee/bin/audiotee → audiotee in the bundle), AND its destination path is in asarUnpack
    - better-sqlite3-multiple-ciphers .node file is covered by asarUnpack glob patterns
    - sqlite-vec platform-specific packages are covered
    - The files section doesn't accidentally exclude any required output files
  </behavior>
  <action>
Run the following audit steps:

1. Verify audiotee binary exists:
```bash
ls node_modules/audiotee/bin/audiotee 2>/dev/null && echo "audiotee found" || echo "audiotee NOT found"
```

2. Verify better-sqlite3-multiple-ciphers native binary:
```bash
find node_modules/better-sqlite3-multiple-ciphers -name "*.node" | head -5
```

3. Verify sqlite-vec binaries:
```bash
ls node_modules/sqlite-vec-darwin-arm64/ 2>/dev/null || echo "no arm64"
ls node_modules/sqlite-vec-darwin-x64/ 2>/dev/null || echo "no x64"
```

4. Check that extraResources `from` path matches the actual audiotee binary location.

If any path in asarUnpack does not match an actual file, correct the entry. Common fixes:
- If audiotee is in `resources/audiotee` in the bundle, the asarUnpack entry `'resources/audiotee'` is already correct.
- If better-sqlite3-multiple-ciphers .node is not matched by `'**/node_modules/better-sqlite3-multiple-ciphers/**/*.node'`, add a more specific pattern.

If all entries are correct, no change is needed to electron-builder.yml.

Also verify: `npmRebuild: true` is set (it is; just confirm and document).
  </action>
  <verify>
    <automated>node -e "const yaml=require('js-yaml');const fs=require('fs');const cfg=yaml.load(fs.readFileSync('electron-builder.yml','utf8'));if(!cfg.npmRebuild)throw new Error('npmRebuild missing');if(!cfg.asarUnpack||cfg.asarUnpack.length===0)throw new Error('asarUnpack empty');console.log('asarUnpack entries:',cfg.asarUnpack.length,'npmRebuild:',cfg.npmRebuild)"</automated>
  </verify>
  <done>asarUnpack entries verified against actual node_modules paths; any incorrect entries corrected; npmRebuild: true confirmed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build (npm run build)</name>
  <files></files>
  <behavior>
    - `npm run build` (which runs electron-vite build) completes without errors
    - The `out/` directory is populated with compiled main, preload, and renderer output
    - TypeScript compilation succeeds (no tsc errors in the build output)
  </behavior>
  <action>
Run the production build:
```bash
npm run build 2>&1
```

If build fails:
- TypeScript errors: fix them (do not use --skipLibCheck workarounds — fix the actual types)
- Missing modules: check that all imports resolve correctly
- Vite errors: check the electron.vite.config.ts for misconfigurations

Common issues to check:
- `src/main/index.ts` changes from 11-01 may introduce type errors if `dialog` or `systemPreferences` imports were added incorrectly
- The `os` module import must be compatible with Electron's Node bundling

Do not proceed to Task 3 if the build fails.
  </action>
  <verify>
    <automated>test -d out/main &amp;&amp; test -d out/renderer &amp;&amp; echo "build output exists" || echo "BUILD FAILED"</automated>
  </verify>
  <done>out/ directory exists with main/, renderer/, preload/ subdirectories; npm run build exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Packaging smoke test — electron-builder --mac --dir</name>
  <files></files>
  <behavior>
    - `npx electron-builder --mac --dir` completes without error
    - A `.app` bundle is produced in `dist/mac-arm64/` or `dist/mac/` (depending on architecture)
    - The .app bundle's Resources/ directory contains the audiotee binary (from extraResources)
    - The .app bundle's app.asar.unpacked/ directory contains the better-sqlite3-multiple-ciphers .node file and sqlite-vec binaries
    - The app launches without crashing (manual verification: `open dist/mac-arm64/MeetingAssist.app`)
  </behavior>
  <action>
Run the packaging smoke test:
```bash
npx electron-builder --mac --dir 2>&1
```

After completion, verify the bundle structure:
```bash
# Check audiotee binary in resources
ls "dist/mac-arm64/MeetingAssist.app/Contents/Resources/audiotee" 2>/dev/null || \
ls "dist/mac/MeetingAssist.app/Contents/Resources/audiotee" 2>/dev/null || \
echo "audiotee NOT found in bundle"

# Check native modules in asar.unpacked
find dist/ -name "*.node" -path "*/asar.unpacked/*" | head -10
```

Then attempt to launch the app:
```bash
open "dist/mac-arm64/MeetingAssist.app" 2>/dev/null || open "dist/mac/MeetingAssist.app" 2>/dev/null
```

Wait 5 seconds and check if the process is running:
```bash
pgrep -f "MeetingAssist" && echo "App launched successfully" || echo "App did not start"
```

Document the result in 11-03-SUMMARY.md:
- Whether the .app bundle built successfully
- Whether audiotee and native .node files are in the correct locations
- Whether the app launched without crash

If the app crashes:
- Check the crash log in ~/Library/Logs/DiagnosticReports/
- The most common cause is a missing asarUnpack entry for a native binary — fix electron-builder.yml and retry
  </action>
  <verify>
    <automated>find dist/ -name "MeetingAssist.app" | head -1 | xargs -I{} test -d {} &amp;&amp; echo "app bundle found" || echo "no app bundle"</automated>
  </verify>
  <done>.app bundle exists in dist/; audiotee binary is in Resources/; native .node files are in asar.unpacked/; app launches without immediate crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output → packaged .app | Native binaries copied from node_modules into .app bundle; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-A | Tampering | asarUnpack / native binaries | low | accept | Native binaries are unsigned in --dir smoke test; hardened runtime entitlements (disable-library-validation) allow this for the audiotee binary. Full signing happens in 11-07. |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `find dist/ -name 'MeetingAssist.app' | wc -l` returns at least 1
3. `find dist/ -name 'audiotee' | wc -l` returns at least 1
4. `find dist/ -name '*.node' -path '*/asar.unpacked/*' | wc -l` returns at least 1
5. `npx tsc --noEmit` exits 0 (type check passes after build)
</verification>

<success_criteria>
- npm run build exits 0
- electron-builder --mac --dir produces a .app bundle without errors
- .app bundle contains audiotee in Resources/ and better-sqlite3-multiple-ciphers .node in app.asar.unpacked/
- App launches without a DB error or entitlement error (PACK-03a satisfied)
- Any asarUnpack corrections are documented in 11-03-SUMMARY.md
</success_criteria>

<output>
Create `.planning/phases/11-packaging-eval-harness/11-03-SUMMARY.md` when done
</output>
